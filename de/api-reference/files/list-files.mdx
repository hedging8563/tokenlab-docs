---
title: "Dateien auflisten"
openapi: "GET /v1/files"
description: "Listet hochgeladene Dateien für Batch-Workflows und unterstützte dateibasierte Kompatibilitätsmodi auf."
---

## Übersicht

Dieser Endpunkt dient in erster Linie TokenLabs OpenAI-kompatibler Batch-API, einschließlich hochgeladener Batch-Eingaben sowie generierter Ausgabe- oder Fehlerdateien.

Wenn die Anfrage in den Anthropic-Files-Kompatibilitätsmodus wechselt, kann dieselbe Route außerdem Anthropic-ähnliche Dateien zurückgeben, die für den gebundenen Workspace verfügbar sind.


Ohne Anthropic-Header kann die Liste sowohl Batch-Dateien als auch TokenLab-Bild-/Referenzdateien enthalten. Nutze `purpose=batch`, `purpose=batch_output`, `purpose=user_data` oder `purpose=vision`, um die Liste einzugrenzen.
## Hinweise

- Batch-Eingabedateien müssen `purpose=batch` verwenden.
- Verwenden Sie `purpose=batch_output`, um sich auf erzeugte Ausgabedateien zu konzentrieren.
- `completion_window` beträgt derzeit `24h`.
- Die Ausgabe-Reihenfolge ist nicht garantiert; immer nach `custom_id` abgleichen.
- Streaming wird innerhalb von Batch-Elementen nicht unterstützt.

## Beispiel

<RequestExample>
```bash cURL
curl -X GET "https://api.tokenlab.sh/v1/files?purpose=batch&limit=20" \
  -H "Authorization: Bearer sk-your-api-key"
```
</RequestExample>

## Anfrage / Antwort

Verwenden Sie das interaktive OpenAPI-Panel oben für das genaue Schema.

## Betriebliche Tipps

- Verwenden Sie `custom_id` für idempotente nachgelagerte Abstimmungen.
- Erwarten Sie `output_file_id` und `error_file_id` erst, nachdem der Worker den Batch abgeschlossen hat.
- Die Preise für Batches können von den Preisen für synchrone Anfragen abweichen, da die Rabattregeln für `isBatchRequest=true` gelten.

## Antwortbeispiel

<ResponseExample>
```json 200 OK
{
  "object": "list",
  "data": [{ "id": "file_abc123", "object": "file", "purpose": "batch" }]
}
```
</ResponseExample>

## Wichtige Felder

<ResponseField name="object" type="string">Objekttyp der Antwort.</ResponseField>
<ResponseField name="data" type="array">Zurückgegebene Liste oder Payload dieser Antwort.</ResponseField>
