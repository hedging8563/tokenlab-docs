---
title: "API-Key erstellen"
openapi: "/openapi/de.json POST /v1/management/api-keys"
description: "Erstellt einen neuen Benutzer-API-Key für die aktuelle Organisation."
# Synced from canonical OpenAPI x-mint.metadata.playground.
playground: simple
---

## Übersicht

Dieser Endpunkt erstellt einen neuen API-Key vom Typ `type=user` in der Organisation, die mit dem aktuellen Management-Token verknüpft ist.

## Request-Body

| Feld | Typ | Standard / Grenzen | Beschreibung |
|------|-----|--------------------|--------------|
| `name` | string | optional, Standard `Default Key`, Länge `1-50` | Anzeigename; führende und nachgestellte Leerzeichen werden serverseitig entfernt |
| `limitAmount` | number \| string \| null | `0–100000` USD | `null` bedeutet unbegrenzt; `0` verhindert Ausgaben. Dezimalstrings erlauben bis zu 6 Nachkommastellen. Bei der Erstellung bedeutet das Weglassen unbegrenzt. |
| `limitCurrency` | enum | Standardwert `USD` | Nur USD. Das Senden von `CNY` gibt `400 currency_retired` zurück. |
| `models` | string[] | Standard `[]` | Optionale Allowlist für logische Modelle |
| `deliveryPolicy` | string \| null | `auto`, `verified`, `official`, `null` | `null` übernimmt die Zustellrichtlinie des Workspace. |
| `expiresAt` | string \| null | RFC3339-Datetime | `null` bedeutet ohne Ablaufzeit |

## Hinweise

- Geldbetragsfelder unterstützen ausschließlich USD. `CNY`-Eingaben sind außer Betrieb genommen und geben `400 currency_retired` zurück.

- Der vollständige API-Key-Wert wird nur einmal in der Antwort angezeigt.
- `models: []` bedeutet, dass keine zusätzliche Modellbeschränkung über die Organisationsrichtlinie hinaus gilt.
- `limitAmount: 0` erstellt einen gültigen Key, der zunächst kein Kontingent verbrauchen kann.

## Beispiel

<RequestExample>
```bash cURL
curl -X POST "https://api.tokenlab.sh/v1/management/api-keys" \
  -H "Authorization: Bearer mt-your-management-token" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Backend Worker",
    "limitAmount": 500,
    "models": ["veo3.1", "kling-3.0-video"],
    "expiresAt": "2026-12-31T23:59:59Z"
  }'
```
</RequestExample>

## Anfrage / Antwort

Diese Seite enthält das OpenAPI-Schema und kopierbare Anfragebeispiele. Senden Sie Verwaltungsanfragen mit einem Management Token über Ihr Terminal oder Ihren Client; direktes Senden ist auf dieser Seite nicht verfügbar.

## Antwortbeispiel

<ResponseExample>
```json 201 Created
{
  "id": "key_abc123def456",
  "name": "Backend Worker",
  "key": "sk-live-redacted",
  "key_prefix": "sk-live...",
  "status": "active",
  "limit_amount": 500,
  "limit_amount_decimal": "500",
  "used_amount": 0,
  "used_amount_decimal": "0",
  "models": [
    "veo3.1",
    "kling-3.0-video"
  ],
  "expires_at": "2026-12-31T23:59:59.000Z",
  "last_used_at": null,
  "created_at": "2026-07-08T03:00:00.000Z",
  "delivery_policy": null
}
```
</ResponseExample>

## Wichtige Felder

<ResponseField name="key" type="string">Vollständiger geheimer API-Schlüsselwert. Wird nur einmal bei der Erstellung zurückgegeben.</ResponseField>
<ResponseField name="id" type="string">API-Schlüssel-ID für nachfolgende Management-API-Aufrufe.</ResponseField>
<ResponseField name="name" type="string">Anzeigename des API-Schlüssels.</ResponseField>
<ResponseField name="key_prefix" type="string">Nicht geheimer Schlüsselpräfix für Anzeige und Support.</ResponseField>
<ResponseField name="status" type="string">Einer der Werte `active`, `inactive`, `suspended` oder `revoked`.</ResponseField>
<ResponseField name="limit_amount" type="number | null">Ausgabenlimit in USD als JSON-Zahl für die Anzeige. `null` bedeutet unbegrenzt.</ResponseField>
<ResponseField name="limit_amount_decimal" type="string | null">Exaktes Ausgabenlimit in USD als Dezimalzeichenfolge. `null` bedeutet unbegrenzt.</ResponseField>
<ResponseField name="used_amount" type="number">Kumulierte Nutzungskosten in USD als JSON-Zahl für die Anzeige.</ResponseField>
<ResponseField name="used_amount_decimal" type="string">Exakte kumulierte Nutzungskosten in USD als Dezimalzeichenfolge.</ResponseField>
<ResponseField name="models" type="string[]">Liste zulässiger Modelle pro Schlüssel. Ein leeres Array bedeutet, dass keine zusätzliche Modellbeschränkung auf Schlüsselebene gilt.</ResponseField>
<ResponseField name="delivery_policy" type="string | null">`auto`, `verified`, `official`, `null`. `null` übernimmt die Zustellrichtlinie des Workspace.</ResponseField>
<ResponseField name="expires_at" type="string | null">ISO-Zeitstempel, zu dem der Schlüssel abläuft, oder `null` ohne Ablaufdatum.</ResponseField>
<ResponseField name="last_used_at" type="string | null">ISO-Zeitstempel der letzten Nutzung oder `null`, wenn noch nicht verwendet.</ResponseField>
<ResponseField name="created_at" type="string">ISO-Zeitstempel der Erstellung.</ResponseField>
