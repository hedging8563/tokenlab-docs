---
title: "Medien-Asset-Upload vorbereiten"
openapi: "/openapi/de.json POST /v1/worlds/media-assets/prepare-upload"
description: "Erstellt ein Upload-Ziel für ein World Labs Medien-Asset"
---

Erstelle einen World Labs Medien-Asset-Datensatz und erhalte eine Upload-URL für ein Bild oder Video. Lade die Binärdatei nach `upload_info.upload_url` hoch und referenziere danach `media_asset.media_asset_id` im `world_prompt` mit `source: "media_asset"`.

TokenLab speichert den Besitzer des Assets. Nur dieselbe Organisation kann dieses Medien-Asset abrufen oder für World-Generierung verwenden.

## Anfragekörper

<ParamField body="file_name" type="string" required>
  Ursprünglicher Dateiname, bis zu 64 Zeichen.
</ParamField>

<ParamField body="kind" type="string" required>
  Medientyp: `image` oder `video`.
</ParamField>

<ParamField body="extension" type="string | null">
  Optionale Erweiterung wie `png`, `jpg` oder `mp4`.
</ParamField>

<ParamField body="metadata" type="object | null">
  Optionales Metadatenobjekt für das Upstream-Medien-Asset.
</ParamField>

## Antwort

<ResponseField name="media_asset" type="object">
  Metadaten des vorbereiteten World Labs Medien-Assets.
</ResponseField>

<ResponseField name="upload_info.upload_url" type="string">
  Signierte Upstream-URL für den Binär-Upload.
</ResponseField>

<ResponseField name="upload_info.upload_method" type="string">
  HTTP-Methode für den Binär-Upload an `upload_info.upload_url`, sofern zurückgegeben.
</ResponseField>

<ResponseField name="upload_info.required_headers" type="object">
  Header für den Binär-Upload, sofern zurückgegeben.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST "https://api.tokenlab.sh/v1/worlds/media-assets/prepare-upload" \
  -H "Authorization: Bearer sk-your-api-key" \
  -H "Content-Type: application/json" \
  -d '{
    "file_name": "reference.png",
    "kind": "image",
    "extension": "png"
  }'
```

```python Python
import requests

response = requests.post(
    "https://api.tokenlab.sh/v1/worlds/media-assets/prepare-upload",
    headers={"Authorization": "Bearer sk-your-api-key"},
    json={"file_name": "reference.png", "kind": "image", "extension": "png"},
)

print(response.json()["media_asset"]["media_asset_id"])
```

```javascript JavaScript
const response = await fetch('https://api.tokenlab.sh/v1/worlds/media-assets/prepare-upload', {
  method: 'POST',
  headers: {
    Authorization: 'Bearer sk-your-api-key',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({ file_name: 'reference.png', kind: 'image', extension: 'png' })
});

const prepared = await response.json();
console.log(prepared.media_asset.media_asset_id, prepared.upload_info.upload_url);
```
</RequestExample>
