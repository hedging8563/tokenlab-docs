---
title: "Einrichtung des TokenLab-Providers"
description: "Konfigurieren Sie TokenLab als benutzerdefinierten Provider in OpenClaw unter Verwendung von OpenAI-, Responses-, Anthropic- und Gemini-kompatiblen Routen"
---

<Note>
Dieser Leitfaden richtet sich an Nutzer von **selbst gehostetem OpenClaw**, die TokenLab als ihren KI-Provider verbinden möchten.
</Note>

## Empfohlen: Plugin installieren

Installieren Sie für die reguläre OpenClaw-Agentenausführung das TokenLab-Provider-Plugin:

```bash
openclaw plugins install @tokenlabai/openclaw-provider
openclaw onboard
```

Starten Sie nach der Installation `openclaw onboard`, wählen Sie TokenLab und geben Sie den Schlüssel nur im lokalen Dialog ein. Lädt das Plugin nicht, nutzen Sie die manuelle Konfiguration unten und prüfen Sie die Versionskompatibilität.

Das Plugin lädt den aktuellen Chatmodell-Katalog von TokenLab und hält 18 Fallback-Modelle für die Offline-Erkennung bereit. Modelle verwenden das Format `tokenlab/<model-id>`.

[npm-Paket](https://www.npmjs.com/package/@tokenlabai/openclaw-provider) · [ClawHub-Eintrag](https://clawhub.ai/tokenlabai/plugins/openclaw-provider) · [Quellcode](https://github.com/hedging8563/tokenlab-openclaw-provider)

## Manuelle Provider-Konfiguration

Verwenden Sie die nachfolgenden manuellen `models.providers`-Einträge nur, wenn Sie getrennte Routen für **Responses API**, **Claude native**, **Gemini native** oder **MiniMax native** benötigen.

Wenn Sie die manuelle Konfiguration wählen, reicht es aus, nur `tokenlab` zu konfigurieren. Fügen Sie die anderen Provider nur hinzu, wenn Sie explizit **Responses API**, **Claude native**, **Gemini native** oder **MiniMax native** Verhalten benötigen.

| Anbieter | OpenClaw `api` | Am besten für | baseUrl |
|----------|----------------|---------------|---------|
| `tokenlab` | `openai-completions` | GPT, DeepSeek, Qwen und die meisten OpenAI-kompatiblen Aufrufe | `https://api.tokenlab.sh/v1` |
| `tokenlab-responses` | `openai-responses` | OpenAI Responses-Workflows, die `/v1/responses`-Semantik erwarten | `https://api.tokenlab.sh/v1` |
| `tokenlab-claude` | `anthropic-messages` | Natives Claude Messages API | `https://api.tokenlab.sh` |
| `tokenlab-gemini` | `google-generative-ai` | Native Gemini-API-Format | `https://api.tokenlab.sh` |
| `tokenlab-minimax` | `anthropic-messages` | Native MiniMax-Routing | `https://api.tokenlab.sh` |

<Warning>
Verwenden Sie den Suffix `/v1` nur für `openai-completions` und `openai-responses`.

Native Provider wie `anthropic-messages` und `google-generative-ai` sollten `https://api.tokenlab.sh` ohne `/v1` verwenden, andernfalls kann OpenClaw den falschen Upstream-Pfad konstruieren.
</Warning>

## Voraussetzungen

- Eine selbst gehostete OpenClaw-Instanz
- Einen TokenLab API-Schlüssel — [Hier erhalten](https://tokenlab.sh/dashboard/api?tab=keys)

## Konfiguration

Bearbeiten Sie Ihre OpenClaw-Konfiguration:

- **Selbst gehostet**: `~/.openclaw/openclaw.json`

Fügen Sie TokenLab-Provider unter `models.providers` hinzu:

Nutzen Sie dasselbe Profil, `OPENCLAW_STATE_DIR` und `OPENCLAW_CONFIG_PATH` wie der laufende Gateway. Sichern Sie die Konfiguration. Speichern Sie `TOKENLAB_API_KEY` in der vertrauenswürdigen globalen `.env` oder Dienstumgebung dieser Instanz. Terminal-export und Projekt-`.env` reichen für den Hintergrunddienst nicht immer. Siehe [Umgebung](https://docs.openclaw.ai/help/environment).

```json5
{
  agents: {
    defaults: {
      model: {
        primary: "tokenlab-claude/claude-sonnet-5"
      }
    }
  },
  models: {
    mode: "merge",
    providers: {
      tokenlab: {
        api: "openai-completions",
        baseUrl: "https://api.tokenlab.sh/v1",
        apiKey: "${TOKENLAB_API_KEY}",
        models: [
          { id: "gpt-5.6-terra", name: "GPT-5.6 Terra" },
          { id: "deepseek-reasoner", name: "DeepSeek Reasoner" },
          { id: "qwen3-coder-flash", name: "Qwen 3 Coder Flash" }
        ]
      },
      "tokenlab-responses": {
        api: "openai-responses",
        baseUrl: "https://api.tokenlab.sh/v1",
        apiKey: "${TOKENLAB_API_KEY}",
        models: [
          { id: "gpt-5.6-terra", name: "GPT-5.6 Terra (Responses)" },
          { id: "gpt-5.2", name: "GPT-5.2 (Responses)" }
        ]
      },
      "tokenlab-claude": {
        api: "anthropic-messages",
        baseUrl: "https://api.tokenlab.sh",
        apiKey: "${TOKENLAB_API_KEY}",
        models: [
          { id: "claude-sonnet-5", name: "Claude Sonnet 5" },
          { id: "claude-opus-5", name: "Claude Opus 5" }
        ]
      },
      "tokenlab-gemini": {
        api: "google-generative-ai",
        baseUrl: "https://api.tokenlab.sh",
        apiKey: "${TOKENLAB_API_KEY}",
        models: [
          { id: "gemini-3.5-flash", name: "Gemini 3.5 Flash" },
          { id: "gemini-2.5-pro", name: "Gemini 2.5 Pro" }
        ]
      },
      "tokenlab-minimax": {
        api: "anthropic-messages",
        baseUrl: "https://api.tokenlab.sh",
        apiKey: "${TOKENLAB_API_KEY}",
        models: [
          { id: "minimax-m3", name: "MiniMax M3" }
        ]
      }
    }
  }
}
```

<Info>
Alle 5 Provider verwenden denselben **API-Schlüssel**. Sie benötigen nur ein TokenLab-Konto.
</Info>

<Info>
Die oben gezeigten `models`-Arrays enthalten nur gängige Beispiele. Fügen Sie je nach Bedarf weitere Modell-IDs zu jedem Provider hinzu.
</Info>

## Verwendung von Modellen

OpenClaw referenziert Modelle weiterhin im Format `provider/model`:

```json5
{
  agents: {
    defaults: {
      model: {
        primary: "tokenlab-gemini/gemini-3.5-flash"
      }
    }
  }
}
```

### Modellbeispiele

| Anbieter | Modellreferenz | Beschreibung |
|----------|----------------|--------------|
| `tokenlab` | `tokenlab/gpt-5.6-terra` | OpenAI-kompatibler Pfad |
| `tokenlab-responses` | `tokenlab-responses/gpt-5.6-terra` | Responses API-Pfad |
| `tokenlab-claude` | `tokenlab-claude/claude-sonnet-5` | Native Claude Messages-Route |
| `tokenlab-gemini` | `tokenlab-gemini/gemini-3.5-flash` | Native Gemini-Route |
| `tokenlab-minimax` | `tokenlab-minimax/minimax-m3` | Native MiniMax-Route |

Alle verfügbaren Modelle finden Sie unter [tokenlab.sh/models](https://tokenlab.sh/de/models).

## Wann Sie welchen Provider verwenden sollten

- `tokenlab`: Standardwahl für die meisten allgemeingültigen Agent- und Chat-Anwendungsfälle.
- `tokenlab-responses`: Verwenden Sie diesen, wenn Ihr OpenClaw-Workflow explizit auf OpenAI Responses-Semantik angewiesen ist.
- `tokenlab-claude`: Verwenden Sie diesen, wenn Sie Claudes native Messages-Verhalten möchten.
- `tokenlab-gemini`: Verwenden Sie diesen, wenn Sie Gemini-native Anfrage-/Antwort-Formatierung oder bestehende Gemini-Integrationen benötigen.
- `tokenlab-minimax`: Verwenden Sie diesen, wenn Sie MiniMax über dessen native Route nutzen möchten.

Wenn Sie kein Gemini-native Verhalten benötigen, können Sie Gemini-Modelle weiterhin über `tokenlab/gemini-*` über die OpenAI-kompatible Route aufrufen.

## Häufige Fehler

<AccordionGroup>
  <Accordion title="Verwendet noch das alte Top-Level `providers`-Array">
    Die aktuelle OpenClaw-Dokumentation verwendet `models.providers`. Wenn Sie weiterhin das ältere Top-Level-`providers`-Array-Format beibehalten, kann OpenClaw die Konfiguration ignorieren oder die Provider-Präfixe nicht wie erwartet auflösen.
  </Accordion>
  <Accordion title="Vergessen von /v1 bei `tokenlab-responses`">
    `openai-responses` mappt auf TokenLabs `/v1/responses`-Pfad, daher muss `tokenlab-responses` `https://api.tokenlab.sh/v1` verwenden.
  </Accordion>
  <Accordion title="Hinzufügen von /v1 zu `tokenlab-claude`, `tokenlab-gemini` oder `tokenlab-minimax`">
    `anthropic-messages` und `google-generative-ai` sollten `https://api.tokenlab.sh` ohne `/v1` verwenden. Das Hinzufügen von `/v1` kann zu falschen Anforderungs-Pfaden führen.
  </Accordion>
  <Accordion title="Unterstützt OpenClaw weiterhin native Gemini?">
    Ja. Die aktuelle OpenClaw-Dokumentation enthält weiterhin den eingebauten `google`-Provider und unterstützt außerdem benutzerdefinierte Provider mit `api: "google-generative-ai"`. `tokenlab-gemini` bleibt daher eine gültige native Gemini-Route für OpenClaw-Nutzer.
  </Accordion>
</AccordionGroup>

## Einrichtung verifizieren

Starten Sie dieselbe Instanz neu und prüfen Sie Modell und Anbieter. `models status` ohne `--probe` testet keinen Modellaufruf. Live-Probes und Chats können Token verbrauchen. Vergleichen Sie Antwort und TokenLab-Anfrageprotokoll. Zum Wiederherstellen wählen Sie das alte Modell und entfernen nur hinzugefügte Einträge.

```bash
openclaw gateway restart
openclaw models status
```

## Nächste Schritte

Sobald OpenClaw verbunden ist, helfen Ihnen diese Guides, TokenLab effektiver zu nutzen:

- [API-Formate](/guides/api-formats) — Verstehen Sie die Unterschiede zwischen OpenAI-, Responses-, Anthropic- und Gemini-Routen
- [IDE-/SDK-Kompatibilität](/guides/ide-sdk-compatibility) — Prüfen Sie, wann `/v1/responses` die bessere Wahl ist
- [Fehlerbehandlung](/guides/error-handling) — Erfahren Sie gängige Ausfallursachen und Wiederherstellungsmuster
- [Modellübersicht](https://tokenlab.sh/de/models) — Durchstöbern Sie Modell-IDs, bevor Sie sie in Agenten einbinden
