---
title: "Langfuse"
description: "Rastreie requisições compatíveis com OpenAI do TokenLab com o Langfuse"
---

## Visão Geral

O Langfuse pode rastrear chamadas do TokenLab através de sua integração com a OpenAI quando o SDK da OpenAI está configurado com a URL base `/v1` do TokenLab.

<Note>
**Tipo**: Observabilidade

**Caminho Principal**: Rastreamento do SDK da OpenAI

**Nível de Suporte**: Caminho de rastreamento compatível com OpenAI suportado
</Note>

## Instalação

```bash
pip install langfuse openai
```

## Ambiente

```bash
export LANGFUSE_SECRET_KEY="sk-lf-..."
export LANGFUSE_PUBLIC_KEY="pk-lf-..."
export LANGFUSE_BASE_URL="https://cloud.langfuse.com"
export TOKENLAB_API_KEY="sk-your-tokenlab-key"
```

## Exemplo

```python
import os

from langfuse import get_client, observe
from langfuse.openai import OpenAI

client = OpenAI(
    api_key=os.environ["TOKENLAB_API_KEY"],
    base_url="https://api.tokenlab.sh/v1",
)


@observe()
def story():
    return client.chat.completions.create(
        model="claude-sonnet-5",
        messages=[{"role": "user", "content": "Write a short haiku."}],
    ).choices[0].message.content


print(story())
get_client().flush()
```

## Notas sobre os Endpoints

Esta página cobre o rastreamento do Langfuse para o caminho do SDK da OpenAI. Se sua aplicação utiliza respostas nativas do TokenLab, mensagens da Anthropic ou endpoints do Gemini, certifique-se de que sua instrumentação capture esse caminho de chamada do cliente.