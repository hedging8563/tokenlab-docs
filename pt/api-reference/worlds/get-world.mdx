---
title: "Obter World"
openapi: "/openapi/pt.json GET /v1/worlds/{id}"
description: "Obtém um World World Labs Marble concluído"
---

Obtenha um Marble World concluído criado via TokenLab. O world deve pertencer à mesma organização da chave de API.

## Parâmetros de caminho

<ParamField path="id" type="string" required>
  ID do mundo da World Labs.
</ParamField>

## Resposta

<ResponseField name="world_id" type="string">
  ID do mundo da World Labs.
</ResponseField>

<ResponseField name="world_marble_url" type="string">
  URL do mundo World Labs Marble.
</ResponseField>

<ResponseField name="assets" type="object">
  Assets World gerados quando disponíveis.
</ResponseField>

<RequestExample>
```bash cURL
curl "https://api.tokenlab.sh/v1/worlds/world_123" \
  -H "Authorization: Bearer sk-your-api-key"
```

```python Python
import requests

response = requests.get(
    "https://api.tokenlab.sh/v1/worlds/world_123",
    headers={"Authorization": "Bearer sk-your-api-key"},
)
print(response.json()["world_id"])
```

```javascript JavaScript
const response = await fetch('https://api.tokenlab.sh/v1/worlds/world_123', {
  headers: { Authorization: 'Bearer sk-your-api-key' }
});
console.log(await response.json());
```
</RequestExample>
