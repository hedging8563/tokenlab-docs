---
title: "Obter preços"
openapi: "/openapi/pt.json GET /v1/pricing"
description: "Obtém informações de preços dos modelos"
---

Este endpoint público não exige uma chave API.

## Resumo

Use este endpoint para listar a superfície pública de preços atual dos modelos ativos.

Para ver o detalhe completo de preços de um modelo, incluindo preços estruturados e procedência, prefira [Obter preço do modelo](/pt/api-reference/models/get-model).

## Parâmetros de consulta

<ParamField query="provider" type="string">
  Filtro opcional por provedor, como `openai`, `anthropic` ou `google`.
</ParamField>

<ParamField query="tag" type="string">
  Filtro opcional por tag de modelo, como `chat`, `image`, `video`, `embedding` ou `translation`.
</ParamField>

## Resposta

<ResponseField name="object" type="string">
  Sempre `pricing`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  Timestamp ISO da publicação dos preços públicos atuais.
</ResponseField>

<ResponseField name="currency" type="string">
  Sempre `USD`.
</ResponseField>

<ResponseField name="data" type="array">
  Array de entradas de preços. Cada item inclui `model`, `provider`, `is_lock_price`, `capabilities`, `aliases` e os campos públicos de resumo de preços, escalares ou estruturados, retornados pelo esquema interativo acima.
</ResponseField>

<RequestExample>
```bash cURL
curl "https://api.tokenlab.sh/v1/pricing?provider=openai&tag=chat"
```

```python Python
import requests

response = requests.get(
    "https://api.tokenlab.sh/v1/pricing",
    params={"provider": "openai", "tag": "chat"}
)

print(response.json())
```

```javascript JavaScript
const response = await fetch(
  'https://api.tokenlab.sh/v1/pricing?provider=openai&tag=chat'
);

const data = await response.json();
console.log(data);
```

```go Go
package main

import (
    "encoding/json"
    "fmt"
    "net/http"
)

func main() {
    req, _ := http.NewRequest("GET", "https://api.tokenlab.sh/v1/pricing?provider=openai&tag=chat", nil)

    client := &http.Client{}
    resp, _ := client.Do(req)
    defer resp.Body.Close()

    var result map[string]interface{}
    json.NewDecoder(resp.Body).Decode(&result)
    fmt.Println(result)
}
```

```php PHP
<?php
$ch = curl_init('https://api.tokenlab.sh/v1/pricing?provider=openai&tag=chat');

curl_setopt_array($ch, [
    CURLOPT_RETURNTRANSFER => true
]);

$response = curl_exec($ch);
curl_close($ch);

$data = json_decode($response, true);
print_r($data);
```
</RequestExample>

<ResponseExample>
```json Response
{
  "object": "pricing",
  "updated_at": "2026-04-20T00:00:00.000Z",
  "currency": "USD",
  "data": [
    {
      "model": "gpt-4o",
      "provider": "openai",
      "is_lock_price": false,
      "input_per_1m_tokens": "2.50",
      "output_per_1m_tokens": "10.00",
      "capabilities": ["chat"],
      "aliases": ["gpt-4o-latest"]
    }
  ]
}
```
</ResponseExample>

<Note>
Use o painel OpenAPI interativo acima para ver o esquema exato de resposta. As entradas públicas de preços também podem expor `lock_price_per_request`, `has_complex_pricing`, `supported_operations`, `pricing_summary` e `pricing_provenance`.
</Note>
