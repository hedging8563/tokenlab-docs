---
title: "Recuperar Lote"
openapi: "/openapi/pt.json GET /v1/batches/{batch_id}"
description: "Recupera o status atual, contagens e arquivos de resultado para um lote."
---

## Visão Geral

Este endpoint é parte da API de Lote compatível com OpenAI da TokenLab. Trabalhos em lote utilizam arquivos JSONL carregados, são executados de forma assíncrona e podem retornar arquivos de saída/erro posteriormente.

## Notas

- Os arquivos de entrada do lote devem usar `purpose=batch`.
- `completion_window` é atualmente `24h`.
- A ordenação da saída não é garantida; sempre combine pelo `custom_id`.
- Streaming não é suportado dentro dos itens do lote.

## Exemplo

<RequestExample>
```bash cURL
curl -X GET "https://api.tokenlab.sh/v1/batches/batch_abc123" \
  -H "Authorization: Bearer sk-your-api-key"
```
</RequestExample>

## Solicitação / Resposta

Use o painel interativo do OpenAPI acima para o esquema exato.

## Dicas Operacionais

- Use `custom_id` para reconciliação idempotente a montante.
- Espere `output_file_id` e `error_file_id` apenas após o trabalhador finalizar o lote.

## Exemplo de resposta

<ResponseExample>
```json 200 OK
{
  "id": "batch_abc123",
  "object": "batch",
  "status": "completed",
  "input_file_id": "file_abc123",
  "output_file_id": "file_out123",
  "error_file_id": "file_err123",
  "request_counts": { "total": 100, "completed": 98, "failed": 2 }
}
```
</ResponseExample>

## Campos importantes

<ResponseField name="request_counts" type="object">Contagens de solicitações totais, concluídas, com falha e canceladas.</ResponseField>
<ResponseField name="output_file_id" type="string|null">Identificador de arquivo usado por APIs relacionadas.</ResponseField>
<ResponseField name="error_file_id" type="string|null">Identificador de arquivo usado por APIs relacionadas.</ResponseField>
