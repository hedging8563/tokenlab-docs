---
title: "Recuperar conteúdo do arquivo"
openapi: "/openapi/pt.json GET /v1/files/{file_id}/content"
description: "Baixa o conteúdo original de um arquivo enviado."
---

## Visão geral

Este endpoint serve principalmente à recuperação de conteúdo de arquivos da API de Batch compatível com OpenAI da TokenLab.

Quando a requisição entra no modo de compatibilidade com Anthropic Files, a mesma rota também pode fazer proxy do conteúdo original armazenado do arquivo para o workspace vinculado.


Sem cabeçalhos Anthropic, esta rota baixa conteúdo de arquivos de lote ou faz proxy do conteúdo de um arquivo de imagem/referência vinculado quando o provedor permite.
## Notas

- Arquivos de entrada do lote devem usar `purpose=batch`.
- `completion_window` é atualmente `24h`.
- A ordenação da saída não é garantida; sempre combine pelo `custom_id`.
- Streaming não é suportado dentro de itens de lote.

## Exemplo

<RequestExample>
```bash cURL
curl -X GET "https://api.tokenlab.sh/v1/files/file_abc123/content" \
  -H "Authorization: Bearer sk-your-api-key" \
  --output batch-output.jsonl
```
</RequestExample>

## Solicitação / Resposta

Use o painel OpenAPI interativo acima para ver o esquema exato.

## Dicas operacionais

- Use `custom_id` para reconciliação downstream idempotente.
- Espere `output_file_id` e `error_file_id` apenas depois que o worker finalizar o lote.

## Tipos de conteúdo

O conteúdo de arquivos em lote é retornado como JSONL ou texto simples. O conteúdo de referências de imagem criadas pelo TokenLab pode ser encaminhado como conteúdo binário quando o provedor de arquivos subjacente suporta a recuperação de conteúdo.

## Exemplo de resposta

<ResponseExample>
```jsonl 200 OK
{"custom_id":"request-1","response":{"status_code":200,"body":{"id":"chatcmpl_abc123"}}}
{"custom_id":"request-2","error":{"code":"invalid_request_error","message":"Invalid model"}}
```
</ResponseExample>

## Campos importantes

<ResponseField name="body" type="binary|string">Conteúdo do arquivo. Salve ou analise conforme `Content-Type`; analise JSONL linha por linha.</ResponseField>
<ResponseField name="custom_id" type="string">ID que associa um item de entrada do lote ao seu resultado.</ResponseField>
