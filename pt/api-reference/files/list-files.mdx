---
title: "Listar arquivos"
openapi: "GET /v1/files"
description: "Lista arquivos enviados para fluxos em lote e modos de compatibilidade com arquivos suportados."
---

## Visão geral

Este endpoint serve principalmente à API de Batch compatível com OpenAI da TokenLab, incluindo entradas em lote enviadas e arquivos de saída ou erro gerados.

Quando a requisição entra no modo de compatibilidade com Anthropic Files, a mesma rota também pode listar arquivos no estilo Anthropic disponíveis para o workspace vinculado.


Sem cabeçalhos Anthropic, a lista pode incluir arquivos de lote e arquivos de imagem/referência da TokenLab. Use `purpose=batch`, `purpose=batch_output`, `purpose=user_data` ou `purpose=vision` para restringir a lista.
## Notas

- Arquivos de entrada do lote devem usar `purpose=batch`.
- Use `purpose=batch_output` para focar nos arquivos de saída gerados.
- `completion_window` é atualmente `24h`.
- A ordenação da saída não é garantida; sempre combine pelo `custom_id`.
- Streaming não é suportado dentro de itens de lote.

## Exemplo

<RequestExample>
```bash cURL
curl -X GET "https://api.tokenlab.sh/v1/files?purpose=batch&limit=20" \
  -H "Authorization: Bearer sk-your-api-key"
```
</RequestExample>

## Solicitação / Resposta

Use o painel OpenAPI interativo acima para ver o esquema exato.

## Dicas operacionais

- Use `custom_id` para reconciliação downstream idempotente.
- Espere `output_file_id` e `error_file_id` apenas depois que o worker finalizar o lote.
- A precificação do lote pode ser diferente da precificação síncrona porque as regras de desconto `isBatchRequest=true` se aplicam.

## Exemplo de resposta

<ResponseExample>
```json 200 OK
{
  "object": "list",
  "data": [{ "id": "file_abc123", "object": "file", "purpose": "batch" }]
}
```
</ResponseExample>

## Campos importantes

<ResponseField name="object" type="string">Tipo de objeto da resposta.</ResponseField>
<ResponseField name="data" type="array">Lista ou payload retornado por esta resposta.</ResponseField>
