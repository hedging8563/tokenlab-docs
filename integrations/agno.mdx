---
title: "Agno"
description: "Use TokenLab models in Agno agents"
---

## Overview

Agno agents can call TokenLab through its OpenAI-compatible model client.


## Environment

```bash
export TOKENLAB_API_KEY="sk-your-tokenlab-key"
```

## TokenLab Model Wrapper

If your Agno version includes the TokenLab model wrapper:

```python
from agno.agent import Agent
from agno.models.tokenlab import TokenLab

agent = Agent(model=TokenLab(id="gpt-5.6-luna"))
agent.print_response("Give me a concise project risk checklist.")
```

Some Agno builds also support model strings:

```python
agent = Agent(model="tokenlab:gpt-5.4-mini")
```

## OpenAI-Compatible Setup

If your version does not include TokenLab yet, use Agno's OpenAI-compatible model path with:

- Base URL: `https://api.tokenlab.sh/v1`
- API key: `TOKENLAB_API_KEY`
- Model: `gpt-5.4-mini`

Model IDs change over time. Choose a current model on the [Models page](https://tokenlab.sh/en/models), then compare quality, speed, and cost on your own agent tasks.
