---
title: "Seedance 2.0 视频模型"
description: "选择 Seedance 2.0 模型以及正确的 TokenLab 接入路径。"
---

如果你要在应用中接入 Seedance 2.0，请从这里开始。本页只负责模型和工作流选择；火山迁移、可复用素材和真人验证分别放在独立指南中。

## 选择接入路径

| 目标 | 使用方式 | 下一步 |
| --- | --- | --- |
| 新建 TokenLab 视频接入 | 统一 `POST /v1/videos/generations` API | [视频生成指南](/zh/guides/video-generation) |
| 迁移现有火山 Seedance 客户端 | 火山兼容 REST 或 Action 接口 | [从火山迁移 Seedance](/zh/guides/seedance-volcengine-migration) |
| 复用虚拟人、商品、风格或真人引用 | TokenLab 素材库 | [Seedance 素材与真人验证](/zh/guides/seedance-materials) |

三条路径都使用 `Authorization: Bearer <TOKENLAB_API_KEY>`。火山兼容路径保留火山请求形状，但不接受 AK/SK 签名。

## 模型选择

| 模型 | 适用场景 | 支持的分辨率 |
|-------|----------|-----------------------|
| `seedance-2.0` | 最高质量和 4K 输出 | `480p`, `720p`, `1080p`, `4k` |
| `seedance-2.0-fast` | 更快、成本更低的输出 | `480p`, `720p` |
| `seedance-2.0-mini` | 最低成本输出 | `480p`, `720p` |

`seedance-2.0-fast` 和 `seedance-2.0-mini` 不支持 `1080p` 和 `4k` 请求。音频仅可在支持的参考工作流中使用；不支持纯音频或仅文本加音频的 Seedance 请求。

## 支持的操作

| 操作 | 典型输入 |
|-----------|----------------|
| `text-to-video` | `prompt` |
| `image-to-video` | `prompt`, `image_url` 或兼容的图像输入 |
| `start-end-to-video` | `prompt`, `start_image`, `end_image` |
| `reference-to-video` | `prompt`, `reference_images`, 可选 `video_urls`, 可选 `audio_urls` |
| `video-to-video` | `prompt`, `video_url` 或兼容的视频输入 |
| `video-extension` | `task_id` 或模型特定的续写输入 |

## 定价与异步结果

Seedance 2.0 的定价取决于输出分辨率、输入类型和模型。在硬编码产品 UI 之前，请使用 [Pricing API](/zh/api-reference/pricing/get-pricing) 或 [Models API](/zh/api-reference/models/list-models)。

视频生成是异步的。请遵循创建请求返回的 `poll_url`，或使用返回的任务 ID 调用 [获取任务状态](/zh/api-reference/tasks/get-task-status)。

## 相关接口

| API | 下一步 |
|-----|---------------|
| `POST /v1/videos/generations` | [创建视频](/zh/api-reference/video/create-video) |
| `GET /v1/tasks/{id}` | [获取任务状态](/zh/api-reference/tasks/get-task-status) |
| Seedance 任务兼容 | [创建任务（火山兼容）](/zh/api-reference/video/create-volc-compatible-seedance-task) |
| 素材 Action 兼容 | [素材 Action（火山兼容）](/zh/api-reference/video/volc-compatible-material-actions) |
