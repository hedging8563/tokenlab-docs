---
title: "列出 API Keys"
openapi: "GET /v1/management/api-keys"
description: "列出当前管理令牌所属组织下的用户 API Key。"
---

## 概览

返回当前管理令牌所属组织下的用户 API Key 列表。

## 注意事项

- 金额字段仅支持 USD。`CNY` 输入已停用，并会返回 `400 currency_retired`。

- 系统托管的 Key 不会出现在结果中。

## 示例

<RequestExample>
```bash cURL
curl -X GET "https://api.tokenlab.sh/v1/management/api-keys" \
  -H "Authorization: Bearer mt-your-management-token"
```
</RequestExample>

## 请求 / 响应

使用上面的交互式 OpenAPI 面板获取确切的模式。

## 响应示例

<ResponseExample>
```json 200 OK
{
  "object": "list",
  "data": [
    {
      "id": "key_abc123def456",
      "name": "Backend Worker",
      "key_prefix": "sk-live...",
      "status": "active",
      "limit_amount": 500,
      "limit_amount_decimal": "500",
      "used_amount": 148.25,
      "used_amount_decimal": "148.25",
      "models": [
        "gpt-5-mini",
        "claude-3-7-sonnet"
      ],
      "expires_at": "2026-12-31T23:59:59.000Z",
      "last_used_at": "2026-07-08T03:12:45.000Z",
      "created_at": "2026-07-01T10:00:00.000Z"
    }
  ],
  "pagination": {
    "page": 1,
    "limit": 50,
    "total": 1,
    "totalPages": 1
  }
}
```
</ResponseExample>

## 重要字段

<ResponseField name="object" type="string">固定为 `list`。</ResponseField>
<ResponseField name="data" type="array">API Key 对象数组。</ResponseField>
<ResponseField name="pagination" type="object">分页元数据，包含 `page`、`limit`、`total` 和 `totalPages`。</ResponseField>
<ResponseField name="data[].id" type="string">用于后续管理 API 调用的 API Key 标识符。</ResponseField>
<ResponseField name="data[].name" type="string">API Key 的显示名称。</ResponseField>
<ResponseField name="data[].key_prefix" type="string">用于展示和支持排查的非敏感 Key 前缀。</ResponseField>
<ResponseField name="data[].status" type="string">取值为 `active`、`inactive` 或 `revoked`。</ResponseField>
<ResponseField name="data[].limit_amount" type="number | null">以 JSON 数字返回的 USD 消费上限，用于展示。`null` 表示不限额。</ResponseField>
<ResponseField name="data[].limit_amount_decimal" type="string | null">以十进制字符串返回的 USD 精确消费上限。`null` 表示不限额。</ResponseField>
<ResponseField name="data[].used_amount" type="number">以 JSON 数字返回的 USD 累计用量消费，用于展示。</ResponseField>
<ResponseField name="data[].used_amount_decimal" type="string">以十进制字符串返回的 USD 精确累计用量消费。</ResponseField>
<ResponseField name="data[].models" type="string[]">单个 Key 的模型白名单。空数组表示没有额外的 Key 级模型限制。</ResponseField>
<ResponseField name="data[].expires_at" type="string | null">Key 到期时间的 ISO 时间戳；`null` 表示永不过期。</ResponseField>
<ResponseField name="data[].last_used_at" type="string | null">最近一次使用的 ISO 时间戳；从未使用时为 `null`。</ResponseField>
<ResponseField name="data[].created_at" type="string">创建时间的 ISO 时间戳。</ResponseField>
