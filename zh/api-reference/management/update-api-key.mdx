---
title: "更新 API 密钥"
openapi: "PATCH /v1/management/api-keys/{keyId}"
description: "更新当前工作区的 API 密钥。"
# Synced from canonical OpenAPI x-mint.metadata.playground.
playground: simple
---

修改 API 密钥的名称、额度、模型白名单、过期时间或状态。

本页提供 OpenAPI 结构说明和可复制的请求示例。请在终端或客户端使用 Management Token 发送管理请求；本页不提供直接发送功能。

## 请求体

PATCH 请求至少需要提供一个字段。

| 字段 | 类型 | 默认值 / 限制 | 说明 |
|------|------|---------------|------|
| `status` | enum | - | `active`, `inactive`, `revoked` |
| `name` | string | 长度 `1-50` | 更新后的展示名称 |
| `limitAmount` | number \| null | 最小 `0`，最高 `100000` | `null` 表示不限额，`0` 表示不能产生费用 |
| `limitCurrency` | enum | 默认 `USD` | 仅支持 USD。发送 `CNY` 会返回 `400 currency_retired`。 |
| `models` | string[] | - | 更新后的模型白名单 |
| `expiresAt` | string \| null | RFC3339 datetime | `null` 表示清除过期时间 |

## 说明

- 金额字段仅支持 USD。`CNY` 输入已停用，并会返回 `400 currency_retired`。

- 管理 API 不提供删除接口。临时停用使用 `inactive`，永久停用使用 `revoked`。
- 已撤销的密钥需要由新密钥替代。

## 示例

<RequestExample>
```bash cURL
curl -X PATCH "https://api.tokenlab.sh/v1/management/api-keys/key_abc123def456" \
  -H "Authorization: Bearer mt-your-management-token" \
  -H "Content-Type: application/json" \
  -d '{
    "status": "inactive",
    "limitAmount": 0
  }'
```
</RequestExample>

## 响应示例

<ResponseExample>
```json 200 OK
{
  "id": "key_abc123def456",
  "name": "Backend Worker",
  "key_prefix": "sk-live...",
  "status": "inactive",
  "limit_amount": 0,
  "limit_amount_decimal": "0",
  "used_amount": 148.25,
  "used_amount_decimal": "148.25",
  "models": [
    "gpt-5.6-luna",
    "claude-3-7-sonnet"
  ],
  "expires_at": "2026-12-31T23:59:59.000Z",
  "last_used_at": "2026-07-08T03:12:45.000Z",
  "created_at": "2026-07-01T10:00:00.000Z"
}
```
</ResponseExample>

## 重要字段

<ResponseField name="id" type="string">密钥 ID。</ResponseField>
<ResponseField name="name" type="string">API 密钥的显示名称。</ResponseField>
<ResponseField name="key_prefix" type="string">可以安全显示的密钥前缀。</ResponseField>
<ResponseField name="status" type="string">取值为 `active`、`inactive` 或 `revoked`。</ResponseField>
<ResponseField name="limit_amount" type="number | null">以 JSON 数字返回的 USD 消费上限，用于展示。`null` 表示不限额。</ResponseField>
<ResponseField name="limit_amount_decimal" type="string | null">以十进制字符串返回的 USD 精确消费上限。`null` 表示不限额。</ResponseField>
<ResponseField name="used_amount" type="number">以 JSON 数字返回的 USD 累计用量消费，用于展示。</ResponseField>
<ResponseField name="used_amount_decimal" type="string">以十进制字符串返回的 USD 精确累计用量消费。</ResponseField>
<ResponseField name="models" type="string[]">模型白名单。空数组表示没有额外的密钥级模型限制。</ResponseField>
<ResponseField name="expires_at" type="string | null">密钥到期时间；`null` 表示永不过期。</ResponseField>
<ResponseField name="last_used_at" type="string | null">最近一次使用的 ISO 时间戳；从未使用时为 `null`。</ResponseField>
<ResponseField name="created_at" type="string">创建时间的 ISO 时间戳。</ResponseField>
