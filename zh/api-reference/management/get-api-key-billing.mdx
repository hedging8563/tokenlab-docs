---
title: "获取 API 密钥账单"
openapi: "GET /v1/management/api-keys/{keyId}/billing"
description: "获取一枚 API 密钥的汇总账单。"
# Synced from canonical OpenAPI x-mint.metadata.playground.
playground: simple
---

返回所选 API 密钥的总费用、请求数、计费用量，以及按模型、模型厂商、场景和日期汇总的费用。

本页提供 OpenAPI 结构说明和可复制的请求示例。请在终端或客户端使用 Management Token 发送管理请求；本页不提供直接发送功能。

## 注意事项

- 金额字段仅支持 USD。

- 不包含工作区充值和发票记录。

## 示例

支持与[密钥用量](/zh/api-reference/management/get-api-key-usage)相同的模型、模型厂商、场景和日期筛选；返回汇总，不分页。

<RequestExample>
```bash cURL
curl -X GET "https://api.tokenlab.sh/v1/management/api-keys/key_abc123def456/billing?startDate=2026-07-01&endDate=2026-07-31" \
  -H "Authorization: Bearer mt-your-management-token"
```
</RequestExample>

## 响应示例

<ResponseExample>
```json 200 OK
{
  "api_key": {
    "id": "key_abc123def456",
    "name": "Backend Worker",
    "key_prefix": "sk-live...",
    "status": "active",
    "limit_amount": 500,
    "limit_amount_decimal": "500",
    "used_amount": 148.25,
    "used_amount_decimal": "148.25",
    "models": [
      "gpt-5.6-luna",
      "claude-3-7-sonnet"
    ],
    "expires_at": "2026-12-31T23:59:59.000Z",
    "last_used_at": "2026-07-08T03:12:45.000Z",
    "created_at": "2026-07-01T10:00:00.000Z",
    "delivery_policy": null
  },
  "summary": {
    "total_requests": 1,
    "total_cost": 0.0012,
    "total_cost_decimal": "0.0012",
    "total_prompt_tokens": 1000,
    "total_completion_tokens": 500,
    "total_tokens": 1500,
    "usage_unit_breakdown": [
      {
        "usage_unit": "per_token",
        "usage_quantity": 1500
      }
    ]
  },
  "breakdowns": {
    "by_model": [
      {
        "model": "gpt-5.6-luna",
        "logical_model": "gpt-5.6-luna",
        "model_vendor": "OpenAI",
        "requests": 1,
        "cost": 0.0012,
        "cost_decimal": "0.0012",
        "usage_quantity": 1500,
        "usage_quantity_decimal": "1500"
      }
    ],
    "by_logical_model": [
      {
        "model": "gpt-5.6-luna",
        "logical_model": "gpt-5.6-luna",
        "model_vendor": "OpenAI",
        "requests": 1,
        "cost": 0.0012,
        "cost_decimal": "0.0012",
        "usage_quantity": 1500,
        "usage_quantity_decimal": "1500"
      }
    ],
    "by_model_vendor": [
      {
        "model_vendor": "OpenAI",
        "requests": 1,
        "cost": 0.0012
      }
    ],
    "by_scene": [
      {
        "scene": "chat",
        "requests": 1,
        "cost": 0.0012,
        "cost_decimal": "0.0012"
      }
    ],
    "daily_cost": [
      {
        "date": "2026-07-08T00:00:00.000Z",
        "cost": 0.0012,
        "cost_decimal": "0.0012"
      }
    ]
  }
}
```
</ResponseExample>

## 重要字段

<ResponseField name="api_key" type="object">API 密钥摘要。</ResponseField>
<ResponseField name="summary" type="object">按所选筛选条件汇总的总计。</ResponseField>
<ResponseField name="breakdowns" type="object">按不同维度汇总的费用和用量。</ResponseField>
<ResponseField name="summary.total_requests" type="number">请求总数。</ResponseField>
<ResponseField name="summary.total_cost" type="number">以 JSON 数字返回的 USD 总费用，用于展示。</ResponseField>
<ResponseField name="summary.total_cost_decimal" type="string">以十进制字符串返回的 USD 精确总费用。</ResponseField>
<ResponseField name="summary.total_prompt_tokens" type="number">提示 token 总数。</ResponseField>
<ResponseField name="summary.total_completion_tokens" type="number">补全 token 总数。</ResponseField>
<ResponseField name="summary.total_tokens" type="number">token 总数。</ResponseField>
<ResponseField name="summary.usage_unit_breakdown" type="array">按计量单位分组的用量。</ResponseField>
<ResponseField name="breakdowns.by_model" type="array">按请求时使用的公开模型拆分。</ResponseField>
<ResponseField name="breakdowns.by_logical_model" type="array">按请求使用的公开模型汇总。</ResponseField>
<ResponseField name="breakdowns.by_model_vendor" type="array">按公开模型厂商拆分。</ResponseField>
<ResponseField name="breakdowns.by_scene" type="array">按公开请求场景拆分。</ResponseField>
<ResponseField name="breakdowns.daily_cost" type="array">每日费用趋势。</ResponseField>
