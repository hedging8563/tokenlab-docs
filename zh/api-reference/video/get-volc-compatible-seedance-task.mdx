---
title: "查询任务（火山兼容）"
openapi: "GET /api/v3/contents/generations/tasks/{id}"
description: "按 cgt 任务 ID 查询 Seedance 兼容任务。"
---

## 概览

使用创建响应返回的 `cgt-...` ID 查询任务。任务为 `queued` 或 `running` 时，每隔几秒轮询一次；任务变为 `succeeded`、`failed`、`cancelled` 或 `expired` 后停止轮询。

<Warning>
不要复用统一 v1 的任务枚举或响应 struct。`/v1/tasks/{id}` 使用 `pending`、`processing`、`completed`、`failed`；本 v3 接口使用上面的官方枚举。v3 JSON 中的 `duration` 在出现时是字符串，`error` 只在失败时出现。严格解码的 Go 客户端应把 `Duration` 声明为 `string`，JSON tag 使用 `duration,omitempty`，并把四个 v3 终态都作为停止条件。
</Warning>

另见 [Seedance 2.0 视频模型](/zh/guides/seedance-2-video) 和 [视频生成](/zh/guides/video-generation)。

## 状态值

- `queued`：已接受，等待执行。
- `running`：正在生成。
- `succeeded`：结果已就绪，`content.video_url` 可用。
- `failed`：生成失败，请查看 `error.code` 和 `error.message`。
- `cancelled`：任务在完成前被取消。
- `expired`：任务在完成前过期。

## 响应字段

- `id`：兼容任务 ID，以 `cgt-` 开头。
- `model`：兼容返回的模型标识。
- `status`：上述状态之一。
- `content.video_url`：成功后的最终视频 URL。
- `content.last_frame_url`：仅在请求且可用时返回。
- `usage`：可用时返回 token 用量。
- `created_at` 和 `updated_at`：任务时间戳。
- `error`：仅任务失败时返回；非失败任务不返回该字段。

响应不会添加 `progress`、`return_last_frame`、`watermark`、`priority` 或 `camera_fixed` 等 TokenLab 扩展字段。

## 示例

```bash
curl https://api.tokenlab.sh/api/v3/contents/generations/tasks/cgt-20260102030405-a1b2c \
  -H "Authorization: Bearer $TOKENLAB_API_KEY"
```

```json
{
  "id": "cgt-20260102030405-a1b2c",
  "model": "doubao-seedance-2-0-260128",
  "status": "succeeded",
  "content": {
    "video_url": "https://example.com/result.mp4"
  },
  "usage": {
    "completion_tokens": 123456
  }
}
```

```go Go
type SeedanceTask struct {
    ID       string `json:"id"`
    Status   string `json:"status"`
    Duration string `json:"duration,omitempty"`
}

func isTerminal(status string) bool {
    switch status {
    case "succeeded", "failed", "cancelled", "expired":
        return true
    default:
        return false
    }
}
```
