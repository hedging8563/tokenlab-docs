---
title: "Phoenix"
description: "使用 Arize Phoenix 追踪 TokenLab 兼容 OpenAI 的请求"
---

## 概述

Phoenix 可以通过自动检测配置了 TokenLab OpenAI 兼容 base URL 的 OpenAI Python 客户端，从而追踪 TokenLab 请求。

<Note>
**类型**: 可观测性 (Observability)

**主要路径**: OpenAI SDK 检测

**支持置信度**: 支持的 OpenAI 兼容追踪路径
</Note>

## 安装

```bash
pip install arize-phoenix openinference-instrumentation-openai openai
```

## 环境

```bash
export TOKENLAB_API_KEY="sk-your-tokenlab-key"
```

## 注册 Phoenix

```python
from phoenix.otel import register

tracer_provider = register(
    project_name="tokenlab-app",
    auto_instrument=True,
)
```

## 调用 TokenLab

```python
import os

from openai import OpenAI

client = OpenAI(
    api_key=os.environ["TOKENLAB_API_KEY"],
    base_url="https://api.tokenlab.sh/v1",
)

response = client.chat.completions.create(
    model="claude-sonnet-5",
    messages=[{"role": "user", "content": "Write a short haiku."}],
)

print(response.choices[0].message.content)
```

## 端点说明

Phoenix 会追踪 OpenAI SDK 的调用路径。请使用此页面进行 Chat Completions 风格的 TokenLab 调用。如果您的应用程序检测捕获了您用于这些请求的客户端，其他 TokenLab 原生端点仍然可以被追踪。