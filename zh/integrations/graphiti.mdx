---
title: "Graphiti"
description: "将 TokenLab 用作兼容 OpenAI 的 Graphiti LLM 提供商"
---

## 概述

在 Graphiti 的 `OpenAIGenericClient` 中填写 TokenLab `/v1` 地址、API 密钥和模型 ID 即可。


## 环境

```bash
export TOKENLAB_API_KEY="sk-your-tokenlab-key"
```

## LLM 客户端

```python
import os

from graphiti_core.llm_client.config import LLMConfig
from graphiti_core.llm_client.openai_generic_client import OpenAIGenericClient

llm_config = LLMConfig(
    api_key=os.environ["TOKENLAB_API_KEY"],
    model="claude-sonnet-5",
    small_model="gpt-5.6-luna",
    base_url="https://api.tokenlab.sh/v1",
)

llm_client = OpenAIGenericClient(config=llm_config)
```

将 `llm_client` 与你的图数据库和嵌入器配置一起传入 `Graphiti(...)` 实例中。

## 端点说明

Graphiti 的兼容 OpenAI 路径是面向 chat-completions 的。请使用能够可靠返回结构化 JSON 以进行提取和去重任务的模型。
