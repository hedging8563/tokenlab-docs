---
title: "LibreChat"
description: "配置 LibreChat 以使用 TokenLab 作为自定义 OpenAI 兼容端点"
---

## 概述

在 LibreChat 中把 TokenLab 添加为自定义 OpenAI 兼容服务即可。


## 环境

```bash
TOKENLAB_API_KEY=sk-your-tokenlab-key
```

## 配置示例

将 TokenLab 添加到你的 LibreChat 端点配置中：

```yaml
customEndpoints:
  - name: 'TokenLab'
    apiKey: '${TOKENLAB_API_KEY}'
    baseURL: 'https://api.tokenlab.sh/v1'
    models:
      default:
        - 'gpt-5.5'
        - 'gpt-5.6-terra'
        - 'gpt-5.6-luna'
        - 'claude-opus-4-8'
        - 'claude-sonnet-5'
        - 'gemini-3.5-flash'
        - 'deepseek-v4-pro'
        - 'deepseek-v4-flash'
      fetch: true
    titleConvo: true
    titleModel: 'gpt-5.6-luna'
    modelDisplayLabel: 'TokenLab'
```

## 注意事项

LibreChat 在此设置中使用 OpenAI 兼容的聊天路径。对于支持相应请求格式的客户端，TokenLab 还提供了原生的 Responses、Anthropic Messages 以及 Gemini 兼容端点。
