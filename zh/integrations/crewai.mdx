---
title: "CrewAI"
description: "通过 OpenAI 兼容的 LLM 设置在 CrewAI 智能体中使用 TokenLab 模型"
---

## 概述

在 CrewAI 的环境变量或 `LLM` 类中填写模型、API 密钥和 Base URL，即可调用 TokenLab 的 OpenAI 兼容 API。


## 环境变量

```python
import os

os.environ["OPENAI_API_KEY"] = "sk-your-tokenlab-key"
os.environ["OPENAI_API_BASE"] = "https://api.tokenlab.sh/v1"
os.environ["OPENAI_MODEL_NAME"] = "openai/claude-sonnet-5"
```

## LLM 类

```python
from crewai import Agent, LLM

llm = LLM(
    model="claude-sonnet-5",
    custom_openai=True,
    base_url="https://api.tokenlab.sh/v1",
    api_key="sk-your-tokenlab-key",
)

agent = Agent(
    role="Research analyst",
    goal="Summarize technical tradeoffs clearly",
    backstory="You produce concise implementation notes.",
    llm=llm,
)
```

模型 ID 会更新。请从[模型页](https://tokenlab.sh/zh/models)选择当前模型，并用自己的 CrewAI 任务比较效果、速度和费用。

## 端点说明

这套配置使用 Chat Completions。需要 Responses、Anthropic Messages 或 Gemini 格式时，请确认 CrewAI 组件支持对应 API。
