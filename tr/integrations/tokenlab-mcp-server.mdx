---
title: "TokenLab MCP Sunucusu"
description: "Claude Code, Cursor, VS Code, Codex ve diğer MCP istemcilerini TokenLab'in model, fiyatlandırma, çıkarım, medya, dosya ve görev araçlarına bağlayın."
---

## Gereken uzantıyı seçin

- MCP uyumlu istemcilere TokenLab API araçları ekler. Aşağıdaki anahtarsız `catalog` ayarıyla başlayın.
- [TokenLab Skill](/tr/integrations/coding-agent-skill), `npx skills add` ile entegrasyon yönergeleri kurar; MCP sunucusu başlatmaz.

İkisi de mevcut ajanı genişletir. Ana model sağlayıcısını değiştirmek için istemcinin kendi kılavuzunu kullanın.

## Kurulumu ajanıma bırak

Bu görevi bilgisayarınızda zaten çalışan bir ajana kopyalayın:

```text
Bu kılavuzu oku ve görevime uygun MCP katalog araçlarını veya TokenLab Skill’i seç:
https://docs.tokenlab.sh/tr/integrations/tokenlab-mcp-server
Önce kurulu sürümümü ve etkin yapılandırmayı kontrol et.
Mevcut hesapları, sağlayıcıları, izinleri ve diğer ayarları koru.
Yerel dosyaları yedekle ve önerilen değişiklikleri göster.
Gereken API anahtarlarını yerel olarak benim girmemi sağla; bunları sohbette isteme, görüntüleme veya yapıştırma.
Önce yapılandırmanın yüklenebildiğini doğrula.
Ücretli istek içeren her testin maliyetini çalıştırmadan önce ayrıca açıkla.
```

[TokenLab MCP sunucusu](https://github.com/hedging8563/tokenlab-mcp-server), TokenLab'in herkese açık OpenAPI sözleşmesinden MCP araçları oluşturur. MCP uyumlu ajanlar; modelleri ve fiyatlandırmayı keşfedebilir, OpenAI uyumlu ve yerel LLM uç noktalarını çağırabilir, çok modlu medya oluşturabilir, dosya yükleyebilir ve asenkron görevleri yönetebilir.

Bir ajanın manuel olarak tutulan bir araç listesi yerine güncel TokenLab API sözleşmesine ihtiyaç duyduğu durumlarda kullanın. Varsayılan `core` profili, 29 adet oluşturulmuş uç nokta aracını ve iki adet bileşik keşif aracını kullanıma sunar. `full` profili ise 78 adet oluşturulmuş uç nokta aracını ve aynı iki bileşik aracı kullanıma sunar.

Bu sayfa TokenLab'i Claude Code, Cursor, VS Code, Codex, Claude Desktop, Cline, Windsurf veya başka bir MCP istemcisine araç sunucusu olarak eklemeyi açıklar. TokenLab'i uygulama kodundan doğrudan çağırmak için [API hızlı başlangıç](/tr/quickstart) sayfasıyla başlayın.

<Note>
Genel katalog ve fiyatlandırma araçları TokenLab API anahtarı gerektirmez. Kimlik doğrulamalı çıkarım (inference), medya, ses, dosya, görev, yerleştirme (embedding), yeniden sıralama (rerank) ve çeviri işlemleri için `TOKENLAB_API_KEY` değerini ayarlayın. API anahtarını asla araç argümanlarına yerleştirmeyin.
</Note>

## Sağladığı Özellikler

- `https://api.tokenlab.sh/v1/models` üzerinden canlı model keşfi.
- `/v1/models/{model}` üzerinden model detay sorgulama.
- `/v1/models/{model}/pricing` üzerinden fiyatlandırma sorgulama.
- `https://api.tokenlab.sh/llms.txt` üzerinden ajan tarafından okunabilir API özeti.
- OpenAI uyumlu Chat Completions ve Responses, Anthropic Messages ve Gemini generateContent; yerel istek yapılarını düzleştirmeden (flattening) destekler.
- JSON URL'leri, TokenLab dosya kimlikleri veya yerel multipart dosyaları aracılığıyla görsel oluşturma ve düzenleme.
- Video, müzik, 3D, konuşma, transkripsiyon, ses çevirisi, yerleştirmeler (embeddings), çok modlu yerleştirmeler, yeniden sıralama ve metin çevirisi.
- Görsel, video, müzik, 3D ve dünya oluşturma işlemleri için normalleştirilmiş asenkron görev özetleri.
- Her gün çalışan ve herkese açık OpenAPI belgesini getiren, araç şemalarını yeniden oluşturan, uç nokta ve dosya testlerini çalıştıran ve yalnızca tüm kontroller başarılı olduğunda `main` dalını güncelleyen bir sözleşme senkronizasyonu.

## Hızlı başlangıç

En hızlı ve güvenli başlangıç, yalnızca altı genel katalog aracını etkinleştirmektir. TokenLab API anahtarı gerekmez; kimlik doğrulamalı araçları daha sonra açabilirsiniz.

<CardGroup cols={2}>
  <Card title="Cursor'a yükle" icon="arrow-pointer" href="https://cursor.com/en/install-mcp?name=tokenlab&config=eyJ0eXBlIjoic3RkaW8iLCJjb21tYW5kIjoibnB4IiwiYXJncyI6WyIteSIsIkB0b2tlbmxhYmFpL21jcC1zZXJ2ZXJAMC42LjIxIl0sImVudiI6eyJUT0tFTkxBQl9NQ1BfVE9PTF9QUk9GSUxFIjoiY2F0YWxvZyJ9fQ%3D%3D">
    Genel `catalog` profilini tek tıkla Cursor'a ekleyin
  </Card>
  <Card title="VS Code'a yükle" icon="code" href="https://vscode.dev/redirect/mcp/install?name=tokenlab&config=%7B%22type%22%3A%22stdio%22%2C%22command%22%3A%22npx%22%2C%22args%22%3A%5B%22-y%22%2C%22%40tokenlabai%2Fmcp-server%400.6.21%22%5D%2C%22env%22%3A%7B%22TOKENLAB_MCP_TOOL_PROFILE%22%3A%22catalog%22%7D%7D">
    Genel `catalog` profilini VS Code kullanıcı yapılandırmasına ekleyin
  </Card>
</CardGroup>

<Steps>
  <Step title="Gereksinimleri kontrol edin">
    Node.js 18.17 veya daha yeni bir sürüm yükleyin ve `npx` komutunun kullanılabildiğini doğrulayın.

    ```bash
    node --version
    npx --version
    ```

    MCP paketini genel olarak yüklemeniz veya depoyu klonlamanız gerekmez.
  </Step>

  <Step title="Erişim düzeyi seçin">
    | Amaç | API anahtarı | Profil |
    |---|---|---|
    | Modelleri, ayrıntıları ve fiyatları incelemek | Gerekmez | `catalog` (6 araç) |
    | Yaygın çıkarım, medya, dosya ve görev işlemlerini kullanmak | Ücretli çağrılar için gerekir | `core` (31 araç, varsayılan) |
    | İzin verilen tüm geliştirici işlemlerini sunmak | Ücretli çağrılar için gerekir | `full` (80 araç) |

    Yalnızca kimlik doğrulamalı API çağrıları için [Dashboard → API Keys](https://tokenlab.sh/dashboard/api) sayfasında bir anahtar oluşturun. Anahtarı istemcinin gizli ortamında saklayın; isteme veya MCP araç argümanına yapıştırmayın.
  </Step>

  <Step title="İstemciyi yapılandırın">
    Genel örnekler `catalog` profilini açıkça seçer; böylece sunulan her araç anahtarsız çalışır.

    Etkin yapılandırmayı yedekleyin. Yalnızca TokenLab girdisini ekleyip mevcut sağlayıcıları, hesapları, varsayılan modeli ve izinleri koruyun. Ad kullanımdaysa başka bir ad seçip komutları güncelleyin. Geri almak için yalnızca eklediğiniz girdiyi kaldırın veya önceki yedeğini yükleyin.

<Tabs>
      <Tab title="Claude Code">
        TokenLab'i tüm projelerde kullanmak için kullanıcı kapsamına ekleyin.

        ```bash
        claude mcp add --env TOKENLAB_MCP_TOOL_PROFILE=catalog --scope user tokenlab -- npx -y @tokenlabai/mcp-server@0.6.21
        ```

        Varsayılan `core` profilini etkinleştirmek için API anahtarınızı ekleyin.



        Yalnızca geçerli proje için `--scope local`, ortak `.mcp.json` oluşturmak için `--scope project` kullanın. Gerçek anahtarı proje yapılandırmasına commit etmeyin.
      </Tab>

      <Tab title="Cursor">
        Yukarıdaki tek tıklamalı kurulumu kullanın veya yapılandırmayı tüm projeler için `~/.cursor/mcp.json`, tek proje için `.cursor/mcp.json` dosyasına kaydedin.

        ```json
        {
          "mcpServers": {
            "tokenlab": {
              "command": "npx",
              "args": ["-y", "@tokenlabai/mcp-server@0.6.21"],
              "env": { "TOKENLAB_MCP_TOOL_PROFILE": "catalog" }
            }
          }
        }
        ```
      </Tab>

      <Tab title="VS Code">
        Tek tıklamalı kurulumu kullanın, **MCP: Add Server** komutunu çalıştırın veya dosyayı `.vscode/mcp.json` olarak kaydedin.

        ```json
        {
          "servers": {
            "tokenlab": {
              "type": "stdio",
              "command": "npx",
              "args": ["-y", "@tokenlabai/mcp-server@0.6.21"],
              "env": { "TOKENLAB_MCP_TOOL_PROFILE": "catalog" }
            }
          }
        }
        ```

        Tüm projelerde kullanılacak bir ayar için **MCP: Open User Configuration** komutunu çalıştırın ve aynı sunucu nesnesini ekleyin.
      </Tab>

      <Tab title="Codex">
        ```bash
        # Genel katalog
                codex mcp add --env TOKENLAB_MCP_TOOL_PROFILE=catalog tokenlab -- npx -y @tokenlabai/mcp-server@0.6.21
        ```
      </Tab>

      <Tab title="Claude Desktop">
        `claude_desktop_config.json` içindeki standart `mcpServers` nesnesine `npx` komutunu ve `-y`, `@tokenlabai/mcp-server@0.6.21` argümanlarını ekleyin. Yalnızca genel araçlar için `TOKENLAB_MCP_TOOL_PROFILE=catalog` ayarlayın.
      </Tab>

      <Tab title="Diğer istemciler">



        Windsurf için standart `mcpServers` yapılandırmasını `~/.codeium/windsurf/mcp_config.json` konumuna kaydedin. Diğer stdio istemcileri aynı komut ve argümanları kullanır; çalışma dizini boş bırakılır.
      </Tab>
    </Tabs>
  </Step>

  <Step title="Yeniden başlatın ve doğrulayın">
    MCP istemcisini yeniden başlatın veya yükleyin, istendiğinde yerel sunucuya güvenin ve `tokenlab` bağlantısını doğrulayın.

    ```bash
    claude mcp list
    codex mcp list
    ```

    Ardından ajandan `list_models` çağrısı yapmasını isteyin. Boş olmayan model listesi sunucunun başladığını, MCP keşfinin tamamlandığını ve TokenLab genel kataloğuna ulaşıldığını kanıtlar. Bu kontrol için API anahtarı gerekmez.
  </Step>
</Steps>

## Yapılandırma başvurusu

Yerel stdio istemcilerinde `tokenlab` sunucu adını, `npx` komutunu, ayrı `-y` ve `@tokenlabai/mcp-server@0.6.21` argümanlarını kullanın; çalışma dizinini boş bırakın. Güncel doğrulanmış sürümü sabitlemek için `@tokenlabai/mcp-server@0.6.21` kullanın.

Kimlik doğrulamalı araçlar için standart yapılandırma:

```json
{
  "mcpServers": {
    "tokenlab": {
      "command": "npx",
      "args": ["-y", "@tokenlabai/mcp-server@0.6.21"],
      "env": {
        "TOKENLAB_API_KEY": "<TOKENLAB_API_KEY>",
        "TOKENLAB_MCP_TOOL_PROFILE": "core"
      }
    }
  }
}
```

VS Code üst düzey `servers` nesnesini kullanır. İstemci tek sunucu nesnesi bekliyorsa yalnızca `command`, `args` ve `env` içeren iç nesneyi kopyalayın.

| Ortam değişkeni | Varsayılan | Amaç |
|---|---|---|
| `TOKENLAB_API_BASE` | `https://api.tokenlab.sh` | API temel URL'sini son eğik çizgi olmadan değiştirir. |
| `TOKENLAB_API_KEY` | Ayarlanmamış | Kimlik doğrulamalı çıkarım, medya, dosya, görev, embedding, rerank ve çeviri çağrıları için gerekir. |
| `TOKENLAB_MCP_TOOL_PROFILE` | `core` | `catalog`, `core` veya `full` seçer. |
| `TOKENLAB_MCP_SCHEMA_MODE` | Profil varsayılanı | `portable`, `exact` veya `strict` seçer; normalde `portable` kullanın. |
| `TOKENLAB_REQUEST_TIMEOUT_MS` | `120000` | API isteği zaman aşımını milisaniye cinsinden ayarlar. |
| `TOKENLAB_MCP_MAX_FILE_BYTES` | `104857600` | Her yerel yükleme dosyasının en büyük bayt boyutu. |
| `TOKENLAB_MCP_INLINE_BYTES` | `2097152` | Satır içinde döndürülen ikili veya JSON sonucunun en büyük bayt boyutu. |
| `TOKENLAB_ARTIFACT_DIR` | İşletim sistemi geçici dizini | Büyük yanıt çıktılarını yazma konumu. |

<Warning>
Gerçek API anahtarı içeren ekran görüntüsü, günlük, shell geçmişi veya yapılandırma dosyası yayımlamayın. İstemcinin gizli girişini veya ortam değişkenlerini kullanın, paylaşılan örneklerde yer tutucular bırakın ve açığa çıkan anahtarları değiştirin.
</Warning>

## Kaynaktan kurulum

Kaynak kopyasını yalnızca MCP sunucusunu denetlemek veya geliştirmek için kullanın. Normal istemci kurulumu yukarıdaki yayımlanmış npm paketini kullanmalıdır.

```bash
git clone https://github.com/hedging8563/tokenlab-mcp-server.git
cd tokenlab-mcp-server
npm install
npm test
npm start
```

`core` ve `full` varsayılan olarak boyut ve derinliği sınırlı `portable` şemalar yayımlar; sunucu her çağrıyı eksiksiz OpenAPI tanımına göre doğrular. Tam iç içe şemalar için `exact`, model sağlayıcısı kapalı nesneler ve tüm özelliklerin `required` içinde olmasını istiyorsa `strict` kullanın.

## Oluşturulan Araç Profilleri

| Profil | Kullanıma sunulan araçlar | Kapsam |
|---|---:|---|
| `catalog` | 6 | Oluşturulmuş dört genel katalog ve fiyatlandırma aracı ile `compare_models` ve `get_api_overview`. API anahtarı gerekmez. |
| `core` (varsayılan) | 31 | 29 oluşturulmuş uç nokta aracı artı `compare_models` ve `get_api_overview`. Katalog, fiyatlandırma, yerel LLM uç noktaları, görseller, video, müzik, 3D, ses, görevler, dosyalar, yerleştirmeler, yeniden sıralama ve çeviriyi kapsar. |
| `full` | 80 | 78 oluşturulmuş uç nokta aracı artı iki bileşik keşif aracı. Yanıt yaşam döngüsü, yığınlar (batches), dünyalar, yerel model keşfi ve diğer izin verilen geliştirici işlemlerini ekler. |

Araç adları, açıklamalar, girdi şemaları, HTTP yöntemleri, yollar, içerik türleri, kimlik doğrulama gereksinimleri ve görev davranışları, kayıtlı OpenAPI anlık görüntüsünden oluşturulur. Yalnızca MCP'ye özel bir katman; kararlı araç takma adları, profil üyeliği, asenkron görev semantiği, multipart varyantları, gizli veri gizleme ve akış dışı (non-streaming) kısıtlamaları sağlar.

Sürüm 0.6.0 ayrıca MCP `structuredContent` üzerinden JSON döndürür, üç sözleşme kaynağı yayımlar ve model seçimi ile yerel istek oluşturma için iki istem şablonu sunar. Oluşturulan ve bileşik araçlar, uyumlu istemcilerin daha güvenli onay politikaları uygulayabilmesi için standart risk açıklamaları içerir.

Yaygın `core` araçları şunlardır:

| Aile | Temsili araçlar |
|---|---|
| Katalog ve fiyatlandırma | `list_models`, `get_model`, `get_model_pricing`, `get_pricing`, `compare_models` |
| Yerel LLM uç noktaları | `create_chat_completion`, `create_response`, `create_anthropic_message`, `create_gemini_content` |
| Görseller | `create_image`, `create_image_file`, `edit_image`, `edit_image_file` |
| Video, müzik ve 3D | `create_video`, `create_music`, `create_3d_model` |
| Ses | `create_speech`, `transcribe_audio`, `translate_audio` |
| Yerleştirmeler ve metin | `create_embedding`, `create_multimodal_embedding`, `rerank_documents`, `translate_text` |
| Dosyalar ve görevler | `upload_file`, `list_files`, `retrieve_file`, `retrieve_file_content`, `delete_file`, `get_task_status`, `cancel_task` |

MCP araç çağrıları tek bir nihai sonuç döndürür. Yalnızca akış (streaming) ve gerçek zamanlı işlemler hariç tutulur; sunucu desteklenen `stream` argümanlarını provider bağdaştırıcılarına boolean sabiti göstermeden dahili olarak `false` değerine sabitler. Küçük görsel ve ses yanıtları yerel MCP içeriği olarak döndürülür; daha büyük ikili (binary) yanıtlar `TOKENLAB_ARTIFACT_DIR` dizinine yazılır ve yerel yolları, MIME türleri ve bayt sayıları ile döndürülür.

## Asenkron Medya Sonuçları

Video, müzik ve 3D oluşturma işlemleri asenkron görev özetleri döndürür. Görsel oluşturma ve düzenleme işlemleri, seçilen modele ve isteğe bağlı olarak tamamlanmış bir sonuç veya asenkron bir görev döndürebilir.

`delivery.mode` değeri `async` olduğunda, `delivery.terminal` değeri `true` olana kadar `{ "id": delivery.task_id }` ile `get_task_status` fonksiyonunu çağırın. Tamamlanma durumunu `status` alanından belirleyin; `progress` isteğe bağlıdır ve terminal koşulu olarak kullanılmamalıdır.

## Önerilen Ajan İş Akışı

1. Kullanıcı bir model belirtmediğinde `list_models` fonksiyonunu çağırın.
2. Görsel, video, müzik, 3D, TTS, STT, yerleştirmeler, yeniden sıralama veya çeviri gibi sohbet dışı görevler için `recommended_for` fonksiyonunu kullanın.
3. Sohbet dışı bir istek oluşturmadan, başarısız bir isteği yeniden denemeden veya uç nokta aileleri arasında geçiş yapmadan önce `get_model` fonksiyonunu çağırın.
4. Model maliyeti kullanıcının seçimini etkilediğinde `get_model_pricing` fonksiyonunu çağırın.
5. Seçim birkaç aday arasında kaldığında `compare_models` fonksiyonunu kullanın.
6. Gerekli API sözleşmesiyle eşleşen oluşturulmuş aracı çağırın veya TokenLab API ailelerinin kompakt bir haritası için `get_api_overview` fonksiyonunu kullanın.

## Yerel Uç Nokta Rehberi

TokenLab, OpenAI uyumlu rotaları ve yerel uç nokta ailelerini destekler. MCP sunucusu, bir ajan kod yazmadan önce doğru rotayı seçmesine yardımcı olur:

| Aile | Yaygın rota |
|---|---|
| OpenAI uyumlu sohbet | `/v1/chat/completions` |
| Yanıtlar | `/v1/responses` |
| Anthropic Messages | `/v1/messages` |
| Gemini yerel | `/v1beta/models/{model}:generateContent` |
| Görseller | `/v1/images/generations`, `/v1/images/edits` |
| Video | `/v1/videos/generations` |
| Müzik | `/v1/music/generations` |
| 3D | `/v1/3d/generations` |
| Ses | `/v1/audio/speech`, `/v1/audio/transcriptions`, `/v1/audio/translations` |
| Yerleştirmeler ve yeniden sıralama | `/v1/embeddings`, `/v1/rerank` |
| Metin çevirisi | `/v1/translations` |

## Barındırılan Model Gezgini

Streamable HTTP destekleyen istemciler şuraya bağlanabilir:

```text
https://tokenlab-model-explorer.vercel.app/mcp
```

Barındırılan gezgin herkese açıktır ve `open_tokenlab_model_explorer`, `compare_tokenlab_models` ve `generate_tokenlab_endpoint_example` araçlarını kullanıma sunar. Oluşturulan core/full profillerine, yerel dosya yüklemelerine veya kimlik doğrulamalı API işlemlerine ihtiyaç duyduğunuzda yukarıdaki yerel npm sunucusunu kullanın.

## TokenLab Yetenekleri (Skills) ile Kullanım

MCP sunucusu çalışma zamanında yararlıyken, [TokenLab yetenekleri deposu](https://github.com/hedging8563/tokenlab-skills), kodlama ajanlarına entegrasyon kodunu nasıl oluşturacaklarını ve onaracaklarını öğretir.

Mümkün olduğunda her ikisini de kullanın:

- MCP sunucusu: güncel modelleri, fiyatlandırmayı ve uç nokta detaylarını keşfedin.
- `tokenlab-api-integration` yeteneği: çalıştırılabilir API örnekleri oluşturun ve yapılandırılmış TokenLab hatalarını yönetin.
- `tokenlab-model-picker` yeteneği: kullanıcının görevi için güçlü modeller seçin.
- `tokenlab-native-endpoints` yeteneği: Responses, Anthropic Messages, Gemini, medya, ses, yerleştirme, yeniden sıralama veya çeviri rotalarının ne zaman kullanılacağına karar verin.

## Sorun Giderme

<AccordionGroup>
  <Accordion title="İstemci sunucuyu başlatamıyor">
    Node.js sürümünün en az 18.17 olduğunu ve istemcinin `npx` komutunu bulabildiğini doğrulayın. npm veya ağ hatalarını görmek için terminalde `npx -y @tokenlabai/mcp-server@0.6.21` çalıştırın. Başarılı stdio başlangıcı MCP mesajlarını sessizce bekler; kontrol sonrası **Ctrl+C** ile kapatın.
  </Accordion>

  <Accordion title="Sunucu yapılandırıldı ancak araçlar görünmüyor">
    İstemciyi yeniden başlatın ve yerel sunucuyu onaylayın. Claude Code'da `claude mcp list` veya `/mcp`, VS Code'da **MCP: List Servers** ardından **Show Output**, Cursor'da **Settings → Tools & MCP** bölümünü kontrol edin.
  </Accordion>

  <Accordion title="Claude Code Pending approval gösteriyor">
    Proje kapsamındaki `.mcp.json` sunucuları çalışma alanı güveni ve açık onay gerektirir. Claude Code'u projede etkileşimli başlatın, güveni kabul edin ve `/mcp` içinde `tokenlab` sunucusunu onaylayın. Tüm projelerde kullanılabilen özel ayar için `--scope user` kullanın.
  </Accordion>

  <Accordion title="Model listesi boş">
    Makinenin `https://api.tokenlab.sh/v1/models` adresine ulaşabildiğini doğrulayın. `TOKENLAB_API_BASE` değerini geçersiz kılıyorsanız, sonunda eğik çizgi (/) olmadığından emin olun.
  </Accordion>

  <Accordion title="Ajan hala eski model kimliklerini seçiyor">
    Ajandan bir model adını sabit kodlamadan (hardcoding) önce `list_models` veya `get_model` fonksiyonunu çağırmasını isteyin. MCP sunucusunu `tokenlab-model-picker` ile eşleştirmek daha iyi sonuçlar verir.
  </Accordion>

  <Accordion title="Bu sunucu ücretli TokenLab çıkarım API'lerini çağırabilir mi?">
    Evet. `TOKENLAB_API_KEY` değerini ayarlayın, ardından oluşturulan LLM, medya, ses, görev, dosya, yerleştirme, yeniden sıralama veya çeviri araçlarını kullanın. Katalog ve fiyatlandırma araçları anahtar olmadan kullanılabilir kalmaya devam eder.
  </Accordion>

  <Accordion title="Kimlik doğrulamalı araç yetkilendirme hatası döndürüyor">
    `TOKENLAB_API_KEY` değerini araç argümanında veya istemde değil MCP sunucusunun ortamında ayarlayın ve değişiklikten sonra istemciyi yeniden başlatın. Paylaşılan dosya, görüntü, günlük veya shell geçmişinde görünen anahtarı değiştirin.
  </Accordion>

  <Accordion title="İstemci şemaları reddediyor veya çok fazla araç yüklüyor">
    6 keşif aracı için `TOKENLAB_MCP_TOOL_PROFILE=catalog`, 31 araç için varsayılan `core` profilini kullanın. Normalde `portable` kullanın; tam iç içe yapı için `exact`, kapalı nesneler ve tüm özelliklerin `required` içinde olması için `strict` seçin.
  </Accordion>

  <Accordion title="Paket sürümünü nasıl sabitler veya güncellerim?">
    Güncel doğrulanmış sürümü sabitlemek için `@tokenlabai/mcp-server@0.6.21` kullanın. Sürüm değiştirirken paket argümanını güncelleyin, sunucuyu yeniden başlatın ve daha yeni sürümü kullanmadan önce genel depoyu inceleyin.
  </Accordion>
</AccordionGroup>

## Kaynaklar

<CardGroup cols={2}>
  <Card title="TokenLab MCP" icon="plug" href="https://tokenlab.sh/mcp">
    Ürün özeti ve Core 31 ile Full 80 araç setleri için bağlantı seçenekleri
  </Card>
  <Card title="GitHub Deposu" icon="github" href="https://github.com/hedging8563/tokenlab-mcp-server">
    Kaynak kod ve yerel kurulum talimatları
  </Card>
  <Card title="TokenLab Yetenekleri" icon="sparkles" href="https://github.com/hedging8563/tokenlab-skills">
    TokenLab entegrasyonları için bakımı yapılan ajan yetenekleri
  </Card>
  <Card title="Model Katalog API" icon="list" href="https://api.tokenlab.sh/v1/models">
    Genel model keşif uç noktası
  </Card>
  <Card title="llms.txt" icon="file-lines" href="https://api.tokenlab.sh/llms.txt">
    Ajan tarafından okunabilir TokenLab API özeti
  </Card>
  <Card title="Glama MCP Sunucusu" icon="globe" href="https://glama.ai/mcp/servers/hedging8563/tokenlab-mcp-server">
    Doğrulanmış TokenLab MCP Sunucusu listesini görüntüleyin
  </Card>
  <Card title="Glama Model Gezgini" icon="search" href="https://glama.ai/mcp/servers/hedging8563/tokenlab-openai-apps-model-explorer">
    Modelleri, fiyatlandırmayı ve yerel uç nokta örneklerini keşfedin
  </Card>
  <Card title="MCP.so Listesi" icon="compass" href="https://mcp.so/servers/tokenlab-mcp-server">
    Yapay Zeka ve Ajanlar dizininde TokenLab'i keşfedin
  </Card>
  <Card title="Barındırılan Model Gezgini" icon="rocket" href="https://tokenlab-model-explorer.vercel.app/widget">
    Genel model gezginini ve uzak MCP uç noktasını açın
  </Card>
  <Card title="Claude Code MCP Rehberi" icon="terminal" href="https://code.claude.com/docs/en/mcp">
    Kapsamlar, onaylar, durum kontrolleri ve MCP yapılandırması
  </Card>
  <Card title="Cursor MCP Rehberi" icon="arrow-pointer" href="https://docs.cursor.com/context/model-context-protocol">
    Yapılandırma konumları, araç denetimi ve güvenlik rehberi
  </Card>
  <Card title="VS Code MCP Rehberi" icon="code" href="https://code.visualstudio.com/docs/agent-customization/mcp-servers">
    Kullanıcı ve çalışma alanı ayarları, güven, günlükler ve sunucu yönetimi
  </Card>
</CardGroup>
