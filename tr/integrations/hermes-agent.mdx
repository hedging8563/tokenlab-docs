---
title: "Hermes Agent"
description: "Hermes Agent ile TokenLab bağlantısı"
---

Mevcut hesapları, ayarları ve araç izinlerini koruyarak TokenLab adlı ayrı bir sağlayıcı ekleyin.

## Bu görevi Agent’a ver

```text
Bu kılavuzu okuyup Hermes’i TokenLab’a bağla:
https://docs.tokenlab.sh/tr/integrations/hermes-agent
Önce kurulu sürümü ve etkin yapılandırmayı kontrol et.
Mevcut hesapları, sağlayıcıları, varsayılan modeli ve izinleri koru; yedek al ve değişiklikleri göster.
Anahtarı yerel olarak gireceğim; sohbette isteme, yazdırma veya yapıştırma.
Önce yapılandırmanın yüklenmesini doğrula. Ücretli denemeyi ayrıca açıkla ve yalnızca ben seçtikten sonra çalıştır.
```

## Hermes kurulumu ve kontrolü

[Resmî kurulum kılavuzu](https://hermes-agent.nousresearch.com/docs/getting-started/installation), Apple Silicon macOS ve Windows masaüstü yükleyicileri ile Linux ve yerel Windows CLI kurulumunu sunar. Sistem gereksinimlerini kontrol edin; Hermes kuruluysa kurulumu atlayın.

## İsteğe bağlı: geri alınabilir yapılandırma

TokenLab Skills **0.1.3**, resmi Hermes **0.21.3** için bir yapılandırma aracı içerir. Değişikliği önizler, yedekler ve yalnızca adlandırılmış sağlayıcı ekler; YAML yorumlarını, hesapları, varsayılan modeli ve izinleri korur. Önce [kurulum başvurusunu](https://github.com/hedging8563/tokenlab-skills/blob/900de0c4e296c0d2ed00b5d842815ed89c403503/skills/tokenlab-api-integration/references/hermes_setup.md) okuyun.

`SKILL_DIR` değişkenini kurulu `tokenlab-api-integration` klasörüne, `HERMES_PYTHON` değişkenini `hermes --version` çıktısındaki kurulum dizininin Python’una ayarlayın: macOS/Linux için `venv/bin/python`, Windows için `venv\Scripts\python.exe`. PowerShell’de `$SkillDir` ve `$HermesPython` kullanılır. Araç mevcut ortamı kullanır; genel Python paketleri kurmaz.

```bash
"$HERMES_PYTHON" "$SKILL_DIR/scripts/configure_hermes.py" --model gpt-5.6-luna
"$HERMES_PYTHON" "$SKILL_DIR/scripts/configure_hermes.py" --model gpt-5.6-luna --apply
```

```powershell
& $HermesPython "$SkillDir/scripts/configure_hermes.py" --model gpt-5.6-luna
& $HermesPython "$SkillDir/scripts/configure_hermes.py" --model gpt-5.6-luna --apply
```

Uyguladıktan sonra aşağıdaki elle birleştirmeyi atlayın; yazdırılan dizin ve yükleme komutlarını izleyin. `active_profile` algılanır. Yapılandırmayı okumak anahtarı doğrulamaz. `--restore` ile geri almayı önizleyin; özgün baytları geri yüklemek için `--apply` ekleyin. Çakışmalar, sonraki düzenlemeler, yönetilen ayarlar veya WSL otomasyonu durdurur; mevcut ayarları koruyup elle kurulum yolunu kullanın.

## Adlandırılmış sağlayıcıyı yapılandırın

Tanımlıysa `HERMES_HOME` dizinini, değilse `~/.hermes` dizinini kullanın (Windows: `%LOCALAPPDATA%\hermes`). `config.yaml` dosyasını yedekleyip diğer ayarları değiştirmeden aşağıdaki girdileri birleştirin. Chat Completions ve araç çağrılarını destekleyen güncel bir model seçin.

Etkin yapılandırmayı yedekleyin. Yalnızca TokenLab girdisini ekleyip mevcut sağlayıcıları, hesapları, varsayılan modeli ve izinleri koruyun. Ad kullanımdaysa başka bir ad seçip komutları güncelleyin. Geri almak için yalnızca eklediğiniz girdiyi kaldırın veya önceki yedeğini yükleyin.

```yaml
providers:
  tokenlab:
    api: https://api.tokenlab.sh/v1
    key_env: TOKENLAB_API_KEY
    transport: chat_completions
    default_model: gpt-5.6-luna
```

[API anahtarları](https://tokenlab.sh/dashboard/api?tab=keys) sayfasında anahtar oluşturun. Anahtarı Hermes dizinindeki `.env` dosyasına veya başlatan sürecin ortamına `TOKENLAB_API_KEY` olarak yerel biçimde kaydedin. Sohbete veya sağlayıcının YAML dosyasına yapıştırmayın.

## Önce kontrol, sonra istek

Sağlayıcıyı `hermes config get providers.tokenlab.api` ile kontrol edin; `https://api.tokenlab.sh/v1` görünmelidir. Mevcut `model` bölümünü koruyun. Aşağıdaki sağlayıcı ve modeli açıkça seçerek yeni oturum başlatın; bu seçenekler olmadan önceki varsayılan kullanılır. Yapılandırma denetimi anahtarı doğrulamaz.

```bash
hermes chat --provider custom:tokenlab --model gpt-5.6-luna
```

TokenLab bakiyesini kullanmaya hazır olduğunuzda kısa bir metin gönderip yanıtı [istek kaydıyla](https://tokenlab.sh/dashboard/runs?section=requests) karşılaştırın. Ardından salt okunur bir araç görevi deneyin. Yardımcı modeller ve isteğe bağlı araçlar ayrı sağlayıcılar veya anahtarlar kullanabilir.

## Geri alma ve sorun giderme

Sağlayıcı tanınmıyorsa sürümü ve [güncel biçimi](https://hermes-agent.nousresearch.com/docs/integrations/providers) kontrol edin. Kimlik doğrulama hatasında anahtarı yazdırmadan ortamı ve anahtar durumunu inceleyin. Geri almak için önceki model seçimini geri yükleyip yalnızca eklediğiniz TokenLab girdilerini kaldırın.

MCP ve Skills ayrı uzantılardır: [MCP kılavuzu](/tr/integrations/tokenlab-mcp-server) API araçları, [Skill kılavuzu](/tr/integrations/coding-agent-skill) entegrasyon yönergeleri sağlar. Bu sağlayıcı ayarı için gerekli değildir.
