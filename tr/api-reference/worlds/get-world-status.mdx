---
title: "World Durumunu Al"
openapi: "GET /v1/worlds/generations/{id}"
description: "World Labs Marble dünya üretim görevinin durumunu ve sonucunu alır"
---

World Labs Marble üretim görevinin durumunu kontrol etmek için bu endpointi poll edin. Oluşturma yanıtı `poll_url` içeriyorsa tam olarak o URLyi kullanmayı tercih edin.

<Info>
Süresi dolmuş veya kullanılamayan bir görev `404 async_task_not_found` döndürebilir. Geçerli API anahtarının görev sahipliği doğrulanamazsa yanıt `403 task_not_owned` olur.
</Info>

## Yol Parametreleri

<ParamField path="id" type="string" required>
  Dünya üretim görevi IDsi.
</ParamField>

## Yanıt

Başarılı durum yanıtında yalnızca `id` ve `status` zorunludur. Görev takma adları, işlem ID’leri, dünya varlıkları, açıklamalar, maliyet ve metin türündeki `error` mevcut olduğunda döner. Tamamlanan görev, tüm varlık URL’lerinin bulunduğunu garanti etmez.

<ResponseField name="id" type="string">
  Genel görev IDsi.
</ResponseField>

<ResponseField name="task_id" type="string">
  Asenkron görev tanımlayıcı aliası.
</ResponseField>

<ResponseField name="operation_id" type="string">
  World Labs işlem kimliği.
</ResponseField>

<ResponseField name="status" type="string">
  Görev durumu: `pending`, `processing`, `completed`, `failed` veya `timeout`.
</ResponseField>

<ResponseField name="world_id" type="string">
  Tamamlandığında World Labs dünya kimliği.
</ResponseField>

<ResponseField name="world_marble_url" type="string">
  Tamamlandığında oluşturulan Marble dünya URLsi.
</ResponseField>

<ResponseField name="model_url" type="string">
  Birincil oluşturulan dünya/model URLsi için alias.
</ResponseField>

<ResponseField name="glb_url" type="string">
  Varsa collider mesh GLB URLsi.
</ResponseField>

<ResponseField name="pano_url" type="string">
  Varsa panorama görüntü URLsi.
</ResponseField>

<ResponseField name="thumbnail_url" type="string">
  Varsa thumbnail görüntü URLsi.
</ResponseField>

<ResponseField name="caption" type="string">
  Oluşturulan dünya için upstream caption.
</ResponseField>

<ResponseField name="cost" type="object">
  Varsa `total_credits` dahil üretim maliyet bilgileri. Faturalanan tutar için kullanım kayıtlarına bakın.
</ResponseField>

<ResponseField name="error" type="string">
  Görev başarısız olduğunda hata mesajı.
</ResponseField>

<RequestExample>
```bash cURL
curl --fail-with-body "https://api.tokenlab.sh/v1/worlds/generations/ldtask_aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa" \
  -H "Authorization: Bearer sk-your-api-key"
```

```python Python
import requests
import time

task_id = "ldtask_aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"

while True:
    response = requests.get(
        f"https://api.tokenlab.sh/v1/worlds/generations/{task_id}",
        headers={"Authorization": "Bearer sk-your-api-key"},
    )
    response.raise_for_status()
    result = response.json()
    if result["status"] in ["completed", "failed", "timeout"]:
        print(result)
        break
    time.sleep(10)
```

```javascript JavaScript
const taskId = 'ldtask_aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa';
const response = await fetch(`https://api.tokenlab.sh/v1/worlds/generations/${taskId}`, {
  headers: { Authorization: 'Bearer sk-your-api-key' }
});

if (!response.ok) throw new Error(await response.text());
const result = await response.json();
console.log(result);
```
</RequestExample>
