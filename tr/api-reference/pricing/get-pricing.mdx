---
title: "Fiyatları Al"
openapi: "GET /v1/pricing"
description: "Modeller için fiyat bilgisi alır"
---

Bu herkese açık uç nokta API anahtarı gerektirmez.

## Genel Bakış

Bu endpoint, aktif modeller için mevcut herkese açık fiyat yüzeyini listeler.

Bir modelin yapılandırılmış fiyatlandırma ve provenance dahil tam fiyat detayları için [Model Fiyatını Al](/tr/api-reference/models/get-model) sayfasını tercih edin.

## Sorgu Parametreleri

<ParamField query="provider" type="string">
  `openai`, `anthropic` veya `google` gibi isteğe bağlı provider filtresi.
</ParamField>

<ParamField query="tag" type="string">
  `chat`, `image`, `video`, `embedding` veya `translation` gibi isteğe bağlı model etiketi filtresi.
</ParamField>

## Yanıt

<ResponseField name="object" type="string">
  Her zaman `pricing`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  Güncel herkese açık fiyatların yayımlandığı ISO zaman damgası.
</ResponseField>

<ResponseField name="currency" type="string">
  Her zaman `USD`.
</ResponseField>

<ResponseField name="data" type="array">
  Fiyat girişleri dizisi. Her öğe `model`, `provider`, `is_lock_price`, `capabilities`, `aliases` ve yukarıdaki etkileşimli şemanın döndürdüğü herkese açık scalar ya da yapılandırılmış fiyat özet alanlarını içerir.
</ResponseField>

<RequestExample>
```bash cURL
curl "https://api.tokenlab.sh/v1/pricing?provider=openai&tag=chat"
```

```python Python
import requests

response = requests.get(
    "https://api.tokenlab.sh/v1/pricing",
    params={"provider": "openai", "tag": "chat"}
)

print(response.json())
```

```javascript JavaScript
const response = await fetch(
  'https://api.tokenlab.sh/v1/pricing?provider=openai&tag=chat'
);

const data = await response.json();
console.log(data);
```

```go Go
package main

import (
    "encoding/json"
    "fmt"
    "net/http"
)

func main() {
    req, _ := http.NewRequest("GET", "https://api.tokenlab.sh/v1/pricing?provider=openai&tag=chat", nil)

    client := &http.Client{}
    resp, _ := client.Do(req)
    defer resp.Body.Close()

    var result map[string]interface{}
    json.NewDecoder(resp.Body).Decode(&result)
    fmt.Println(result)
}
```

```php PHP
<?php
$ch = curl_init('https://api.tokenlab.sh/v1/pricing?provider=openai&tag=chat');

curl_setopt_array($ch, [
    CURLOPT_RETURNTRANSFER => true
]);

$response = curl_exec($ch);
curl_close($ch);

$data = json_decode($response, true);
print_r($data);
```
</RequestExample>

<ResponseExample>
```json Response
{
  "object": "pricing",
  "updated_at": "2026-04-20T00:00:00.000Z",
  "currency": "USD",
  "data": [
    {
      "model": "gpt-4o",
      "provider": "openai",
      "is_lock_price": false,
      "input_per_1m_tokens": "2.50",
      "output_per_1m_tokens": "10.00",
      "capabilities": ["chat"],
      "aliases": ["gpt-4o-latest"]
    }
  ]
}
```
</ResponseExample>

<Note>
Doğru yanıt şeması için yukarıdaki etkileşimli OpenAPI panelini kullanın. Genel fiyat girişleri ayrıca `lock_price_per_request`, `has_complex_pricing`, `supported_operations`, `pricing_summary` ve `pricing_provenance` alanlarını da gösterebilir.
</Note>
