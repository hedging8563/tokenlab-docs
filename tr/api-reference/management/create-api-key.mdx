---
title: "API Anahtarı Oluştur"
openapi: "POST /v1/management/api-keys"
description: "Geçerli kuruluş için yeni bir kullanıcı API anahtarı oluşturur."
# Synced from canonical OpenAPI x-mint.metadata.playground.
playground: simple
---

## Genel Bakış

Geçerli yönetim token'ı ile ilişkili kuruluşta yeni bir `type=user` API anahtarı oluşturur.

## İstek Gövdesi

| Alan | Tür | Varsayılan / Sınırlar | Notlar |
|------|-----|----------------------|--------|
| `name` | string | isteğe bağlı, varsayılan `Default Key`, uzunluk `1-50` | Görünen ad, sunucu tarafında baştaki ve sondaki boşluklar kaldırılır |
| `limitAmount` | number \| string \| null | `0–100000` USD | `null` sınırsız anlamına gelir; `0` harcamayı engeller. Ondalık dizeler en fazla 6 ondalık basamak destekler. Anahtar oluştururken bu alanı atlamak sınırsız anlamına gelir. |
| `limitCurrency` | enum | varsayılan `USD` | Yalnızca USD. `CNY` gönderildiğinde `400 currency_retired` döner. |
| `models` | string[] | varsayılan `[]` | Opsiyonel model allowlist'i |
| `deliveryPolicy` | string \| null | `auto`, `verified`, `official`, `null` | `null`, çalışma alanının teslim politikasını devralır. |
| `expiresAt` | string \| null | RFC3339 datetime | `null` son kullanma olmadığı anlamına gelir |

## Notlar

- Parasal alanlar yalnızca USD’yi destekler. `CNY` girişleri kullanımdan kaldırılmıştır ve `400 currency_retired` döndürür.

- API anahtarının tam değeri yanıtta yalnızca bir kez gösterilir.
- `models: []`, anahtarın kuruluş politikasının ötesinde ek model kısıtı olmadığı anlamına gelir.
- `limitAmount: 0`, geçerli ama kota harcayamayan bir anahtar oluşturur; daha sonra güncellenebilir.

## Örnek

<RequestExample>
```bash cURL
curl -X POST "https://api.tokenlab.sh/v1/management/api-keys" \
  -H "Authorization: Bearer mt-your-management-token" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Backend Worker",
    "limitAmount": 500,
    "models": ["veo3.1", "kling-3.0-video"],
    "expiresAt": "2026-12-31T23:59:59Z"
  }'
```
</RequestExample>

## İstek / Yanıt

Bu sayfa OpenAPI şemasını ve kopyalanabilir istek örneklerini sunar. Yönetim isteklerini Management Token kullanarak terminalinizden veya istemcinizden gönderin; bu sayfadan doğrudan gönderim yapılamaz.

## Yanıt örneği

<ResponseExample>
```json 201 Created
{
  "id": "key_abc123def456",
  "name": "Backend Worker",
  "key": "sk-live-redacted",
  "key_prefix": "sk-live...",
  "status": "active",
  "limit_amount": 500,
  "limit_amount_decimal": "500",
  "used_amount": 0,
  "used_amount_decimal": "0",
  "models": [
    "veo3.1",
    "kling-3.0-video"
  ],
  "expires_at": "2026-12-31T23:59:59.000Z",
  "last_used_at": null,
  "created_at": "2026-07-08T03:00:00.000Z",
  "delivery_policy": null
}
```
</ResponseExample>

## Önemli alanlar

<ResponseField name="key" type="string">Tam gizli API anahtarı değeri. Yalnızca oluşturma sırasında bir kez döndürülür.</ResponseField>
<ResponseField name="id" type="string">Sonraki Yönetim API’si çağrılarında kullanılan API anahtarı tanımlayıcısı.</ResponseField>
<ResponseField name="name" type="string">API anahtarının görünen adı.</ResponseField>
<ResponseField name="key_prefix" type="string">Görüntüleme ve destek için gizli olmayan anahtar öneki.</ResponseField>
<ResponseField name="status" type="string">`active`, `inactive`, `suspended` veya `revoked` değerlerinden biri.</ResponseField>
<ResponseField name="limit_amount" type="number | null">Görüntüleme için JSON sayısı olarak USD harcama limiti. `null` sınırsız anlamına gelir.</ResponseField>
<ResponseField name="limit_amount_decimal" type="string | null">Ondalık dize olarak kesin USD harcama limiti. `null` sınırsız anlamına gelir.</ResponseField>
<ResponseField name="used_amount" type="number">Görüntüleme için JSON sayısı olarak USD cinsinden birikmiş kullanım harcaması.</ResponseField>
<ResponseField name="used_amount_decimal" type="string">Ondalık dize olarak USD cinsinden kesin birikmiş kullanım harcaması.</ResponseField>
<ResponseField name="models" type="string[]">Anahtar başına izin verilen model listesi. Boş dizi, anahtar düzeyinde ek model kısıtlaması olmadığı anlamına gelir.</ResponseField>
<ResponseField name="delivery_policy" type="string | null">`auto`, `verified`, `official`, `null`. `null`, çalışma alanının teslim politikasını devralır.</ResponseField>
<ResponseField name="expires_at" type="string | null">Anahtarın süresinin dolduğu ISO zaman damgası veya süre sonu yoksa `null`.</ResponseField>
<ResponseField name="last_used_at" type="string | null">En son kullanımın ISO zaman damgası veya hiç kullanılmadıysa `null`.</ResponseField>
<ResponseField name="created_at" type="string">Oluşturma ISO zaman damgası.</ResponseField>
