---
title: "Seedance 2.0 Video Modelleri"
description: "Bir Seedance 2.0 modeli ve doğru TokenLab entegrasyon yolunu seçin."
---

Bir uygulamaya Seedance 2.0 eklerken buradan başlayın. Bu sayfa yalnızca model ve iş akışı seçimini ele alır; Volcengine geçişi, yeniden kullanılabilir materyaller ve gerçek kişi doğrulaması ayrı kılavuzlardadır.

## Entegrasyon yolunu seçin

| Amaç | Kullanılacak yol | Sonraki belge |
| --- | --- | --- |
| Yeni TokenLab video entegrasyonu oluşturmak | Birleşik `POST /v1/videos/generations` API | [Video oluşturma kılavuzu](/tr/guides/video-generation) |
| Mevcut Volcengine Seedance istemcisini taşımak | Volcengine uyumlu REST veya Action API | [Seedance’i Volcengine’den taşıma](/tr/guides/seedance-volcengine-migration) |
| Avatar, ürün, stil veya gerçek kişi referansını yeniden kullanmak | TokenLab materyal kitaplığı | [Seedance materyalleri ve gerçek kişi doğrulaması](/tr/guides/seedance-materials) |

Üç yol da `Authorization: Bearer <TOKENLAB_API_KEY>` kullanır. Volcengine uyumlu yollar istek şeklini korur ancak AK/SK imzasını kabul etmez.

## Model Seçimi

| Model | Desteklenen çözünürlükler |
|-------|--------------------------|
| `seedance-2.0` | `480p`, `720p`, `1080p`, `4k` |
| `seedance-2.0-fast` | `480p`, `720p` |
| `seedance-2.0-mini` | `480p`, `720p` |

`seedance-2.0-fast` ve `seedance-2.0-mini` modelleri `1080p` ve `4k` isteklerini reddeder. Ses, yalnızca desteklenen referans iş akışlarında kullanılabilir; sadece ses veya sadece metin artı ses içeren Seedance istekleri desteklenmez.

## Desteklenen İşlemler

| İşlem | Tipik girdiler |
|-----------|----------------|
| `text-to-video` | `prompt` |
| `image-to-video` | `prompt`, `image_url` veya uyumlu görsel girdisi |
| `start-end-to-video` | `prompt`, `start_image`, `end_image` |
| `reference-to-video` | `prompt`, `reference_images`, isteğe bağlı `video_urls`, isteğe bağlı `audio_urls` |
| `video-to-video` | `prompt`, `video_url` veya uyumlu video girdisi |
| `video-extension` | `task_id` veya modele özel devam ettirme girdisi |

## Fiyatlandırma ve Asenkron Sonuçlar

Seedance 2.0 fiyatlandırması çıktı çözünürlüğüne, girdi türüne ve modele bağlıdır. Ürün arayüzünü kodlamadan önce [Pricing API](/tr/api-reference/pricing/get-pricing) veya [Models API](/tr/api-reference/models/list-models) kullanın.

Video oluşturma asenkrondur. Oluşturma istekleri tarafından döndürülen `poll_url` adresini takip edin veya dönen görev kimliği (task ID) ile [Görev Durumunu Al](/tr/api-reference/tasks/get-task-status) kullanın.

## İlgili API’ler

| API | Sonraki belge |
|-----|---------------|
| `POST /v1/videos/generations` | [Video oluştur](/tr/api-reference/video/create-video) |
| `GET /v1/tasks/{id}` | [Görev durumunu al](/tr/api-reference/tasks/get-task-status) |
| Seedance görev uyumluluğu | [Volcengine uyumlu görev oluştur](/tr/api-reference/video/create-volc-compatible-seedance-task) |
| Materyal Action uyumluluğu | [Materyal Action’ları (Volcengine uyumlu)](/tr/api-reference/video/volc-compatible-material-actions) |
