---
title: "Server MCP TokenLab"
description: "Hubungkan Claude Code, Cursor, VS Code, Codex, dan klien MCP lainnya ke alat model, harga, inferensi, media, file, dan tugas TokenLab."
---

## Pilih ekstensi yang diperlukan

- MCP menambahkan alat TokenLab API pada klien yang kompatibel. Mulai dengan `catalog` tanpa kunci API di bawah.
- [TokenLab Skill](/id/integrations/coding-agent-skill) memasang petunjuk integrasi melalui `npx skills add`, bukan menjalankan server MCP.

Keduanya memperluas agen yang sudah ada. Untuk mengganti penyedia model utama, ikuti panduan klien tersebut.

## Serahkan pengaturan kepada agen saya

Salin tugas ini ke agen yang sudah berfungsi di komputer Anda:

```text
Baca panduan ini dan pilih alat katalog MCP atau TokenLab Skill sesuai tugas saya:
https://docs.tokenlab.sh/id/integrations/tokenlab-mcp-server
Periksa terlebih dahulu versi yang terpasang dan konfigurasi yang aktif.
Pertahankan akun, penyedia, izin, dan pengaturan lain yang sudah ada.
Cadangkan file lokal dan tampilkan perubahan yang diusulkan.
Biarkan saya memasukkan kunci API yang diperlukan secara lokal; jangan pernah meminta, menampilkan, atau menempelkannya di chat.
Periksa terlebih dahulu apakah konfigurasi dapat dimuat.
Jelaskan secara terpisah biaya setiap pengujian yang mengirim permintaan berbayar sebelum menjalankannya.
```

[Server MCP TokenLab](https://github.com/hedging8563/tokenlab-mcp-server) menghasilkan alat MCP dari kontrak OpenAPI publik TokenLab. Agen yang kompatibel dengan MCP dapat menemukan model dan harga, memanggil endpoint LLM native dan yang kompatibel dengan OpenAI, membuat media multimodal, mengunggah file, dan mengelola tugas asinkron.

Gunakan server ini saat agen memerlukan kontrak API TokenLab terkini alih-alih daftar alat yang dikelola secara manual. Profil `core` default mengekspos 29 alat endpoint yang dihasilkan ditambah dua alat penemuan komposit. Profil `full` mengekspos 78 alat endpoint yang dihasilkan ditambah dua alat komposit yang sama.

Halaman ini menjelaskan cara menambahkan TokenLab sebagai server alat di Claude Code, Cursor, VS Code, Codex, Claude Desktop, Cline, Windsurf, atau klien MCP lainnya. Untuk memanggil TokenLab langsung dari kode aplikasi, mulai dari [panduan cepat API](/id/quickstart).

<Note>
Alat katalog publik dan harga tidak memerlukan kunci API TokenLab. Atur `TOKENLAB_API_KEY` untuk operasi inferensi, media, audio, file, tugas, embedding, rerank, dan terjemahan yang memerlukan kredensial. Jangan pernah menempatkan kunci API dalam argumen alat.
</Note>

## Apa yang Disediakan

- Penemuan model langsung dari `https://api.tokenlab.sh/v1/models`.
- Pencarian detail model dari `/v1/models/{model}`.
- Pencarian harga dari `/v1/models/{model}/pricing`.
- Ringkasan API yang dapat dibaca agen dari `https://api.tokenlab.sh/llms.txt`.
- Chat Completions dan Responses yang kompatibel dengan OpenAI, Anthropic Messages, dan Gemini generateContent tanpa mengubah bentuk permintaan native-nya.
- Pembuatan dan pengeditan gambar melalui URL JSON, ID file TokenLab, atau file multipart lokal.
- Video, musik, 3D, ucapan, transkripsi, terjemahan audio, embedding, embedding multimodal, rerank, dan terjemahan teks.
- Ringkasan tugas asinkron yang dinormalisasi untuk operasi pembuatan gambar, video, musik, 3D, dan dunia.
- Sinkronisasi kontrak harian yang mengambil dokumen OpenAPI publik, membuat ulang skema alat, menjalankan pengujian endpoint dan file, serta memperbarui `main` hanya jika setiap pemeriksaan berhasil.

## Mulai cepat

Cara paling cepat dan aman untuk memulai adalah mengaktifkan hanya enam alat katalog publik. Anda tidak memerlukan kunci API TokenLab dan dapat mengaktifkan alat berkredensial nanti.

<CardGroup cols={2}>
  <Card title="Instal di Cursor" icon="arrow-pointer" href="https://cursor.com/en/install-mcp?name=tokenlab&config=eyJ0eXBlIjoic3RkaW8iLCJjb21tYW5kIjoibnB4IiwiYXJncyI6WyIteSIsIkB0b2tlbmxhYmFpL21jcC1zZXJ2ZXJAMC42LjE5Il0sImVudiI6eyJUT0tFTkxBQl9NQ1BfVE9PTF9QUk9GSUxFIjoiY2F0YWxvZyJ9fQ%3D%3D">
    Tambahkan profil publik `catalog` ke Cursor dengan sekali klik
  </Card>
  <Card title="Instal di VS Code" icon="code" href="https://vscode.dev/redirect/mcp/install?name=tokenlab&config=%7B%22type%22%3A%22stdio%22%2C%22command%22%3A%22npx%22%2C%22args%22%3A%5B%22-y%22%2C%22%40tokenlabai%2Fmcp-server%400.6.19%22%5D%2C%22env%22%3A%7B%22TOKENLAB_MCP_TOOL_PROFILE%22%3A%22catalog%22%7D%7D">
    Tambahkan profil publik `catalog` ke konfigurasi pengguna VS Code
  </Card>
</CardGroup>

<Steps>
  <Step title="Periksa persyaratan">
    Instal Node.js 18.17 atau yang lebih baru dan pastikan `npx` tersedia.

    ```bash
    node --version
    npx --version
    ```

    Anda tidak perlu memasang paket MCP secara global atau mengkloning repositorinya.
  </Step>

  <Step title="Pilih tingkat akses">
    | Tujuan | Kunci API | Profil |
    |---|---|---|
    | Menelusuri model, detail, dan harga | Tidak diperlukan | `catalog` (6 alat) |
    | Menggunakan operasi inferensi, media, file, dan tugas umum | Diperlukan untuk panggilan berbayar | `core` (31 alat, default) |
    | Menampilkan semua operasi developer yang diizinkan | Diperlukan untuk panggilan berbayar | `full` (80 alat) |

    Buat kunci di [Dashboard → API Keys](https://tokenlab.sh/dashboard/api) hanya untuk panggilan API berkredensial. Simpan kunci di lingkungan rahasia klien dan jangan tempelkan ke prompt atau argumen alat MCP.
  </Step>

  <Step title="Konfigurasikan klien">
    Contoh publik secara eksplisit memilih `catalog` agar semua alat yang ditampilkan bekerja tanpa kunci.

    Cadangkan konfigurasi aktif. Tambahkan hanya entri TokenLab dan pertahankan penyedia, akun, model default, serta izin yang ada. Jika namanya sudah dipakai, pilih nama lain dan sesuaikan perintah. Untuk membatalkan, hapus hanya entri tambahan atau pulihkan cadangan sebelumnya.

<Tabs>
      <Tab title="Claude Code">
        Tambahkan TokenLab pada cakupan pengguna agar tersedia di semua proyek.

        ```bash
        claude mcp add --env TOKENLAB_MCP_TOOL_PROFILE=catalog --scope user tokenlab -- npx -y @tokenlabai/mcp-server@0.6.19
        ```

        Untuk mengaktifkan profil `core` default, tambahkan kunci API Anda.



        Gunakan `--scope local` hanya untuk proyek saat ini atau `--scope project` untuk membuat `.mcp.json` bersama. Jangan commit kunci asli ke konfigurasi proyek.
      </Tab>

      <Tab title="Cursor">
        Gunakan instalasi sekali klik di atas, atau simpan konfigurasi di `~/.cursor/mcp.json` untuk semua proyek maupun `.cursor/mcp.json` untuk satu proyek.

        ```json
        {
          "mcpServers": {
            "tokenlab": {
              "command": "npx",
              "args": ["-y", "@tokenlabai/mcp-server@0.6.19"],
              "env": { "TOKENLAB_MCP_TOOL_PROFILE": "catalog" }
            }
          }
        }
        ```
      </Tab>

      <Tab title="VS Code">
        Gunakan instalasi sekali klik, jalankan **MCP: Add Server**, atau simpan file sebagai `.vscode/mcp.json`.

        ```json
        {
          "servers": {
            "tokenlab": {
              "type": "stdio",
              "command": "npx",
              "args": ["-y", "@tokenlabai/mcp-server@0.6.19"],
              "env": { "TOKENLAB_MCP_TOOL_PROFILE": "catalog" }
            }
          }
        }
        ```

        Untuk konfigurasi yang berlaku di semua proyek, jalankan **MCP: Open User Configuration** dan tambahkan objek server yang sama.
      </Tab>

      <Tab title="Codex">
        ```bash
        # Katalog publik
                codex mcp add --env TOKENLAB_MCP_TOOL_PROFILE=catalog tokenlab -- npx -y @tokenlabai/mcp-server@0.6.19
        ```
      </Tab>

      <Tab title="Claude Desktop">
        Dalam `claude_desktop_config.json`, tambahkan perintah `npx` serta argumen `-y` dan `@tokenlabai/mcp-server@0.6.19` ke objek standar `mcpServers`. Jika hanya memerlukan alat publik, atur `TOKENLAB_MCP_TOOL_PROFILE=catalog`.
      </Tab>

      <Tab title="Klien lain">



        Untuk Windsurf, simpan konfigurasi standar `mcpServers` di `~/.codeium/windsurf/mcp_config.json`. Klien stdio lainnya memakai perintah dan argumen yang sama tanpa direktori kerja.
      </Tab>
    </Tabs>
  </Step>

  <Step title="Mulai ulang dan verifikasi">
    Mulai ulang atau muat ulang klien MCP, setujui server lokal saat diminta, lalu pastikan `tokenlab` terhubung.

    ```bash
    claude mcp list
    codex mcp list
    ```

    Setelah itu, minta agen memanggil `list_models`. Daftar model yang tidak kosong membuktikan server telah dimulai, penemuan MCP selesai, dan katalog publik TokenLab dapat dijangkau. Pemeriksaan ini tidak memerlukan kunci API.
  </Step>
</Steps>

## Referensi konfigurasi

Pada klien stdio lokal, gunakan nama `tokenlab`, perintah `npx`, argumen terpisah `-y` dan `@tokenlabai/mcp-server@0.6.19`, serta biarkan direktori kerja kosong. Gunakan `@tokenlabai/mcp-server@0.6.19` untuk mengunci rilis yang saat ini telah diverifikasi.

Konfigurasi standar untuk alat berkredensial:

```json
{
  "mcpServers": {
    "tokenlab": {
      "command": "npx",
      "args": ["-y", "@tokenlabai/mcp-server@0.6.19"],
      "env": {
        "TOKENLAB_API_KEY": "<TOKENLAB_API_KEY>",
        "TOKENLAB_MCP_TOOL_PROFILE": "core"
      }
    }
  }
}
```

VS Code memakai objek `servers` di tingkat teratas. Jika klien hanya meminta satu objek server, salin objek bagian dalam yang berisi `command`, `args`, dan `env`.

| Variabel lingkungan | Default | Tujuan |
|---|---|---|
| `TOKENLAB_API_BASE` | `https://api.tokenlab.sh` | Mengganti URL dasar API tanpa garis miring di akhir. |
| `TOKENLAB_API_KEY` | Tidak diatur | Diperlukan untuk panggilan inferensi, media, file, tugas, embedding, rerank, dan terjemahan berkredensial. |
| `TOKENLAB_MCP_TOOL_PROFILE` | `core` | Pilih `catalog`, `core`, atau `full`. |
| `TOKENLAB_MCP_SCHEMA_MODE` | Default profil | Pilih `portable`, `exact`, atau `strict`; biasanya pertahankan `portable`. |
| `TOKENLAB_REQUEST_TIMEOUT_MS` | `120000` | Batas waktu permintaan API dalam milidetik. |
| `TOKENLAB_MCP_MAX_FILE_BYTES` | `104857600` | Ukuran maksimum setiap file lokal yang diunggah dalam byte. |
| `TOKENLAB_MCP_INLINE_BYTES` | `2097152` | Ukuran maksimum hasil biner atau JSON yang dikembalikan secara inline. |
| `TOKENLAB_ARTIFACT_DIR` | Direktori sementara sistem operasi | Lokasi penulisan artefak respons yang lebih besar. |

<Warning>
Jangan publikasikan tangkapan layar, log, riwayat shell, atau file konfigurasi yang berisi kunci API asli. Utamakan masukan rahasia atau variabel lingkungan klien, gunakan placeholder pada contoh bersama, dan rotasi kunci yang terpapar.
</Warning>

## Instalasi dari sumber

Gunakan checkout sumber hanya untuk mengaudit atau mengembangkan server MCP. Konfigurasi klien biasa harus memakai paket npm terpublikasi yang ditampilkan di atas.

```bash
git clone https://github.com/hedging8563/tokenlab-mcp-server.git
cd tokenlab-mcp-server
npm install
npm test
npm start
```

Profil `core` dan `full` secara default menerbitkan schema `portable` dengan batas ukuran dan kedalaman, sementara server memvalidasi setiap panggilan memakai definisi OpenAPI lengkap. Gunakan `exact` untuk schema bertingkat lengkap atau `strict` jika penyedia model mewajibkan objek tertutup dan semua properti dalam `required`.

## Profil Alat yang Dihasilkan

| Profil | Alat yang diekspos | Cakupan |
|---|---:|---|
| `catalog` | 6 | Empat alat katalog dan harga publik yang dihasilkan, ditambah `compare_models` dan `get_api_overview`. Tidak memerlukan kunci API. |
| `core` (default) | 31 | 29 alat endpoint yang dihasilkan ditambah `compare_models` dan `get_api_overview`. Mencakup katalog, harga, endpoint LLM native, gambar, video, musik, 3D, audio, tugas, file, embedding, rerank, dan terjemahan. |
| `full` | 80 | 78 alat endpoint yang dihasilkan ditambah dua alat penemuan komposit. Menambahkan siklus hidup respons, batch, dunia, penemuan model native, dan operasi pengembang lain yang diizinkan. |

Nama alat, deskripsi, skema input, metode HTTP, path, tipe konten, persyaratan autentikasi, dan perilaku tugas dihasilkan dari snapshot OpenAPI yang tersimpan. Overlay khusus MCP menyediakan alias alat yang stabil, keanggotaan profil, semantik tugas asinkron, varian multipart, penghilangan rahasia, dan batasan non-streaming.

Versi 0.6.0 juga mengembalikan JSON melalui `structuredContent` MCP, menerbitkan tiga sumber daya kontrak, dan menyediakan dua templat prompt untuk pemilihan model serta penyusunan permintaan native. Alat yang dihasilkan dan komposit menyertakan anotasi risiko standar agar klien yang kompatibel dapat menerapkan kebijakan persetujuan yang lebih aman.

Alat `core` yang umum meliputi:

| Keluarga | Alat representatif |
|---|---|
| Katalog dan harga | `list_models`, `get_model`, `get_model_pricing`, `get_pricing`, `compare_models` |
| Endpoint LLM native | `create_chat_completion`, `create_response`, `create_anthropic_message`, `create_gemini_content` |
| Gambar | `create_image`, `create_image_file`, `edit_image`, `edit_image_file` |
| Video, musik, dan 3D | `create_video`, `create_music`, `create_3d_model` |
| Audio | `create_speech`, `transcribe_audio`, `translate_audio` |
| Embedding dan teks | `create_embedding`, `create_multimodal_embedding`, `rerank_documents`, `translate_text` |
| File dan tugas | `upload_file`, `list_files`, `retrieve_file`, `retrieve_file_content`, `delete_file`, `get_task_status`, `cancel_task` |

Panggilan alat MCP mengembalikan satu hasil akhir. Operasi khusus streaming dan real-time dikecualikan; server menetapkan argumen `stream` yang didukung ke `false` secara internal tanpa mengekspos konstanta boolean ke adaptor provider. Respons gambar dan audio kecil mengembalikan konten MCP native; respons biner yang lebih besar ditulis ke `TOKENLAB_ARTIFACT_DIR` dan dikembalikan dengan path lokal, tipe MIME, dan jumlah byte-nya.

## Hasil Media Asinkron

Pembuatan video, musik, dan 3D mengembalikan ringkasan tugas asinkron. Pembuatan dan pengeditan gambar dapat mengembalikan hasil yang selesai atau tugas asinkron, tergantung pada model dan permintaan yang dipilih.

Saat `delivery.mode` adalah `async`, panggil `get_task_status` dengan `{ "id": delivery.task_id }` sampai `delivery.terminal` bernilai `true`. Tentukan penyelesaian dari `status`; `progress` bersifat opsional dan tidak boleh digunakan sebagai kondisi terminal.

## Alur Kerja Agen yang Direkomendasikan

1. Panggil `list_models` saat pengguna belum menentukan model.
2. Gunakan `recommended_for` untuk tugas non-chat, seperti gambar, video, musik, 3D, TTS, STT, embedding, rerank, atau terjemahan.
3. Panggil `get_model` sebelum membuat permintaan non-chat, mencoba ulang permintaan yang gagal, atau beralih keluarga endpoint.
4. Panggil `get_model_pricing` saat biaya model memengaruhi pilihan pengguna.
5. Gunakan `compare_models` saat pilihan bergantung pada beberapa kandidat.
6. Panggil alat yang dihasilkan yang sesuai dengan kontrak API yang diperlukan, atau gunakan `get_api_overview` untuk peta ringkas keluarga API TokenLab.

## Panduan Endpoint Native

TokenLab mendukung rute yang kompatibel dengan OpenAI dan keluarga endpoint native. Server MCP membantu agen memilih rute yang tepat sebelum menulis kode:

| Keluarga | Rute umum |
|---|---|
| Chat kompatibel OpenAI | `/v1/chat/completions` |
| Respons | `/v1/responses` |
| Anthropic Messages | `/v1/messages` |
| Gemini native | `/v1beta/models/{model}:generateContent` |
| Gambar | `/v1/images/generations`, `/v1/images/edits` |
| Video | `/v1/videos/generations` |
| Musik | `/v1/music/generations` |
| 3D | `/v1/3d/generations` |
| Audio | `/v1/audio/speech`, `/v1/audio/transcriptions`, `/v1/audio/translations` |
| Embedding dan rerank | `/v1/embeddings`, `/v1/rerank` |
| Terjemahan teks | `/v1/translations` |

## Penjelajah model terkelola

Klien yang mendukung HTTP Streamable dapat terhubung ke:

```text
https://tokenlab-model-explorer.vercel.app/mcp
```

Explorer yang di-host bersifat publik dan mengekspos `open_tokenlab_model_explorer`, `compare_tokenlab_models`, dan `generate_tokenlab_endpoint_example`. Gunakan server npm lokal di atas saat Anda memerlukan profil core/full yang dihasilkan, unggahan file lokal, atau operasi API dengan kredensial.

## Gunakan dengan TokenLab Skills

Server MCP berguna saat runtime, sementara [repositori TokenLab skills](https://github.com/hedging8563/tokenlab-skills) mengajarkan agen pengodean cara membuat dan memperbaiki kode integrasi.

Gunakan keduanya jika tersedia:

- Server MCP: temukan model, harga, dan detail endpoint terkini.
- Skill `tokenlab-api-integration`: buat contoh API yang dapat dijalankan dan tangani kesalahan TokenLab terstruktur.
- Skill `tokenlab-model-picker`: pilih model yang kuat untuk tugas pengguna.
- Skill `tokenlab-native-endpoints`: putuskan kapan harus menggunakan rute Responses, Anthropic Messages, Gemini, media, audio, embedding, rerank, atau terjemahan.

## Pemecahan Masalah

<AccordionGroup>
  <Accordion title="Klien tidak dapat memulai server">
    Pastikan Node.js minimal versi 18.17 dan klien dapat menemukan `npx`. Jalankan `npx -y @tokenlabai/mcp-server@0.6.19` di terminal untuk menampilkan kesalahan npm atau jaringan. Jika stdio berhasil dimulai, proses akan menunggu pesan MCP tanpa keluaran; tekan **Ctrl+C** setelah memeriksa.
  </Accordion>

  <Accordion title="Server sudah dikonfigurasi tetapi alat tidak muncul">
    Mulai ulang klien dan setujui server lokal. Di Claude Code, periksa `claude mcp list` atau `/mcp`; di VS Code, gunakan **MCP: List Servers** lalu **Show Output**; di Cursor, buka **Settings → Tools & MCP**.
  </Accordion>

  <Accordion title="Claude Code menampilkan Pending approval">
    Server `.mcp.json` tingkat proyek memerlukan kepercayaan workspace dan persetujuan eksplisit. Mulai Claude Code secara interaktif di proyek, terima kepercayaan, lalu setujui `tokenlab` di `/mcp`. Gunakan `--scope user` untuk konfigurasi pribadi yang tersedia di semua proyek.
  </Accordion>

  <Accordion title="Daftar model kosong">
    Verifikasi bahwa mesin dapat menjangkau `https://api.tokenlab.sh/v1/models`. Jika Anda mengganti `TOKENLAB_API_BASE`, pastikan tidak menyertakan garis miring di akhir.
  </Accordion>

  <Accordion title="Agen masih memilih ID model yang kedaluwarsa">
    Minta agen untuk memanggil `list_models` atau `get_model` sebelum melakukan hardcode nama model. Memasangkan server MCP dengan `tokenlab-model-picker` memberikan hasil yang lebih baik.
  </Accordion>

  <Accordion title="Bisakah server ini memanggil API inferensi TokenLab berbayar?">
    Ya. Atur `TOKENLAB_API_KEY`, lalu gunakan alat LLM, media, audio, tugas, file, embedding, rerank, atau terjemahan yang dihasilkan. Alat katalog dan harga tetap tersedia tanpa kunci.
  </Accordion>

  <Accordion title="Alat berkredensial mengembalikan kesalahan autentikasi">
    Atur `TOKENLAB_API_KEY` di lingkungan server MCP, bukan di argumen alat atau prompt, lalu mulai ulang klien. Rotasi kunci yang pernah muncul dalam file bersama, gambar, log, atau riwayat shell.
  </Accordion>

  <Accordion title="Klien menolak schema atau memuat terlalu banyak alat">
    Gunakan `TOKENLAB_MCP_TOOL_PROFILE=catalog` untuk 6 alat penemuan atau profil `core` default untuk 31 alat. Biasanya pertahankan `portable`; gunakan `exact` untuk struktur bertingkat lengkap dan `strict` untuk objek tertutup dengan semua properti dalam `required`.
  </Accordion>

  <Accordion title="Bagaimana cara mengunci atau memperbarui versi paket?">
    Gunakan `@tokenlabai/mcp-server@0.6.19` untuk mengunci rilis yang saat ini diverifikasi. Untuk mengganti versi, perbarui argumen paket, mulai ulang server, dan tinjau repositori publik sebelum memakai rilis yang lebih baru.
  </Accordion>
</AccordionGroup>

## Sumber Daya

<CardGroup cols={2}>
  <Card title="TokenLab MCP" icon="plug" href="https://tokenlab.sh/mcp">
    Ringkasan produk dan opsi koneksi untuk set alat Core 31 dan Full 80
  </Card>
  <Card title="Repositori GitHub" icon="github" href="https://github.com/hedging8563/tokenlab-mcp-server">
    Kode sumber dan instruksi pengaturan lokal
  </Card>
  <Card title="TokenLab Skills" icon="sparkles" href="https://github.com/hedging8563/tokenlab-skills">
    Skill agen yang dikelola untuk integrasi TokenLab
  </Card>
  <Card title="API Katalog Model" icon="list" href="https://api.tokenlab.sh/v1/models">
    Endpoint penemuan model publik
  </Card>
  <Card title="llms.txt" icon="file-lines" href="https://api.tokenlab.sh/llms.txt">
    Ringkasan API TokenLab yang dapat dibaca agen
  </Card>
  <Card title="Server MCP Glama" icon="globe" href="https://glama.ai/mcp/servers/hedging8563/tokenlab-mcp-server">
    Lihat daftar Server MCP TokenLab yang terverifikasi
  </Card>
  <Card title="Model Explorer Glama" icon="search" href="https://glama.ai/mcp/servers/hedging8563/tokenlab-openai-apps-model-explorer">
    Jelajahi model, harga, dan contoh endpoint native
  </Card>
  <Card title="Daftar MCP.so" icon="compass" href="https://mcp.so/servers/tokenlab-mcp-server">
    Temukan TokenLab di direktori AI & Agen
  </Card>
  <Card title="Model Explorer yang Di-host" icon="rocket" href="https://tokenlab-model-explorer.vercel.app/widget">
    Buka explorer model publik dan endpoint MCP jarak jauh
  </Card>
  <Card title="Panduan MCP Claude Code" icon="terminal" href="https://code.claude.com/docs/en/mcp">
    Cakupan, persetujuan, pemeriksaan status, dan konfigurasi MCP
  </Card>
  <Card title="Panduan MCP Cursor" icon="arrow-pointer" href="https://docs.cursor.com/context/model-context-protocol">
    Lokasi konfigurasi, kontrol alat, dan panduan keamanan
  </Card>
  <Card title="Panduan MCP VS Code" icon="code" href="https://code.visualstudio.com/docs/agent-customization/mcp-servers">
    Konfigurasi pengguna dan workspace, kepercayaan, log, dan pengelolaan server
  </Card>
</CardGroup>
