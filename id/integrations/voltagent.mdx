---
title: "VoltAgent"
description: "Gunakan model TokenLab di agen VoltAgent"
---

## Gambaran Umum

VoltAgent menerima model bahasa AI SDK secara langsung. Konfigurasikan penyedia AI SDK yang kompatibel dengan OpenAI menggunakan URL dasar `/v1` dari TokenLab dan masukkan model yang dipilih ke dalam agen Anda.

<Note>
**Tipe**: Kerangka kerja agen TypeScript

**Jalur Utama**: Chat Completions yang kompatibel dengan OpenAI melalui AI SDK

**Tingkat Dukungan**: Jalur yang kompatibel dengan OpenAI didukung
</Note>

## Instalasi

```bash
npm install @voltagent/core@2 ai@6 @ai-sdk/openai@3 zod
```

## Lingkungan

```bash
export TOKENLAB_API_KEY="sk-your-tokenlab-key"
```

## Penyedia AI SDK Langsung

```ts
import { Agent } from "@voltagent/core";
import { createOpenAI } from "@ai-sdk/openai";

const tokenlab = createOpenAI({
  apiKey: process.env.TOKENLAB_API_KEY,
  baseURL: "https://api.tokenlab.sh/v1",
});

const agent = new Agent({
  name: "tokenlab-agent",
  instructions: "Answer concisely.",
  model: tokenlab.chat("claude-sonnet-5"),
});
```

## Jalur Model Router

Jika snapshot registri VoltAgent Anda menyertakan entri penyedia TokenLab dari `models.dev`, konfigurasikan:

```bash
export TOKENLAB_API_KEY="sk-your-tokenlab-key"
export TOKENLAB_BASE_URL="https://api.tokenlab.sh/v1"
```

Kemudian gunakan string model registri, contohnya `tokenlab/claude-sonnet-5`.

## Catatan Endpoint

Jalur AI SDK langsung adalah pengaturan yang paling eksplisit. Gunakan respons asli TokenLab, Anthropic Messages, atau endpoint Gemini hanya jika penyedia/klien AI SDK yang Anda pilih dikonfigurasi untuk protokol tersebut.