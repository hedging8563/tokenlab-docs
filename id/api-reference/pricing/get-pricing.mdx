---
title: "Lihat Harga"
openapi: "/openapi/id.json GET /v1/pricing"
description: "Ambil informasi harga untuk model"
---

Endpoint publik ini tidak memerlukan kunci API.

## Ikhtisar

Gunakan endpoint ini untuk menampilkan permukaan harga publik saat ini untuk model yang aktif.

Untuk detail harga lengkap satu model, termasuk harga terstruktur dan provenance, utamakan [Dapatkan Harga Model](/id/api-reference/models/get-model).

## Parameter Query

<ParamField query="provider" type="string">
  Filter provider opsional seperti `openai`, `anthropic`, atau `google`.
</ParamField>

<ParamField query="tag" type="string">
  Filter tag model opsional seperti `chat`, `image`, `video`, `embedding`, atau `translation`.
</ParamField>

## Respons

<ResponseField name="object" type="string">
  Selalu `pricing`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  Timestamp ISO saat harga publik saat ini diterbitkan.
</ResponseField>

<ResponseField name="currency" type="string">
  Selalu `USD`.
</ResponseField>

<ResponseField name="data" type="array">
  Array entri harga. Setiap item mencakup `model`, `provider`, `is_lock_price`, `capabilities`, `aliases`, dan field ringkasan harga publik berbentuk scalar atau terstruktur yang dikembalikan skema interaktif di atas.
</ResponseField>

<RequestExample>
```bash cURL
curl "https://api.tokenlab.sh/v1/pricing?provider=openai&tag=chat"
```

```python Python
import requests

response = requests.get(
    "https://api.tokenlab.sh/v1/pricing",
    params={"provider": "openai", "tag": "chat"}
)

print(response.json())
```

```javascript JavaScript
const response = await fetch(
  'https://api.tokenlab.sh/v1/pricing?provider=openai&tag=chat'
);

const data = await response.json();
console.log(data);
```

```go Go
package main

import (
    "encoding/json"
    "fmt"
    "net/http"
)

func main() {
    req, _ := http.NewRequest("GET", "https://api.tokenlab.sh/v1/pricing?provider=openai&tag=chat", nil)

    client := &http.Client{}
    resp, _ := client.Do(req)
    defer resp.Body.Close()

    var result map[string]interface{}
    json.NewDecoder(resp.Body).Decode(&result)
    fmt.Println(result)
}
```

```php PHP
<?php
$ch = curl_init('https://api.tokenlab.sh/v1/pricing?provider=openai&tag=chat');

curl_setopt_array($ch, [
    CURLOPT_RETURNTRANSFER => true
]);

$response = curl_exec($ch);
curl_close($ch);

$data = json_decode($response, true);
print_r($data);
```
</RequestExample>

<ResponseExample>
```json Response
{
  "object": "pricing",
  "updated_at": "2026-04-20T00:00:00.000Z",
  "currency": "USD",
  "data": [
    {
      "model": "gpt-4o",
      "provider": "openai",
      "is_lock_price": false,
      "input_per_1m_tokens": "2.50",
      "output_per_1m_tokens": "10.00",
      "capabilities": ["chat"],
      "aliases": ["gpt-4o-latest"]
    }
  ]
}
```
</ResponseExample>

<Note>
Gunakan panel OpenAPI interaktif di atas untuk skema respons yang tepat. Entri harga publik juga dapat menampilkan `lock_price_per_request`, `has_complex_pricing`, `supported_operations`, `pricing_summary`, dan `pricing_provenance`.
</Note>
