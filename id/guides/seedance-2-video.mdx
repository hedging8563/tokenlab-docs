---
title: "Model Video Seedance 2.0"
description: "Pilih model Seedance 2.0 dan jalur integrasi TokenLab yang tepat."
---

Mulai dari sini saat menambahkan Seedance 2.0 ke aplikasi. Halaman ini hanya membahas pemilihan model dan alur; migrasi dari Volcengine, material yang dapat digunakan ulang, dan verifikasi orang asli memiliki panduan terpisah.

## Pilih jalur integrasi

| Tujuan | Gunakan | Lanjutkan ke |
| --- | --- | --- |
| Membangun integrasi video TokenLab baru | API terpadu `POST /v1/videos/generations` | [Panduan pembuatan video](/id/guides/video-generation) |
| Memigrasikan klien Seedance Volcengine | API REST atau Action yang kompatibel dengan Volcengine | [Migrasi Seedance dari Volcengine](/id/guides/seedance-volcengine-migration) |
| Menggunakan ulang avatar, produk, gaya, atau orang asli | Pustaka material TokenLab | [Material Seedance dan verifikasi orang asli](/id/guides/seedance-materials) |

Ketiga jalur memakai `Authorization: Bearer <TOKENLAB_API_KEY>`. Jalur kompatibel Volcengine mempertahankan bentuk request, tetapi tidak menerima tanda tangan AK/SK.

## Pilihan Model

| Model | Resolusi yang didukung |
|-------|-----------------------|
| `seedance-2.0` | `480p`, `720p`, `1080p`, `4k` |
| `seedance-2.0-fast` | `480p`, `720p` |
| `seedance-2.0-mini` | `480p`, `720p` |

`seedance-2.0-fast` dan `seedance-2.0-mini` menolak permintaan `1080p` dan `4k`. Audio hanya dapat digunakan dalam alur kerja referensi yang didukung; permintaan Seedance yang murni hanya audio atau hanya teks-plus-audio tidak didukung.

## Operasi yang Didukung

| Operasi | Input tipikal |
|-----------|----------------|
| `text-to-video` | `prompt` |
| `image-to-video` | `prompt`, `image_url` atau input gambar yang kompatibel |
| `start-end-to-video` | `prompt`, `start_image`, `end_image` |
| `reference-to-video` | `prompt`, `reference_images`, opsional `video_urls`, opsional `audio_urls` |
| `video-to-video` | `prompt`, `video_url` atau input video yang kompatibel |
| `video-extension` | `task_id` atau input kelanjutan khusus model |

## Harga dan Hasil Asinkron

Harga Seedance 2.0 bergantung pada resolusi output, tipe input, dan model. Gunakan [Pricing API](/id/api-reference/pricing/get-pricing) atau [Models API](/id/api-reference/models/list-models) sebelum melakukan hard-coding pada UI produk.

Pembuatan video bersifat asinkron. Ikuti `poll_url` yang dikembalikan oleh permintaan pembuatan, atau gunakan [Dapatkan Status Task](/id/api-reference/tasks/get-task-status) dengan ID tugas yang dikembalikan.

## API terkait

| API | Lanjutkan ke |
|-----|---------------|
| `POST /v1/videos/generations` | [Buat video](/id/api-reference/video/create-video) |
| `GET /v1/tasks/{id}` | [Ambil status task](/id/api-reference/tasks/get-task-status) |
| Kompatibilitas task Seedance | [Buat task kompatibel Volcengine](/id/api-reference/video/create-volc-compatible-seedance-task) |
| Kompatibilitas Action material | [Action material kompatibel Volcengine](/id/api-reference/video/volc-compatible-material-actions) |
