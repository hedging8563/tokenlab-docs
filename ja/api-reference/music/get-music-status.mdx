---
title: "音楽生成ステータスの取得"
openapi: "GET /v1/tasks/{id}"
description: "音楽生成タスクのステータスと結果を取得します"
---

このエンドポイントをポーリングして、音楽生成タスクのステータスを確認します。

<Info>
作成レスポンスに `poll_url` が含まれている場合は、ポーリングにはその URL を優先して使ってください。`id` と `task_id` は同じ非同期タスク ID として扱います。


期限切れまたは利用できない公開タスクでは `404 async_task_not_found` が返る場合があります。現在の API キーでタスクの所有権を確認できない場合は `403 task_not_owned` が返ります。
</Info>

## パスパラメータ

<ParamField path="id" type="string" required>
  音楽生成タスクのID。
</ParamField>

## レスポンス

<ResponseField name="id" type="string">
  タスクID。
</ResponseField>

<ResponseField name="task_id" type="string">
  同じ非同期タスク ID の別名。返される場合のみ存在します。
</ResponseField>

<ResponseField name="poll_url" type="string">
  作成レスポンスで提供された場合に優先して使うポーリング URL。
</ResponseField>

<ResponseField name="status" type="string">
  タスクのステータス: `pending`、`processing`、`completed`、または `failed`。
</ResponseField>

<ResponseField name="progress" type="integer">
  任意の進捗値です。実際の進捗値が利用できる場合のみ返されます。完了判定には `status` を使用してください。
</ResponseField>

結果のフィールドは操作によって異なります。音楽は `audio_url` と `audio_id`、歌詞専用タスクは `lyrics`、音源分離は `data` 内に複数ファイルを返す場合があります。`video_url`、タイトル、進捗などは任意です。完了は `status` で確認し、実際に返されたフィールドを読んでください。

<ResponseField name="audio_id" type="string">
  後続の対応操作で、このタスク ID とともに使用できる任意の音声 ID。
</ResponseField>

<ResponseField name="data" type="array">
  任意の出力配列。分離した音声ファイルの `audio_url` または `url` を含みます。
</ResponseField>

<ResponseField name="audio_url" type="string">
  オーディオファイルをダウンロードするためのURL（完了時）。
</ResponseField>

<ResponseField name="video_url" type="string">
  ビジュアライザー付きビデオバージョンのURL（完了時）。
</ResponseField>

<ResponseField name="title" type="string">
  生成された曲のタイトル。
</ResponseField>

<ResponseField name="lyrics" type="string">
  生成された、または提供された歌詞。
</ResponseField>

<ResponseField name="error" type="string | object">
  エラーメッセージ（失敗時）。
</ResponseField>

<RequestExample>
```bash cURL
curl --fail-with-body "https://api.tokenlab.sh/v1/tasks/ldtask_aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa" \
  -H "Authorization: Bearer sk-your-api-key"
```

```python Python
import requests
import time

task_id = "ldtask_aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"

while True:
    response = requests.get(
        f"https://api.tokenlab.sh/v1/tasks/{task_id}",
        headers={"Authorization": "Bearer sk-your-api-key"}
    )
    response.raise_for_status()
    result = response.json()

    if result["status"] == "completed":
        print(result)
        break
    elif result["status"] == "failed":
        print(result.get("error", "Task failed"))
        break

    time.sleep(5)  # Poll every 5 seconds
```

```javascript JavaScript
const taskId = 'ldtask_aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa';

async function pollForMusic() {
  while (true) {
    const response = await fetch(
      `https://api.tokenlab.sh/v1/tasks/${taskId}`,
      { headers: { 'Authorization': 'Bearer sk-your-api-key' } }
    );

    if (!response.ok) throw new Error(await response.text());
    const result = await response.json();

    if (result.status === 'completed') {
      console.log(result);
      return result;
    } else if (result.status === 'failed') {
      throw new Error(typeof result.error === 'string'
        ? result.error
        : JSON.stringify(result.error ?? result));
    }

    await new Promise(r => setTimeout(r, 5000));
  }
}

await pollForMusic();
```

```go Go
package main

import (
    "encoding/json"
    "fmt"
    "io"
    "log"
    "net/http"
    "time"
)

func main() {
    taskID := "ldtask_aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"

    for {
        req, err := http.NewRequest("GET",
            fmt.Sprintf("https://api.tokenlab.sh/v1/tasks/%s", taskID), nil)
        if err != nil {
            log.Fatal(err)
        }
        req.Header.Set("Authorization", "Bearer sk-your-api-key")

        client := &http.Client{}
        resp, err := client.Do(req)
        if err != nil {
            log.Fatal(err)
        }
        if resp.StatusCode < 200 || resp.StatusCode >= 300 {
            body, _ := io.ReadAll(resp.Body)
            resp.Body.Close()
            log.Fatalf("HTTP %d: %s", resp.StatusCode, body)
        }

        var result map[string]interface{}
        err = json.NewDecoder(resp.Body).Decode(&result)
        resp.Body.Close()
        if err != nil {
            log.Fatal(err)
        }

        if result["status"] == "completed" {
            fmt.Println(result)
            break
        } else if result["status"] == "failed" {
            fmt.Println(result)
            break
        }

        time.Sleep(5 * time.Second)
    }
}
```

```php PHP
<?php
$taskId = 'ldtask_aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa';

while (true) {
    $ch = curl_init("https://api.tokenlab.sh/v1/tasks/{$taskId}");

    curl_setopt_array($ch, [
        CURLOPT_RETURNTRANSFER => true,
        CURLOPT_HTTPHEADER => [
            'Authorization: Bearer sk-your-api-key'
        ]
    ]);

    $response = curl_exec($ch);
    $httpStatus = curl_getinfo($ch, CURLINFO_HTTP_CODE);
    $curlError = curl_error($ch);
    curl_close($ch);

    if ($response === false) {
        throw new RuntimeException($curlError);
    }
    if ($httpStatus < 200 || $httpStatus >= 300) {
        throw new RuntimeException("HTTP {$httpStatus}: {$response}");
    }

    $result = json_decode($response, true, 512, JSON_THROW_ON_ERROR);

    if ($result['status'] === 'completed') {
        echo json_encode($result, JSON_UNESCAPED_SLASHES) . "\n";
        break;
    } elseif ($result['status'] === 'failed') {
        echo json_encode($result, JSON_UNESCAPED_SLASHES) . "\n";
        break;
    }

    sleep(5);
}
```
</RequestExample>

<ResponseExample>
```json Response (Completed)
{
  "id": "ldtask_aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa",
  "task_id": "ldtask_aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa",
  "poll_url": "/v1/tasks/ldtask_aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa",
  "status": "completed",
  "audio_url": "https://cdn.example.com/music/abc123.mp3",
  "title": "Night Drive",
  "lyrics": "[Verse 1]\nDriving through the night...",
  "audio_id": "audio_abc123"
}
```
</ResponseExample>
