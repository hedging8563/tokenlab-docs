---
title: "Opik"
description: "Opikを使用してTokenLabのOpenAI互換リクエストをトレースする"
---

## 概要

Opikは、TokenLabのOpenAI互換エンドポイントを指すOpenAIクライアントをラップすることで、TokenLabの呼び出しをログに記録できます。

## インストール

```bash
pip install opik openai
```

## 環境設定

```bash
export TOKENLAB_API_KEY="sk-your-tokenlab-key"
export OPIK_PROJECT_NAME="tokenlab-integration-demo"
```

## OpenAIクライアントのラップ

この例を実行する前に Opik を設定してください。Opik Cloud では `OPIK_API_KEY` が必要で、必要に応じて `OPIK_WORKSPACE` も設定します。既存のセルフホスト環境では、`OPIK_URL_OVERRIDE` に API URL（例：`http://localhost:5173/api`）を指定します。[Opik 設定ガイド](https://www.comet.com/docs/opik/tracing/advanced/sdk_configuration)を参照してください。

```python
import os

from openai import OpenAI
from opik.integrations.openai import track_openai

client = OpenAI(
    api_key=os.environ["TOKENLAB_API_KEY"],
    base_url="https://api.tokenlab.sh/v1",
)

openai_client = track_openai(client)
```

## チャットコンプリーションのログ記録

```python
completion = openai_client.chat.completions.create(
    model="claude-sonnet-5",
    messages=[
        {"role": "user", "content": "Summarize why LLM observability matters."}
    ],
)

print(completion.choices[0].message.content)
```

## エンドポイントに関する注意点

この設定はOpenAI互換のSDKパスをトレースします。OpenAI SDK以外のTokenLabネイティブエンドポイントを使用する場合は、そのクライアントまたはワークフローを個別にインストルメントしてください。