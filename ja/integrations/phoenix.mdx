---
title: "Phoenix"
description: "Arize Phoenixを使用してTokenLabのOpenAI互換リクエストをトレースする"
---

## 概要

Phoenixは、TokenLabのOpenAI互換ベースURLで構成されたOpenAI Pythonクライアントを自動計測することで、TokenLabのリクエストをトレースできます。

<Note>
**タイプ**: 可観測性 (Observability)

**主要パス**: OpenAI SDK計測

**サポート信頼度**: サポートされているOpenAI互換トレースパス
</Note>

## インストール

```bash
pip install arize-phoenix openinference-instrumentation-openai openai
```

## 環境設定

```bash
export TOKENLAB_API_KEY="sk-your-tokenlab-key"
```

## Phoenixの登録

```python
from phoenix.otel import register

tracer_provider = register(
    project_name="tokenlab-app",
    auto_instrument=True,
)
```

## TokenLabの呼び出し

```python
import os

from openai import OpenAI

client = OpenAI(
    api_key=os.environ["TOKENLAB_API_KEY"],
    base_url="https://api.tokenlab.sh/v1",
)

response = client.chat.completions.create(
    model="claude-sonnet-5",
    messages=[{"role": "user", "content": "Write a short haiku."}],
)

print(response.choices[0].message.content)
```

## エンドポイントに関する注意点

PhoenixはOpenAI SDKの呼び出しパスをトレースします。Chat Completions形式のTokenLab呼び出しにはこのページを参照してください。その他のTokenLabネイティブエンドポイントについても、アプリケーションの計測機能がそれらのリクエストに使用するクライアントをキャプチャできれば、トレースが可能です。