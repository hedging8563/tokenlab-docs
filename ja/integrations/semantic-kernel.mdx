---
title: "Semantic Kernel"
description: "カスタムのOpenAI互換エンドポイントを使用して、Microsoft Semantic KernelをTokenLabに接続します"
---

## 概要

Semantic Kernelは、OpenAI互換のカスタムエンドポイントチャット補完サービスを通じてTokenLabを呼び出すことができます。

<Note>
**タイプ**: SDK / エージェントフレームワーク

**主要パス**: OpenAI互換チャット補完

**サポート信頼度**: サポートされているカスタムエンドポイントパス
</Note>

## 環境設定

```bash
export TOKENLAB_API_KEY="sk-your-tokenlab-key"
```

## .NET Kernel Builder

Semantic Kernelのカスタムエンドポイントは、`/v1/chat/completions` ではなく、OpenAI互換のルートエンドポイントである `/v1` を指定する必要があります。

```csharp
using Microsoft.SemanticKernel;

#pragma warning disable SKEXP0010

var builder = Kernel.CreateBuilder();

builder.AddOpenAIChatCompletion(
    modelId: "gpt-5.4-mini",
    endpoint: new Uri("https://api.tokenlab.sh/v1"),
    apiKey: Environment.GetEnvironmentVariable("TOKENLAB_API_KEY"));

var kernel = builder.Build();
```

## 基本的なプロンプト

```csharp
var result = await kernel.InvokePromptAsync(
    "Give me a concise checklist for evaluating an LLM gateway.");

Console.WriteLine(result);
```

## エンドポイントに関する注意点

以下を使用してください:

```text
https://api.tokenlab.sh/v1
```

以下は使用しないでください:

```text
https://api.tokenlab.sh/v1/chat/completions
```

Semantic Kernelは、このコネクタに対して内部的にチャット補完パスを付加します。