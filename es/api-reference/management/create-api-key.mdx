---
title: "Crear API Key"
openapi: "POST /v1/management/api-keys"
description: "Crea una nueva API Key de usuario para la organización actual."
# Synced from canonical OpenAPI x-mint.metadata.playground.
playground: simple
---

## Resumen

Este endpoint crea una nueva API Key de tipo `type=user` en la organización asociada al token de gestión actual.

## Cuerpo de la solicitud

| Campo | Tipo | Valor predeterminado / límites | Notas |
|-------|------|-------------------------------|-------|
| `name` | string | requerido, predeterminado `Default Key`, longitud `1-50` | Nombre visible, recortado en el servidor |
| `limitAmount` | number \| null | mínimo `0`, máximo de entrada `1000000` | `null` o omitido = ilimitado, `0` = cuota cero. Los valores positivos se normalizan a un límite almacenado que no puede superar `100000` USD equivalente. |
| `limitCurrency` | enum | predeterminado `USD` | Solo USD. Enviar `CNY` devuelve `400 currency_retired`. |
| `models` | string[] | predeterminado `[]` | Lista opcional de modelos lógicos permitidos |
| `expiresAt` | string \| null | datetime RFC3339 | `null` significa sin caducidad |

## Notas

- Los campos monetarios solo admiten USD. Las entradas `CNY` están retiradas y devuelven `400 currency_retired`.

- El valor completo de la API key solo se muestra una vez en la respuesta.
- `models: []` significa que la clave no tiene restricciones adicionales de modelo más allá de la política de la organización.
- `limitAmount: 0` crea una clave válida pero sin cuota utilizable hasta que se actualice.

## Ejemplo

<RequestExample>
```bash cURL
curl -X POST "https://api.tokenlab.sh/v1/management/api-keys" \
  -H "Authorization: Bearer mt-your-management-token" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Backend Worker",
    "limitAmount": 500,
    "models": ["veo3.1", "kling-3.0-video"],
    "expiresAt": "2026-12-31T23:59:59Z"
  }'
```
</RequestExample>

## Solicitud / Respuesta

Esta página ofrece el esquema OpenAPI y ejemplos de solicitudes que puedes copiar. Envía las solicitudes de gestión desde tu terminal o cliente con un Management Token; esta página no permite enviarlas directamente.

## Ejemplo de respuesta

<ResponseExample>
```json 201 Created
{
  "id": "key_abc123def456",
  "name": "Backend Worker",
  "key": "sk-live-redacted",
  "key_prefix": "sk-live...",
  "status": "active",
  "limit_amount": 500,
  "limit_amount_decimal": "500",
  "used_amount": 0,
  "used_amount_decimal": "0",
  "models": [
    "veo3.1",
    "kling-3.0-video"
  ],
  "expires_at": "2026-12-31T23:59:59.000Z",
  "last_used_at": null,
  "created_at": "2026-07-08T03:00:00.000Z"
}
```
</ResponseExample>

## Campos importantes

<ResponseField name="key" type="string">Valor secreto completo de la clave API. Solo se devuelve una vez al crearla.</ResponseField>
<ResponseField name="id" type="string">Identificador de clave API usado en llamadas posteriores a la API de gestión.</ResponseField>
<ResponseField name="name" type="string">Nombre para mostrar de la clave API.</ResponseField>
<ResponseField name="key_prefix" type="string">Prefijo de clave no secreto para visualización y soporte.</ResponseField>
<ResponseField name="status" type="string">Uno de los valores `active`, `inactive` o `revoked`.</ResponseField>
<ResponseField name="limit_amount" type="number | null">Límite de gasto en USD como número JSON para visualización. `null` significa sin límite.</ResponseField>
<ResponseField name="limit_amount_decimal" type="string | null">Límite de gasto exacto en USD como cadena decimal. `null` significa sin límite.</ResponseField>
<ResponseField name="used_amount" type="number">Gasto de uso acumulado en USD como número JSON para visualización.</ResponseField>
<ResponseField name="used_amount_decimal" type="string">Gasto de uso acumulado exacto en USD como cadena decimal.</ResponseField>
<ResponseField name="models" type="string[]">Lista de modelos permitidos por clave. Un array vacío significa que no hay una restricción adicional de modelos a nivel de clave.</ResponseField>
<ResponseField name="expires_at" type="string | null">Marca de tiempo ISO en la que vence la clave, o `null` si no caduca.</ResponseField>
<ResponseField name="last_used_at" type="string | null">Marca de tiempo ISO del uso más reciente, o `null` si nunca se ha usado.</ResponseField>
<ResponseField name="created_at" type="string">Marca de tiempo ISO de creación.</ResponseField>
