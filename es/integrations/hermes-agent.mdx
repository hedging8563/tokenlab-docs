---
title: "Hermes Agent"
description: "Conectar Hermes Agent con TokenLab"
---

Añade TokenLab como proveedor con nombre propio y conserva tus cuentas, ajustes y permisos de herramientas.

## Encargar esta tarea a mi agente

```text
Lee esta guía y conecta Hermes con TokenLab:
https://docs.tokenlab.sh/es/integrations/hermes-agent
Comprueba primero la versión instalada y la configuración activa.
Conserva cuentas, proveedores, modelo predeterminado y permisos; respalda los archivos y muestra los cambios.
Introduciré la clave localmente. No la pidas ni la muestres en el chat.
Comprueba primero la carga de configuración. Explica por separado las pruebas de pago y ejecútalas solo cuando yo las elija.
```

## Instalar o comprobar Hermes

La [guía oficial](https://hermes-agent.nousresearch.com/docs/getting-started/installation) ofrece instaladores de escritorio para macOS con Apple Silicon y Windows, y una CLI para Linux y Windows nativo. Revisa los requisitos; omite la instalación si ya tienes Hermes.

## Opcional: configuración reversible

TokenLab Skills **0.1.3** incluye un asistente para Hermes oficial **0.21.3**. Muestra y respalda los cambios y añade solo un proveedor con nombre. Conserva comentarios YAML, cuentas, modelo predeterminado y permisos. Lee primero la [guía](https://github.com/hedging8563/tokenlab-skills/blob/900de0c4e296c0d2ed00b5d842815ed89c403503/skills/tokenlab-api-integration/references/hermes_setup.md).

Define `SKILL_DIR` como la carpeta instalada `tokenlab-api-integration` y `HERMES_PYTHON` como el Python del directorio indicado por `hermes --version`: `venv/bin/python` en macOS/Linux o `venv\Scripts\python.exe` en Windows. En PowerShell usa `$SkillDir` y `$HermesPython`. Se reutiliza ese entorno sin instalar paquetes Python globales.

```bash
"$HERMES_PYTHON" "$SKILL_DIR/scripts/configure_hermes.py" --model gpt-5.6-luna
"$HERMES_PYTHON" "$SKILL_DIR/scripts/configure_hermes.py" --model gpt-5.6-luna --apply
```

```powershell
& $HermesPython "$SkillDir/scripts/configure_hermes.py" --model gpt-5.6-luna
& $HermesPython "$SkillDir/scripts/configure_hermes.py" --model gpt-5.6-luna --apply
```

Tras aplicar, omite la combinación manual siguiente y sigue el directorio y los comandos mostrados; se detecta `active_profile`. Leer la configuración no valida la clave. Previsualiza la restauración con `--restore` y añade `--apply` para recuperar los bytes originales. Los conflictos, cambios posteriores, ajustes administrados o WSL detienen la automatización; conserva la configuración y usa la vía manual.

## Configurar un proveedor

Usa el directorio `HERMES_HOME` si está definido; en caso contrario, `~/.hermes` (Windows: `%LOCALAPPDATA%\hermes`). Haz una copia de `config.yaml` y combina estas entradas sin sustituir los demás ajustes. Elige un modelo actual compatible con Chat Completions y llamadas a herramientas.

Haz una copia de la configuración activa. Añade solo la entrada TokenLab y conserva proveedores, cuentas, modelo predeterminado y permisos existentes. Si el nombre ya está usado, elige otro y ajusta los comandos. Para deshacerlo, elimina solo tu entrada o restaura su copia anterior.

```yaml
providers:
  tokenlab:
    api: https://api.tokenlab.sh/v1
    key_env: TOKENLAB_API_KEY
    transport: chat_completions
    default_model: gpt-5.6-luna
```

Crea una clave en [Claves API](https://tokenlab.sh/dashboard/api?tab=keys). Guárdala localmente como `TOKENLAB_API_KEY` en el `.env` de Hermes o en el entorno del proceso que lo inicia. No pegues la clave en conversaciones ni en el YAML del proveedor.

## Comprobar antes de enviar

Comprueba el proveedor con `hermes config get providers.tokenlab.api`; debe mostrar `https://api.tokenlab.sh/v1`. Conserva la sección `model` existente. Inicia una sesión nueva con el proveedor y modelo explícitos de abajo; sin esas opciones se utiliza tu selección predeterminada anterior. Esta inspección no valida la clave.

```bash
hermes chat --provider custom:tokenlab --model gpt-5.6-luna
```

Cuando quieras usar saldo de TokenLab, envía un texto corto y comprueba la respuesta y su [registro de solicitud](https://tokenlab.sh/dashboard/runs?section=requests). Después prueba una herramienta de solo lectura. Los modelos auxiliares y herramientas opcionales pueden utilizar otros proveedores o claves.

## Restaurar y resolver errores

Si el proveedor no se reconoce, revisa la versión y el [formato actual](https://hermes-agent.nousresearch.com/docs/integrations/providers). Para errores de autenticación, revisa el entorno y el estado de la clave sin mostrarla. Para deshacerlo, restaura el modelo anterior y elimina solo las entradas TokenLab añadidas.

MCP y Skills son extensiones separadas: la [guía MCP](/es/integrations/tokenlab-mcp-server) añade herramientas API y la [guía Skill](/es/integrations/coding-agent-skill) instrucciones de integración. No son necesarias para este proveedor.
