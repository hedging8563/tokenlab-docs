---
title: "Configuración del proveedor TokenLab"
description: "Configura TokenLab como un proveedor personalizado en OpenClaw usando rutas compatibles con OpenAI, Responses, Anthropic y Gemini"
---

## Dejar que mi agente lo configure

Copia esta tarea en un agente que ya funcione en tu ordenador:

```text
Lee esta guía y ayúdame a conectar OpenClaw con TokenLab:
https://docs.tokenlab.sh/es/guides/tokenlab-provider
Comprueba primero mi versión instalada y la configuración activa.
Conserva las cuentas, los proveedores, los permisos y los demás ajustes existentes.
Haz una copia de seguridad de los archivos locales y muestra los cambios propuestos.
Las claves API necesarias las introduciré localmente; nunca las pidas, muestres ni pegues en el chat.
Comprueba primero que la configuración se carga correctamente.
Explica por separado el coste de cualquier prueba con peticiones de pago antes de ejecutarla.
```

<Note>
Esta guía es para usuarios de **OpenClaw autohospedado** que desean conectar TokenLab como su proveedor de IA.
</Note>

## Recomendado: instala el plugin

Opcional: instala el complemento publicado **0.1.0**. Proporciona `TOKENLAB_API_KEY` en el entorno del Gateway descrito abajo y selecciona el modelo solo para la sesión. El asistente cambia el modelo predeterminado, por lo que esta guía no ejecuta `openclaw onboard`. Conserva complementos y permisos existentes; comprueba la carga y la lista de modelos antes de enviar una solicitud.

Revisa las capacidades solicitadas por el instalador y sigue sus confirmaciones. No concedas permisos ajenos a la instalación ni cambies los de otros plugins.

```bash
openclaw plugins install @tokenlabai/openclaw-provider@0.1.0
openclaw plugins list
openclaw models list --provider tokenlab
```

```text
/model tokenlab/claude-sonnet-5 -s
/model default -s
```

[npm](https://www.npmjs.com/package/@tokenlabai/openclaw-provider/v/0.1.0) · [GitHub](https://github.com/hedging8563/tokenlab-openclaw-provider)

## Configuración manual del proveedor

Usa las entradas manuales de `models.providers` que aparecen a continuación solo cuando necesites rutas independientes para **Responses API**, **Claude nativo**, **Gemini nativo** o **MiniMax nativo**.

Si eliges la configuración manual, configurar únicamente `tokenlab` es suficiente. Añade los otros providers solo cuando necesites explícitamente el comportamiento de **Responses API**, **Claude nativo**, **Gemini nativo** o **MiniMax nativo**.

| Proveedor | OpenClaw `api` | Ideal para | baseUrl |
|----------|----------------|------------|---------|
| `tokenlab` | `openai-completions` | GPT, DeepSeek, Qwen y la mayoría de las llamadas compatibles con OpenAI | `https://api.tokenlab.sh/v1` |
| `tokenlab-responses` | `openai-responses` | Flujos de trabajo de OpenAI Responses que esperan la semántica de `/v1/responses` | `https://api.tokenlab.sh/v1` |
| `tokenlab-claude` | `anthropic-messages` | API nativa de Claude Messages | `https://api.tokenlab.sh` |
| `tokenlab-gemini` | `google-generative-ai` | Formato nativo de la API de Gemini | `https://api.tokenlab.sh` |
| `tokenlab-minimax` | `anthropic-messages` | Enrutamiento nativo de MiniMax | `https://api.tokenlab.sh` |

<Warning>
Usa el sufijo `/v1` solo para `openai-completions` y `openai-responses`.

Los providers nativos como `anthropic-messages` y `google-generative-ai` deben usar `https://api.tokenlab.sh` sin `/v1`, de lo contrario OpenClaw puede construir una ruta upstream incorrecta.
</Warning>

## Requisitos previos

- Una instancia de OpenClaw autohospedada
- OpenClaw **2026.9.4** requiere Node.js `>=24.16.0 <25 || >=26.1.0` ([requisitos de versión](https://github.com/openclaw/openclaw/blob/v2026.9.4/package.json)). Para otras versiones de OpenClaw, consulta sus requisitos específicos.
- Una TokenLab API Key — [Get one here](https://tokenlab.sh/dashboard/api?tab=keys)

## Configuración

Edita tu configuración de OpenClaw:

- **Autohospedado**: `~/.openclaw/openclaw.json`

Agrega los providers de TokenLab bajo `models.providers`:

Usa el mismo perfil, `OPENCLAW_STATE_DIR` y `OPENCLAW_CONFIG_PATH` que el Gateway activo y copia la configuración. Guarda `TOKENLAB_API_KEY` en su `.env` global de confianza o entorno de servicio. El export del terminal o `.env` del proyecto puede no llegar al proceso de fondo. Consulta el [entorno](https://docs.openclaw.ai/help/environment).

Haz una copia de la configuración activa. Añade solo la entrada TokenLab y conserva proveedores, cuentas, modelo predeterminado y permisos existentes. Si el nombre ya está usado, elige otro y ajusta los comandos. Para deshacerlo, elimina solo tu entrada o restaura su copia anterior.

```json5
{
  models: {
    mode: "merge",
    providers: {
      tokenlab: {
        api: "openai-completions",
        baseUrl: "https://api.tokenlab.sh/v1",
        apiKey: "${TOKENLAB_API_KEY}",
        models: [
          { id: "gpt-5.6-terra", name: "GPT-5.6 Terra" },
          { id: "deepseek-reasoner", name: "DeepSeek Reasoner" },
          { id: "qwen3-coder-flash", name: "Qwen 3 Coder Flash" }
        ]
      },
      "tokenlab-responses": {
        api: "openai-responses",
        baseUrl: "https://api.tokenlab.sh/v1",
        apiKey: "${TOKENLAB_API_KEY}",
        models: [
          { id: "gpt-5.6-terra", name: "GPT-5.6 Terra (Responses)" },
          { id: "gpt-5.2", name: "GPT-5.2 (Responses)" }
        ]
      },
      "tokenlab-claude": {
        api: "anthropic-messages",
        baseUrl: "https://api.tokenlab.sh",
        apiKey: "${TOKENLAB_API_KEY}",
        models: [
          { id: "claude-sonnet-5", name: "Claude Sonnet 5" },
          { id: "claude-opus-5", name: "Claude Opus 5" }
        ]
      },
      "tokenlab-gemini": {
        api: "google-generative-ai",
        baseUrl: "https://api.tokenlab.sh",
        apiKey: "${TOKENLAB_API_KEY}",
        models: [
          { id: "gemini-3.5-flash", name: "Gemini 3.5 Flash" },
          { id: "gemini-2.5-pro", name: "Gemini 2.5 Pro" }
        ]
      },
      "tokenlab-minimax": {
        api: "anthropic-messages",
        baseUrl: "https://api.tokenlab.sh",
        apiKey: "${TOKENLAB_API_KEY}",
        models: [
          { id: "minimax-m3", name: "MiniMax M3" }
        ]
      }
    }
  }
}
```

<Info>
Los 5 providers usan la **misma API Key**. Solo necesitas una cuenta de TokenLab.
</Info>

<Info>
Los arrays `models` de arriba solo muestran ejemplos comunes. Añade más IDs de modelos a cada provider según sea necesario.
</Info>

## Uso de modelos

OpenClaw sigue referenciando modelos con el formato `provider/model`:

En **OpenClaw 2026.9.4**, selecciona el modelo solo para la sesión actual con `-s`. Conserva `agents.defaults.model`, los valores predeterminados del agente y los permisos. Usa `/model default -s` para recuperar el modelo predeterminado en esta sesión.

```text
/model tokenlab-claude/claude-sonnet-5 -s
/model default -s
```

### Ejemplos de modelos

| Proveedor | Referencia del modelo | Descripción |
|----------|-----------------------|-------------|
| `tokenlab` | `tokenlab/gpt-5.6-terra` | Ruta compatible con OpenAI |
| `tokenlab-responses` | `tokenlab-responses/gpt-5.6-terra` | Ruta de Responses API |
| `tokenlab-claude` | `tokenlab-claude/claude-sonnet-5` | Ruta nativa de Claude Messages |
| `tokenlab-gemini` | `tokenlab-gemini/gemini-3.5-flash` | Ruta nativa de Gemini |
| `tokenlab-minimax` | `tokenlab-minimax/minimax-m3` | Ruta nativa de MiniMax |

Explora todos los modelos disponibles en [tokenlab.sh/models](https://tokenlab.sh/es/models).

## Cuándo usar cada proveedor

- `tokenlab`: elección predeterminada para la mayoría de los casos de uso generales de agentes y chat.
- `tokenlab-responses`: úsalo cuando tu flujo de trabajo en OpenClaw dependa explícitamente de la semántica de OpenAI Responses.
- `tokenlab-claude`: úsalo cuando quieras el comportamiento nativo de Messages de Claude.
- `tokenlab-gemini`: úsalo cuando necesites el formato de request/response nativo de Gemini o integraciones existentes al estilo Gemini.
- `tokenlab-minimax`: úsalo cuando quieras MiniMax en su ruta nativa.

Si no necesitas el comportamiento nativo de Gemini, aún puedes llamar a modelos de Gemini a través de `tokenlab/gemini-*` en la ruta compatible con OpenAI.

## Errores comunes

<AccordionGroup>
  <Accordion title="Aún usando el antiguo array top-level providers">
    La documentación actual de OpenClaw usa `models.providers`. Si mantienes el formato anterior del array top-level `providers`, OpenClaw puede ignorar la configuración o no resolver los prefijos de provider como se espera.
  </Accordion>
  <Accordion title="Olvidar /v1 en tokenlab-responses">
    `openai-responses` se mapea a la ruta `/v1/responses` de TokenLab, por lo que `tokenlab-responses` debe usar `https://api.tokenlab.sh/v1`.
  </Accordion>
  <Accordion title="Agregar /v1 a tokenlab-claude, tokenlab-gemini o tokenlab-minimax">
    `anthropic-messages` y `google-generative-ai` deben usar `https://api.tokenlab.sh` sin `/v1`. Agregar `/v1` puede producir rutas de solicitud incorrectas.
  </Accordion>
  <Accordion title="¿OpenClaw aún soporta Gemini nativo?">
    Sí. La documentación actual de OpenClaw todavía incluye el provider integrado `google` y también soporta providers personalizados usando `api: "google-generative-ai"`. Así que `tokenlab-gemini` sigue siendo una ruta Gemini nativa válida para los usuarios de OpenClaw.
  </Accordion>
</AccordionGroup>

## Verificar la configuración

Reinicia esa misma instancia y verifica modelo y proveedor. `models status` sin `--probe` no prueba una llamada al modelo. Las pruebas reales y mensajes pueden consumir tokens. Compara respuesta y registro TokenLab. Para restaurar, elige el modelo anterior y elimina solo las entradas añadidas.

```bash
openclaw gateway restart
openclaw models status
```

## Próximos pasos

Una vez que OpenClaw esté conectado, estas guías te ayudarán a usar TokenLab de forma más eficaz:

- [Formatos de API](/guides/api-formats) — comprende las diferencias entre las rutas OpenAI, Responses, Anthropic y Gemini
- [IDE / SDK Compatibility](/guides/ide-sdk-compatibility) — consulta cuándo `/v1/responses` es la mejor opción
- [Manejo de errores](/guides/error-handling) — aprende modos de fallo comunes y patrones de recuperación
- [Resumen de modelos](https://tokenlab.sh/es/models) — explora los IDs de modelos antes de integrarlos en los agents
