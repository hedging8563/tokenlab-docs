---
title: "작업 조회(Volc 호환)"
openapi: "GET /api/v3/contents/generations/tasks/{id}"
description: "cgt 작업 ID로 Seedance 호환 작업을 조회합니다."
---

## 개요

이 엔드포인트는 Volc 스타일 Seedance 작업 구조를 이미 사용하는 팀을 위한 것입니다. 인증은 TokenLab Bearer API Key를 사용합니다. 반환되는 작업 ID는 `cgt-`로 시작하며, `status`가 `succeeded`, `failed`, `cancelled`, `expired` 중 하나가 될 때까지 폴링하세요.

<Warning>
v1 상태 enum이나 응답 struct를 재사용하지 마세요. `/v1/tasks/{id}`는 `pending`, `processing`, `completed`, `failed`를 사용하고, 이 엔드포인트는 위의 v3 enum을 사용합니다. v3에서 `duration`은 문자열이며 `error`는 실패한 경우에만 나타납니다.
</Warning>

같이 보기 [Seedance 2.0 비디오 모델](/ko/guides/seedance-2-video) 및 [비디오 생성](/ko/guides/video-generation).

## 응답 형식

작업 상태는 `queued`, `running`, `succeeded`, `failed`, `cancelled`, `expired`를 사용합니다. 성공 시 `content.video_url`을 반환하며 가능한 경우 `content.last_frame_url`, 공식 생성 필드, `usage`도 포함됩니다. `error`는 실패한 경우에만 포함됩니다. `progress`, `return_last_frame`, `watermark`, `priority`, `camera_fixed` 같은 TokenLab 확장 필드는 추가하지 않습니다.

## 예시

```bash
curl https://api.tokenlab.sh/api/v3/contents/generations/tasks/cgt-20260102030405-a1b2c \
  -H "Authorization: Bearer $TOKENLAB_API_KEY"
```
