---
title: "음악 생성 상태 조회"
openapi: "GET /v1/tasks/{id}"
description: "음악 생성 작업의 상태와 결과를 조회합니다."
---

이 엔드포인트를 폴링하여 음악 생성 작업의 상태를 확인하세요.

<Info>
생성 응답에 `poll_url` 이 포함되어 있으면, 상태 조회에는 그 URL 을 우선 사용하세요. `id` 와 `task_id` 는 같은 비동기 작업 ID 로 취급해야 합니다.


만료되었거나 사용할 수 없는 공개 작업은 `404 async_task_not_found`를 반환할 수 있습니다. 현재 API 키로 작업 소유권을 확인할 수 없으면 `403 task_not_owned`가 반환됩니다.
</Info>

## 경로 파라미터

<ParamField path="id" type="string" required>
  음악 생성 작업 ID입니다.
</ParamField>

## 응답

<ResponseField name="id" type="string">
  작업 ID.
</ResponseField>

<ResponseField name="task_id" type="string">
  동일한 비동기 작업 ID의 별칭이며 반환되는 경우에만 존재합니다.
</ResponseField>

<ResponseField name="poll_url" type="string">
  생성 응답에서 제공될 때 우선 사용할 폴링 URL입니다.
</ResponseField>

<ResponseField name="status" type="string">
  작업 상태: `pending`, `processing`, `completed` 또는 `failed`.
</ResponseField>

<ResponseField name="progress" type="integer">
  선택적 진행 값입니다. 실제 진행 값이 제공될 때만 반환됩니다. 완료 여부는 `status` 로 판단하세요.
</ResponseField>

결과 필드는 작업에 따라 다릅니다. 음악은 `audio_url`과 `audio_id`, 가사 전용 작업은 `lyrics`, 음원 분리는 `data`에 여러 파일을 반환할 수 있습니다. `video_url`, 제목, 진행률 등은 선택 사항입니다. `status`로 완료 여부를 확인하고 실제 반환된 필드를 읽으세요.

<ResponseField name="audio_id" type="string">
  지원되는 후속 작업에서 이 작업 ID와 함께 사용할 수 있는 선택적 오디오 ID입니다.
</ResponseField>

<ResponseField name="data" type="array">
  선택적 출력 배열입니다. 분리된 오디오 파일의 `audio_url` 또는 `url`을 포함합니다.
</ResponseField>

<ResponseField name="audio_url" type="string">
  오디오 파일을 다운로드할 URL (완료 시).
</ResponseField>

<ResponseField name="video_url" type="string">
  비주얼라이저가 포함된 비디오 버전의 URL (완료 시).
</ResponseField>

<ResponseField name="title" type="string">
  생성된 곡 제목.
</ResponseField>

<ResponseField name="lyrics" type="string">
  생성되거나 제공된 가사.
</ResponseField>

<ResponseField name="error" type="string | object">
  에러 메시지 (실패 시).
</ResponseField>

<RequestExample>
```bash cURL
curl --fail-with-body "https://api.tokenlab.sh/v1/tasks/ldtask_aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa" \
  -H "Authorization: Bearer sk-your-api-key"
```

```python Python
import requests
import time

task_id = "ldtask_aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"

while True:
    response = requests.get(
        f"https://api.tokenlab.sh/v1/tasks/{task_id}",
        headers={"Authorization": "Bearer sk-your-api-key"}
    )
    response.raise_for_status()
    result = response.json()

    if result["status"] == "completed":
        print(result)
        break
    elif result["status"] == "failed":
        print(result.get("error", "Task failed"))
        break

    time.sleep(5)  # Poll every 5 seconds
```

```javascript JavaScript
const taskId = 'ldtask_aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa';

async function pollForMusic() {
  while (true) {
    const response = await fetch(
      `https://api.tokenlab.sh/v1/tasks/${taskId}`,
      { headers: { 'Authorization': 'Bearer sk-your-api-key' } }
    );

    if (!response.ok) throw new Error(await response.text());
    const result = await response.json();

    if (result.status === 'completed') {
      console.log(result);
      return result;
    } else if (result.status === 'failed') {
      throw new Error(typeof result.error === 'string'
        ? result.error
        : JSON.stringify(result.error ?? result));
    }

    await new Promise(r => setTimeout(r, 5000));
  }
}

await pollForMusic();
```

```go Go
package main

import (
    "encoding/json"
    "fmt"
    "io"
    "log"
    "net/http"
    "time"
)

func main() {
    taskID := "ldtask_aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"

    for {
        req, err := http.NewRequest("GET",
            fmt.Sprintf("https://api.tokenlab.sh/v1/tasks/%s", taskID), nil)
        if err != nil {
            log.Fatal(err)
        }
        req.Header.Set("Authorization", "Bearer sk-your-api-key")

        client := &http.Client{}
        resp, err := client.Do(req)
        if err != nil {
            log.Fatal(err)
        }
        if resp.StatusCode < 200 || resp.StatusCode >= 300 {
            body, _ := io.ReadAll(resp.Body)
            resp.Body.Close()
            log.Fatalf("HTTP %d: %s", resp.StatusCode, body)
        }

        var result map[string]interface{}
        err = json.NewDecoder(resp.Body).Decode(&result)
        resp.Body.Close()
        if err != nil {
            log.Fatal(err)
        }

        if result["status"] == "completed" {
            fmt.Println(result)
            break
        } else if result["status"] == "failed" {
            fmt.Println(result)
            break
        }

        time.Sleep(5 * time.Second)
    }
}
```

```php PHP
<?php
$taskId = 'ldtask_aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa';

while (true) {
    $ch = curl_init("https://api.tokenlab.sh/v1/tasks/{$taskId}");

    curl_setopt_array($ch, [
        CURLOPT_RETURNTRANSFER => true,
        CURLOPT_HTTPHEADER => [
            'Authorization: Bearer sk-your-api-key'
        ]
    ]);

    $response = curl_exec($ch);
    $httpStatus = curl_getinfo($ch, CURLINFO_HTTP_CODE);
    $curlError = curl_error($ch);
    curl_close($ch);

    if ($response === false) {
        throw new RuntimeException($curlError);
    }
    if ($httpStatus < 200 || $httpStatus >= 300) {
        throw new RuntimeException("HTTP {$httpStatus}: {$response}");
    }

    $result = json_decode($response, true, 512, JSON_THROW_ON_ERROR);

    if ($result['status'] === 'completed') {
        echo json_encode($result, JSON_UNESCAPED_SLASHES) . "\n";
        break;
    } elseif ($result['status'] === 'failed') {
        echo json_encode($result, JSON_UNESCAPED_SLASHES) . "\n";
        break;
    }

    sleep(5);
}
```
</RequestExample>

<ResponseExample>
```json 응답 (완료)
{
  "id": "ldtask_aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa",
  "task_id": "ldtask_aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa",
  "poll_url": "/v1/tasks/ldtask_aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa",
  "status": "completed",
  "audio_url": "https://cdn.example.com/music/abc123.mp3",
  "title": "Night Drive",
  "lyrics": "[Verse 1]\nDriving through the night...",
  "audio_id": "audio_abc123"
}
```
</ResponseExample>
