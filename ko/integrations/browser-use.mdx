---
title: "Browser Use"
description: "TokenLab 모델로 Browser Use 에이전트 실행하기"
---

## 개요

Browser Use는 OpenAI 호환 채팅 모델 래퍼인 `ChatOpenAILike`를 통해 TokenLab을 호출할 수 있습니다.

<Note>
**유형**: 브라우저 자동화 에이전트 프레임워크

**기본 경로**: OpenAI 호환 Chat Completions

**지원 신뢰도**: 지원되는 OpenAI 호환 경로
</Note>

## 환경 설정

```bash
export TOKENLAB_API_KEY="sk-your-tokenlab-key"
```

## 예시

```python
import os

from browser_use import Agent
from browser_use.llm.openai.like import ChatOpenAILike

llm = ChatOpenAILike(
    model="claude-sonnet-5",
    api_key=os.environ["TOKENLAB_API_KEY"],
    base_url="https://api.tokenlab.sh/v1",
)

agent = Agent(
    task="Compare the top search results for TokenLab integrations",
    llm=llm,
)
```

## 엔드포인트 참고 사항

OpenAI 호환 클라이언트를 통해 Chat Completions를 호출하는 Browser Use 워크플로우에는 이 페이지를 참조하십시오. 워크플로우에서 TokenLab 네이티브 응답, Anthropic Messages 또는 Gemini 엔드포인트가 필요한 경우, 해당 프로토콜을 직접 지원하는 Browser Use의 클라이언트 경로를 사용하십시오.