---
title: "Quickstart"
description: "Send your first TokenLab API request"
---

## Get an API key

<Steps>
  <Step title="Create an account">
    Sign up at [tokenlab.sh](https://tokenlab.sh) using your email, Gmail, or GitHub account.
  </Step>
  <Step title="Create or copy an API key">
    Go to **Console → API Keys** and create a new key, or copy an existing one. Copy it now. The full key is shown only once.
  </Step>
  <Step title="Use the included trial balance">
    New accounts include enough trial balance for a few small requests. You do not need to add funds for this Quickstart.
  </Step>
</Steps>

<Warning>
Keep the API key secret. Do not put it in client-side code or a public repository.
</Warning>

## Install a client

<CodeGroup>

```bash Python
pip install openai
```

```bash JavaScript
npm install openai
```

```bash Go
go get github.com/openai/openai-go/v3
```

```bash PHP
composer require openai-php/client
```

</CodeGroup>

## Send your first request

For most new integrations, start with **Chat Completions** on `POST /v1/chat/completions`.

<CodeGroup>

```bash cURL
curl https://api.tokenlab.sh/v1/chat/completions \
  -H "Authorization: Bearer sk-your-api-key" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-5.6-terra",
    "messages": [
      {"role": "user", "content": "What is the capital of France?"}
    ]
  }'
```

```python Python
from openai import OpenAI

client = OpenAI(
    api_key="sk-your-api-key",
    base_url="https://api.tokenlab.sh/v1"
)

response = client.chat.completions.create(
    model="gpt-5.6-terra",
    messages=[{"role": "user", "content": "What is the capital of France?"}]
)

print(response.choices[0].message.content)
```

```javascript JavaScript
import OpenAI from 'openai';

const client = new OpenAI({
  apiKey: 'sk-your-api-key',
  baseURL: 'https://api.tokenlab.sh/v1'
});

const response = await client.chat.completions.create({
  model: 'gpt-5.6-terra',
  messages: [{ role: 'user', content: 'What is the capital of France?' }],
});

console.log(response.choices[0].message.content);
```

</CodeGroup>

<Note>
Use `POST /v1/responses` only when you need Responses-specific behavior. Support for Responses-only fields depends on the selected model.
</Note>

## Add balance when you need it

The trial balance covers a few small requests. Add funds in **Console → Billing** when you are ready to make more.

## Try another model

Change the `model` field to try a different model. The [Models page](https://tokenlab.sh/en/models) lists the IDs currently available.

```python
response = client.chat.completions.create(model="gpt-5.6-terra", messages=[{"role": "user", "content": "Hello"}])
response = client.chat.completions.create(model="gpt-5.6-luna", messages=[{"role": "user", "content": "Hello"}])
response = client.chat.completions.create(model="claude-sonnet-5", messages=[{"role": "user", "content": "Hello"}])
response = client.chat.completions.create(model="gemini-3.5-flash", messages=[{"role": "user", "content": "Hello"}])
response = client.chat.completions.create(model="deepseek-reasoner", messages=[{"role": "user", "content": "Hello"}])
```

## Stream the response

```python
stream = client.chat.completions.create(
    model="gpt-5.6-terra",
    messages=[{"role": "user", "content": "Tell me a short story."}],
    stream=True
)

for chunk in stream:
    delta = chunk.choices[0].delta.content
    if delta:
        print(delta, end="")
```

## Keep building

<CardGroup cols={2}>
  <Card title="Authentication" icon="key" href="/authentication">
    Store, limit, and rotate API keys.
  </Card>
  <Card title="OpenAI SDK" icon="code" href="/integrations/openai-sdk">
    Use your existing OpenAI SDK with TokenLab.
  </Card>
  <Card title="API Reference" icon="book" href="/api-reference/introduction">
    Find request fields, response shapes, and examples.
  </Card>
  <Card title="Models" icon="robot" href="https://tokenlab.sh/en/models">
    Browse current model availability and pricing.
  </Card>
</CardGroup>
