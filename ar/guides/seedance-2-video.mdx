---
title: "نماذج فيديو Seedance 2.0"
description: "اختر نموذج Seedance 2.0 ومسار التكامل المناسب في TokenLab."
---

ابدأ من هنا عند إضافة Seedance 2.0 إلى تطبيق. تقتصر هذه الصفحة على اختيار النموذج ومسار العمل، بينما توجد أدلة مستقلة للترحيل من Volcengine والمواد القابلة لإعادة الاستخدام والتحقق من الأشخاص الحقيقيين.

## اختر مسار التكامل

| الهدف | الاستخدام | الخطوة التالية |
| --- | --- | --- |
| إنشاء تكامل فيديو جديد في TokenLab | واجهة `POST /v1/videos/generations` الموحدة | [دليل توليد الفيديو](/ar/guides/video-generation) |
| ترحيل عميل Seedance حالي من Volcengine | واجهة REST أو Action متوافقة مع Volcengine | [ترحيل Seedance من Volcengine](/ar/guides/seedance-volcengine-migration) |
| إعادة استخدام صورة رمزية أو منتج أو نمط أو شخص حقيقي | مكتبة مواد TokenLab | [مواد Seedance والتحقق من الأشخاص](/ar/guides/seedance-materials) |

تستخدم المسارات الثلاثة `Authorization: Bearer <TOKENLAB_API_KEY>`. تحافظ المسارات المتوافقة مع Volcengine على شكل الطلب، لكنها لا تقبل توقيع AK/SK.

## اختيار النموذج

| النموذج | الدقة المدعومة |
|-------|-----------------------|
| `seedance-2.0` | `480p`, `720p`, `1080p`, `4k` |
| `seedance-2.0-fast` | `480p`, `720p` |
| `seedance-2.0-mini` | `480p`, `720p` |

ترفض نماذج `seedance-2.0-fast` و `seedance-2.0-mini` طلبات `1080p` و `4k`. يمكن استخدام الصوت فقط في سير عمل المراجع المدعومة؛ ولا يتم دعم طلبات Seedance التي تعتمد على الصوت فقط أو النص مع الصوت فقط.

## العمليات المدعومة

| العملية | المدخلات النموذجية |
|-----------|----------------|
| `text-to-video` | `prompt` |
| `image-to-video` | `prompt`, `image_url` أو مدخل صورة متوافق |
| `start-end-to-video` | `prompt`, `start_image`, `end_image` |
| `reference-to-video` | `prompt`, `reference_images`, `video_urls` (اختياري), `audio_urls` (اختياري) |
| `video-to-video` | `prompt`, `video_url` أو مدخل فيديو متوافق |
| `video-extension` | `task_id` أو مدخل استكمال خاص بالنموذج |

## التسعير والنتائج غير المتزامنة

يعتمد تسعير Seedance 2.0 على دقة المخرجات ونوع المدخلات والنموذج. استخدم [Pricing API](/ar/api-reference/pricing/get-pricing) أو [Models API](/ar/api-reference/models/list-models) قبل برمجة واجهة المستخدم الخاصة بمنتجك بشكل ثابت.

توليد الفيديو عملية غير متزامنة. اتبع `poll_url` المُرجع بواسطة طلبات الإنشاء، أو استخدم [الحصول على حالة المهمة](/ar/api-reference/tasks/get-task-status) مع معرف المهمة المُرجع.

## واجهات API ذات الصلة

| API | الخطوة التالية |
|-----|---------------|
| `POST /v1/videos/generations` | [إنشاء فيديو](/ar/api-reference/video/create-video) |
| `GET /v1/tasks/{id}` | [جلب حالة المهمة](/ar/api-reference/tasks/get-task-status) |
| توافق مهام Seedance | [إنشاء مهمة متوافقة مع Volcengine](/ar/api-reference/video/create-volc-compatible-seedance-task) |
| توافق إجراءات المواد | [إجراءات المواد المتوافقة مع Volcengine](/ar/api-reference/video/volc-compatible-material-actions) |
