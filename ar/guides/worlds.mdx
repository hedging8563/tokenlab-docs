---
title: "Worlds"
description: "أنشئ Worlds قابلة للاستكشاف، وارفع مطالبات وسائط، وتابع المهام، وأدر Worlds المكتملة."
---

تنشئ عملية World عوالم Marble قابلة للاستكشاف بشكل غير متزامن. استخدم Worlds عندما يكون الناتج بيئة يمكن للمستخدم فتحها أو الرجوع إليها. استخدم توليد 3D عندما تحتاج إلى أصل نموذج مستقل.

## المدخلات وأصول الوسائط

يمكن أن تبدأ مهام World من نص أو صور أو صور متعددة أو فيديو حسب النموذج المحدد وشكل الطلب. للمطالبات الإعلامية القابلة لإعادة الاستخدام، جهّز media asset أولاً ثم أشر إليه في طلب World.

يُتحقق من الوصول إلى سجلات الوسائط والعوالم حسب المؤسسة المالكة لمفتاح API. راجع خيارات `permission` للنموذج قبل المشاركة؛ فقد تكون لروابط الأصول أو العرض قواعد وصول مستقلة.

## الإنشاء والمتابعة

تتضمن استجابات الإنشاء ID مهمة عامًا وغالبًا poll_url. احفظ هذا ID قبل عرض التقدم، ثم اتبع poll_url أو GET /v1/tasks/{id} حتى تصبح المهمة completed أو failed.

```bash
curl -X POST "https://api.tokenlab.sh/v1/worlds/generations" \
  -H "Authorization: Bearer sk-your-api-key" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "marble-1.1",
    "prompt": "A quiet coastal town at sunset with narrow alleys and warm lights"
  }'
```

## إدارة Worlds المكتملة

استخدم نقاط قائمة وتفاصيل Worlds للعوالم المكتملة المتاحة في حسابك. استخدم نقاط حالة المهمة للمهام المنتظرة أو الفاشلة. لا تحذف إلا عندما يزيل المستخدم World الناتج عمدًا من منتجك.

## أخطاء شائعة

- لا تفترض أن كل نموذج World يدعم كل نوع إدخال.
- أبقِ URLs الصور أو الفيديو الموقعة صالحة بما يكفي لمحاولات جلب الخدمة.
- احفظ ID المهمة الراجع قبل إعادة المحاولة بعد timeout.
- استخدم توليد 3D لملفات النماذج القابلة للتنزيل بدلاً من Worlds القابلة للاستكشاف.

## مرجع API

| الموضوع | المرجع |
|-------|-----------|
| إنشاء World | [إنشاء World](/ar/api-reference/worlds/create-world) |
| جلب حالة World | [جلب حالة World](/ar/api-reference/worlds/get-world-status) |
| تحضير رفع Media Asset | [تحضير رفع Media Asset](/ar/api-reference/worlds/prepare-media-asset-upload) |
| جلب Media Asset | [جلب Media Asset](/ar/api-reference/worlds/get-media-asset) |
| عرض Worlds | [عرض Worlds](/ar/api-reference/worlds/list-worlds) |
| جلب World | [جلب World](/ar/api-reference/worlds/get-world) |
| حذف World | [حذف World](/ar/api-reference/worlds/delete-world) |
| المهام غير المتزامنة | [المهام غير المتزامنة](/ar/guides/async-jobs-polling) |
| دليل توليد 3D | [دليل توليد 3D](/ar/guides/3d-generation) |
