---
title: "الحصول على الأسعار"
openapi: "/openapi/ar.json GET /v1/pricing"
description: "الحصول على معلومات الأسعار للنماذج"
---

هذه نقطة نهاية عامة ولا تتطلب مفتاح API.

## نظرة عامة

استخدم هذا الـ endpoint لعرض سطح الأسعار العام الحالي للنماذج النشطة.

للاطلاع على تفاصيل التسعير الكاملة لنموذج واحد، بما في ذلك التسعير المنظم ومصدر البيانات، فضّل [الحصول على تسعير النموذج](/ar/api-reference/models/get-model).

## معلمات الاستعلام

<ParamField query="provider" type="string">
  فلتر اختياري حسب المزود، مثل `openai` أو `anthropic` أو `google`.
</ParamField>

<ParamField query="tag" type="string">
  فلتر اختياري حسب وسم النموذج، مثل `chat` أو `image` أو `video` أو `embedding` أو `translation`.
</ParamField>

## الاستجابة

<ResponseField name="object" type="string">
  دائمًا `pricing`.
</ResponseField>

<ResponseField name="updated_at" type="string">
  طابع زمني بصيغة ISO لوقت نشر الأسعار العامة الحالية.
</ResponseField>

<ResponseField name="currency" type="string">
  دائمًا `USD`.
</ResponseField>

<ResponseField name="data" type="array">
  مصفوفة من إدخالات التسعير. يتضمن كل عنصر `model` و `provider` و `is_lock_price` و `capabilities` و `aliases` وحقول الملخص العام للتسعير، سواء كانت قيمًا scalar أو بنية منظمة، كما تعيدها الواجهة التفاعلية أعلاه.
</ResponseField>

<RequestExample>
```bash cURL
curl "https://api.tokenlab.sh/v1/pricing?provider=openai&tag=chat"
```

```python Python
import requests

response = requests.get(
    "https://api.tokenlab.sh/v1/pricing",
    params={"provider": "openai", "tag": "chat"}
)

print(response.json())
```

```javascript JavaScript
const response = await fetch(
  'https://api.tokenlab.sh/v1/pricing?provider=openai&tag=chat'
);

const data = await response.json();
console.log(data);
```

```go Go
package main

import (
    "encoding/json"
    "fmt"
    "net/http"
)

func main() {
    req, _ := http.NewRequest("GET", "https://api.tokenlab.sh/v1/pricing?provider=openai&tag=chat", nil)

    client := &http.Client{}
    resp, _ := client.Do(req)
    defer resp.Body.Close()

    var result map[string]interface{}
    json.NewDecoder(resp.Body).Decode(&result)
    fmt.Println(result)
}
```

```php PHP
<?php
$ch = curl_init('https://api.tokenlab.sh/v1/pricing?provider=openai&tag=chat');

curl_setopt_array($ch, [
    CURLOPT_RETURNTRANSFER => true
]);

$response = curl_exec($ch);
curl_close($ch);

$data = json_decode($response, true);
print_r($data);
```
</RequestExample>

<ResponseExample>
```json Response
{
  "object": "pricing",
  "updated_at": "2026-04-20T00:00:00.000Z",
  "currency": "USD",
  "data": [
    {
      "model": "gpt-4o",
      "provider": "openai",
      "is_lock_price": false,
      "input_per_1m_tokens": "2.50",
      "output_per_1m_tokens": "10.00",
      "capabilities": ["chat"],
      "aliases": ["gpt-4o-latest"]
    }
  ]
}
```
</ResponseExample>

<Note>
استخدم لوحة OpenAPI التفاعلية أعلاه للحصول على مخطط الاستجابة الدقيق. قد تكشف إدخالات التسعير العامة أيضًا `lock_price_per_request` و `has_complex_pricing` و `supported_operations` و `pricing_summary` و `pricing_provenance`.
</Note>
