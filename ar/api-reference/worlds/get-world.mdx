---
title: "جلب World"
openapi: "/openapi/ar.json GET /v1/worlds/{id}"
description: "يجلب World Labs Marble world مكتمل"
---

اجلب Marble World مكتمل تم إنشاؤه عبر TokenLab. يجب أن ينتمي world إلى نفس مؤسسة مفتاح API.

## معلمات المسار

<ParamField path="id" type="string" required>
  معرّف عالم World Labs.
</ParamField>

## الاستجابة

<ResponseField name="world_id" type="string">
  معرّف عالم World Labs.
</ResponseField>

<ResponseField name="world_marble_url" type="string">
  عنوان URL لعالم World Labs Marble.
</ResponseField>

<ResponseField name="assets" type="object">
  أصول World المولدة عند توفرها.
</ResponseField>

<RequestExample>
```bash cURL
curl "https://api.tokenlab.sh/v1/worlds/world_123" \
  -H "Authorization: Bearer sk-your-api-key"
```

```python Python
import requests

response = requests.get(
    "https://api.tokenlab.sh/v1/worlds/world_123",
    headers={"Authorization": "Bearer sk-your-api-key"},
)
print(response.json()["world_id"])
```

```javascript JavaScript
const response = await fetch('https://api.tokenlab.sh/v1/worlds/world_123', {
  headers: { Authorization: 'Bearer sk-your-api-key' }
});
console.log(await response.json());
```
</RequestExample>
