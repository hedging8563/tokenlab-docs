---
title: "عرض Worlds"
openapi: "POST /v1/worlds/list"
description: "يعرض عوالم World Labs Marble المكتملة التي تم إنشاؤها عبر TokenLab"
---

يعرض World Labs Marble worlds المكتملة التي تم إنشاؤها عبر TokenLab. النتائج مقيدة بمؤسستك وتشمل فقط الموارد المنشأة عبر TokenLab.

استخدم `/v1/worlds/generations/{id}` للمهام قيد الانتظار أو الفاشلة. هذه القائمة تعيد فقط العوالم المكتملة التي فهرسها TokenLab بعد الإنشاء.

## جسم الطلب

<ParamField body="page_size" type="integer" default="20">
  عدد worlds المراد إرجاعها، من 1 إلى 100.
</ParamField>

<ParamField body="page_token" type="string">
  رمز الصفحة من الاستجابة السابقة.
</ParamField>

<ParamField body="status" type="string">
  مرشح اختياري: `SUCCEEDED` أو `PENDING` أو `RUNNING` أو `FAILED`. تعرض هذه النقطة العوالم المكتملة؛ وتعيد القيم الثلاث الأخرى قائمة فارغة. استعلم عن مهمة التوليد لمتابعة التقدم.
</ParamField>

<ParamField body="model" type="string">
  مرشح اختياري لنموذج Marble.
</ParamField>

<ParamField body="tags" type="array">
  مرشح اختياري للوسوم.
</ParamField>

<ParamField body="is_public" type="boolean">
  مرشح اختياري للرؤية.
</ParamField>

<ParamField body="created_after" type="string">
  إرجاع worlds المنشأة بعد هذا الوقت فقط.
</ParamField>

<ParamField body="created_before" type="string">
  إرجاع worlds المنشأة قبل هذا الوقت فقط.
</ParamField>

<ParamField body="sort_by" type="string" default="created_at">
  الترتيب حسب `created_at` أو `updated_at`.
</ParamField>

<ParamField body="sort_order" type="string" default="desc">
  اتجاه الترتيب: `asc` أو `desc`.
</ParamField>

## الاستجابة

<ResponseField name="worlds" type="array">
  مصفوفة سجلات world.
</ResponseField>

<ResponseField name="next_page_token" type="string | null">
  رمز الصفحة التالية أو `null`.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST "https://api.tokenlab.sh/v1/worlds/list" \
  -H "Authorization: Bearer sk-your-api-key" \
  -H "Content-Type: application/json" \
  -d '{"page_size": 20, "status": "SUCCEEDED", "model": "marble-1.1"}'
```

```python Python
import requests

response = requests.post(
    "https://api.tokenlab.sh/v1/worlds/list",
    headers={"Authorization": "Bearer sk-your-api-key"},
    json={"page_size": 20, "status": "SUCCEEDED", "model": "marble-1.1"},
)
print(response.json()["worlds"])
```

```javascript JavaScript
const response = await fetch('https://api.tokenlab.sh/v1/worlds/list', {
  method: 'POST',
  headers: {
    Authorization: 'Bearer sk-your-api-key',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({ page_size: 20, status: 'SUCCEEDED', model: 'marble-1.1' })
});
console.log((await response.json()).worlds);
```
</RequestExample>
