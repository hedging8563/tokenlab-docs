---
title: "قائمة الدفعات"
openapi: "/openapi/ar.json GET /v1/batches"
description: "يستعرض مهام الدفعات لمنظمة مفتاح API الحالية."
---

## نظرة عامة

هذه النقطة النهائية هي جزء من واجهة برمجة التطبيقات للدفعات المتوافقة مع OpenAI من TokenLab. تستخدم مهام الدفعات ملفات JSONL المرفوعة، وتعمل بشكل غير متزامن، ويمكن أن تعيد ملفات الإخراج/الأخطاء لاحقًا.

## ملاحظات

- يجب أن تستخدم ملفات الإدخال للدفعات `purpose=batch`.
- `completion_window` هو حاليًا `24h`.
- ترتيب الإخراج غير مضمون؛ يجب دائمًا المطابقة بواسطة `custom_id`.
- البث غير مدعوم داخل عناصر الدفعة.

## مثال

<RequestExample>
```bash cURL
curl -X GET "https://api.tokenlab.sh/v1/batches?limit=20" \
  -H "Authorization: Bearer sk-your-api-key"
```
</RequestExample>

## الطلب / الاستجابة

استخدم لوحة OpenAPI التفاعلية أعلاه للحصول على المخطط الدقيق.

## نصائح تشغيلية

- استخدم `custom_id` للتسوية غير القابلة للتغيير في الأسفل.
- توقع `output_file_id` و `error_file_id` فقط بعد أن ينهي العامل الدفعة.

## مثال الاستجابة

<ResponseExample>
```json 200 OK
{
  "object": "list",
  "data": [{ "id": "batch_abc123", "object": "batch", "status": "completed" }],
  "first_id": "batch_abc123",
  "last_id": "batch_abc123",
  "has_more": false
}
```
</ResponseExample>

## حقول مهمة

<ResponseField name="object" type="string">نوع كائن الاستجابة.</ResponseField>
<ResponseField name="data" type="array">القائمة أو payload المعاد في هذه الاستجابة.</ResponseField>
<ResponseField name="status" type="string">حالة دورة الحياة الحالية لهذا المورد.</ResponseField>
<ResponseField name="has_more" type="boolean">تتوفر نتائج إضافية عندما تكون قيمة `has_more` هي true.</ResponseField>
