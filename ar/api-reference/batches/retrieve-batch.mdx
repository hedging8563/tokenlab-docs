---
title: "استرجاع دفعة"
openapi: "/openapi/ar.json GET /v1/batches/{batch_id}"
description: "يسترجع الحالة الحالية، العد، وملفات النتائج لدفعة."
---

## نظرة عامة

تعتبر هذه النقطة جزءًا من واجهة برمجة التطبيقات الخاصة بالدفعات المتوافقة مع OpenAI من TokenLab. تستخدم مهام الدفعات ملفات JSONL المرفوعة، وتعمل بشكل غير متزامن، ويمكن أن تعيد ملفات الإخراج/الأخطاء لاحقًا.

## ملاحظات

- يجب أن تستخدم ملفات الإدخال للدفعة `purpose=batch`.
- `completion_window` حاليًا `24h`.
- ترتيب الإخراج غير مضمون؛ يجب دائمًا المطابقة بواسطة `custom_id`.
- لا يتم دعم البث داخل عناصر الدفعة.

## مثال

<RequestExample>
```bash cURL
curl -X GET "https://api.tokenlab.sh/v1/batches/batch_abc123" \
  -H "Authorization: Bearer sk-your-api-key"
```
</RequestExample>

## الطلب / الاستجابة

استخدم لوحة OpenAPI التفاعلية أعلاه للحصول على المخطط الدقيق.

## نصائح تشغيلية

- استخدم `custom_id` للمصالحة غير القابلة للتغيير في الأسفل.
- توقع `output_file_id` و `error_file_id` فقط بعد أن ينهي العامل الدفعة.

## مثال الاستجابة

<ResponseExample>
```json 200 OK
{
  "id": "batch_abc123",
  "object": "batch",
  "status": "completed",
  "input_file_id": "file_abc123",
  "output_file_id": "file_out123",
  "error_file_id": "file_err123",
  "request_counts": { "total": 100, "completed": 98, "failed": 2 }
}
```
</ResponseExample>

## حقول مهمة

<ResponseField name="request_counts" type="object">أعداد الطلبات الإجمالية والمكتملة والفاشلة والملغاة.</ResponseField>
<ResponseField name="output_file_id" type="string|null">معرّف ملف تستخدمه APIs المرتبطة.</ResponseField>
<ResponseField name="error_file_id" type="string|null">معرّف ملف تستخدمه APIs المرتبطة.</ResponseField>
