---
title: "إنشاء الموسيقى"
openapi: "POST /v1/music/generations"
description: "إنشاء مهمة توليد موسيقى باستخدام Suno"
---

قم بتوليد الموسيقى والكلمات باستخدام الذكاء الاصطناعي. هذه API غير متزامنة. تعيد استجابة الإنشاء هوية المهمة، وقد تتضمن أيضًا `poll_url` مفضلًا عند توفره.

## جسم الطلب

<ParamField body="model" type="string" default="suno_music">
  النموذج المستخدم: `suno_music` لتوليد الموسيقى، و `suno_lyrics` للكلمات فقط.
</ParamField>

<ParamField body="prompt" type="string">
  وصف الموسيقى. مطلوب لطلبات music و lyrics و upload-cover و upload-extend؛ ويمكن حذفه مع add-instrumental.
</ParamField>

<ParamField body="title" type="string">
  عنوان الأغنية التي تم توليدها.
</ParamField>

<ParamField body="tags" type="string">
  علامات النمط (مثل: "pop, upbeat, electronic").
</ParamField>

<ParamField body="negative_tags" type="string">
  الأنماط المطلوب تجنبها. مطلوب عندما تكون `audio_operation` هي `add-instrumental`.
</ParamField>

<ParamField body="action" type="string">
  نوع التوليد: `MUSIC` (افتراضي) أو `LYRICS`.
</ParamField>

<ParamField body="mv" type="string">
  إصدار نموذج Suno الرسمي. مطلوب لتوليد الموسيقى (عند حذف `action` أو استخدام `MUSIC`)؛ اتركه في طلبات الكلمات فقط. القيم المدعومة: `chirp-v3-5`, `chirp-v4`, `chirp-v4-5`, `chirp-v4.5`, `chirp-v4-5plus`, `chirp-v4.5plus`, `chirp-v4-5all`, `chirp-v4.5all`, `chirp-v5`, `chirp-v5-5`, `chirp-v5.5`.
</ParamField>

<ParamField body="make_instrumental" type="boolean">
  اضبطه على `true` لإنشاء نتيجة موسيقية بلا غناء.
</ParamField>

<ParamField body="audio_url" type="string">
  رابط صوت مرجعي أو مرفوع يمكن الوصول إليه علناً. عند إرساله من دون `audio_operation` يستخدم TokenLab تدفق upload-cover.
</ParamField>

<ParamField body="audio_operation" type="string">
  وضع الصوت المرفوع: `upload-cover` أو `upload-extend` أو `add-instrumental`. يتطلب `upload-extend` الحقل `continue_at`؛ ويتطلب `add-instrumental` الحقول `audio_url` و `title` و `tags` و `negative_tags`.
</ParamField>

<ParamField body="continue_clip_id" type="string">
  معرف (ID) لمقطع سابق للمتابعة منه.
</ParamField>

<ParamField body="continue_at" type="number">
  الطابع الزمني بالثواني الذي يبدأ منه الامتداد. مطلوب عندما تكون `audio_operation` هي `upload-extend`.
</ParamField>

## الاستجابة

<ResponseField name="id" type="string">
  معرف المهمة (Task ID) للاستعلام عن الحالة.
</ResponseField>

<ResponseField name="task_id" type="string">
  الاسم البديل لمعرّف المهمة غير المتزامنة عندما يعيده المهايئ.
</ResponseField>

<ResponseField name="poll_url" type="string">
  رابط polling المفضّل عندما يكون متاحًا.
</ResponseField>

<ResponseField name="status" type="string">
  حالة المهمة: `pending` أو `processing` أو `completed` أو `failed`.
</ResponseField>

<ResponseField name="created" type="integer">
  طابع Unix الزمني لوقت إنشاء المهمة.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST "https://api.tokenlab.sh/v1/music/generations" \
  -H "Authorization: Bearer sk-your-api-key" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "suno_music",
    "mv": "chirp-v4",
    "prompt": "An upbeat electronic dance track with heavy bass",
    "title": "Night Drive",
    "tags": "electronic, EDM, energetic"
  }'
```

```python Python
import requests

response = requests.post(
    "https://api.tokenlab.sh/v1/music/generations",
    headers={"Authorization": "Bearer sk-your-api-key"},
    json={
        "model": "suno_music",
        "mv": "chirp-v4",
        "prompt": "An upbeat electronic dance track with heavy bass",
        "title": "Night Drive",
        "tags": "electronic, EDM, energetic"
    }
)

task_id = response.json()["id"]
print(f"Task ID: {task_id}")
```

```javascript JavaScript
const response = await fetch('https://api.tokenlab.sh/v1/music/generations', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer sk-your-api-key',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    model: 'suno_music',
    mv: 'chirp-v4',
    prompt: 'An upbeat electronic dance track with heavy bass',
    title: 'Night Drive',
    tags: 'electronic, EDM, energetic'
  })
});

const data = await response.json();
console.log(`Task ID: ${data.id}`);
```

```go Go
package main

import (
    "bytes"
    "encoding/json"
    "fmt"
    "net/http"
)

func main() {
    payload := map[string]interface{}{
        "model":  "suno_music",
        "mv":     "chirp-v4",
        "prompt": "An upbeat electronic dance track with heavy bass",
        "title":  "Night Drive",
        "tags":   "electronic, EDM, energetic",
    }
    body, _ := json.Marshal(payload)

    req, _ := http.NewRequest("POST", "https://api.tokenlab.sh/v1/music/generations", bytes.NewBuffer(body))
    req.Header.Set("Authorization", "Bearer sk-your-api-key")
    req.Header.Set("Content-Type", "application/json")

    client := &http.Client{}
    resp, _ := client.Do(req)
    defer resp.Body.Close()

    var result map[string]interface{}
    json.NewDecoder(resp.Body).Decode(&result)
    fmt.Printf("Task ID: %s\n", result["id"])
}
```

```php PHP
<?php
$ch = curl_init('https://api.tokenlab.sh/v1/music/generations');

curl_setopt_array($ch, [
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_POST => true,
    CURLOPT_HTTPHEADER => [
        'Content-Type: application/json',
        'Authorization: Bearer sk-your-api-key'
    ],
    CURLOPT_POSTFIELDS => json_encode([
        'model' => 'suno_music',
        'mv' => 'chirp-v4',
        'prompt' => 'An upbeat electronic dance track with heavy bass',
        'title' => 'Night Drive',
        'tags' => 'electronic, EDM, energetic'
    ])
]);

$response = curl_exec($ch);
curl_close($ch);

$data = json_decode($response, true);
echo "Task ID: " . $data['id'];
```
</RequestExample>

<ResponseExample>
```json Response
{
  "id": "ldtask_aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa",
  "status": "pending",
  "created": 1706000000,
  "model": "suno_music"
}
```
</ResponseExample>
