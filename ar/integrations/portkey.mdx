---
title: "بوابة Portkey"
description: "استخدم TokenLab كمزود أساسي (upstream provider) في بوابة Portkey"
---

## نظرة عامة

اضبط TokenLab في Portkey كمضيف مخصص متوافق مع OpenAI. استخدم Responses فقط إذا كان إصدار Portkey لديك يدعم نقطة نهاية Responses مخصصة.

<Note>
**النوع**: بوابة ذكاء اصطناعي (AI Gateway)

**المسارات الأساسية**: إكمال المحادثات المتوافق مع OpenAI و Responses

**مستوى دعم المسار**: مسار مدعوم
</Note>

## قيم المزود

| الحقل | القيمة |
|-------|-------|
| المزود | `openai` |
| Custom Host | `https://api.tokenlab.sh/v1` |
| API key | `sk-your-tokenlab-key` |
| النموذج | معرّف نموذج TokenLab متاح حالياً |

## إكمال المحادثات (Chat Completions)

استخدم TokenLab لطلبات المحادثة المتوافقة مع OpenAI:

يتطلب هذا المثال بوابة Portkey Gateway ذاتية الاستضافة قيد التشغيل. استبدل عنوان البوابة بعنوان النشر لديك. عند استخدام Portkey Cloud، أرسل أيضاً مفتاح Portkey API عبر `x-portkey-api-key`. راجع [دليل إعداد Portkey](https://portkey.ai/docs/api-reference/inference-api/headers).

```bash
curl https://your-portkey-gateway.example/v1/chat/completions \
  -H "Content-Type: application/json" \
  -H "x-portkey-provider: openai" \
  -H "x-portkey-custom-host: https://api.tokenlab.sh/v1" \
  -H "Authorization: Bearer sk-your-tokenlab-key" \
  -d '{
    "model": "gpt-5.6-luna",
    "messages": [{"role": "user", "content": "Say hello from TokenLab."}]
  }'
```

## الردود (Responses)

إذا كان إصدار Portkey لديك يدعم عنوان Responses مخصصاً، فاختر نموذجاً متاحاً يدعم Responses من [صفحة النماذج](https://tokenlab.sh/ar/models) واستخدم `/v1/responses`.

## ملاحظة حول نقطة النهاية الأصلية (Native Endpoint)

لا يمكن استخدام Anthropic Messages وGemini عبر Portkey إلا إذا كان إصدار Portkey لديك يدعم عناوين مخصصة لهاتين الصيغتين.