---
title: "Xiaomi (MiMo)"
description: "Xiaomi (MiMo) models available through TokenLab"
---

## Overview

TokenLab provides access to Xiaomi (MiMo) models. Total: **4 models** available.

## Chat Models

| Model ID | Pricing |
|----------|--------|
| `mimo-v2.5` | Input $0.14 / Output $0.28 per 1M tokens |
| `mimo-v2.5-pro` | Input $0.435 / Output $0.87 per 1M tokens |
| `mimo-v2.6-flash` | Input $0.14 / Output $0.28 per 1M tokens |
| `mimo-v2.6-pro` | Input $0.435 / Output $0.87 per 1M tokens |


## Usage

This example uses one model's declared request format. Other models on this page may accept different formats.

```python
import os
from openai import OpenAI

client = OpenAI(
    api_key=os.environ["TOKENLAB_API_KEY"],
    base_url="https://api.tokenlab.sh/v1"
)

response = client.chat.completions.create(
    model="mimo-v2.5",
    messages=[{"role": "user", "content": "Hello!"}]
)
```

Check the [model details](/api-reference/models/get-model) for `mimo-v2.5` with `GET /v1/models/mimo-v2.5` and your API key.

<Note>
The table shows current public pricing as input/output rates, a unit price, or a price range. See your dashboard for complete option-specific pricing.
</Note>
