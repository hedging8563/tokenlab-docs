---
title: "Hyper3D"
description: "Hyper3D generation models"
---

## Overview

TokenLab provides access to Hyper3D models. Total: **1 model** available.

## 3D Generation

| Model ID | Pricing |
|----------|--------|
| `hyper3d-gen2` | $0.75–$2.25/request |


## Usage

No supported request example was selected from the published format declarations. Check the [model details](/api-reference/models/get-model) for `hyper3d-gen2` with `GET /v1/models/hyper3d-gen2` and your API key. Its declared request endpoint is `/v1/3d/generations`.

See the [API Reference](/api-reference/introduction) for the declared endpoint's request shape. Category and provider names do not establish a request protocol.

<Note>
The table shows current public pricing as input/output rates, a unit price, or a price range. See your dashboard for complete option-specific pricing.
</Note>
