---
title: "Groq"
description: "Groq compound AI systems"
---

## Overview

TokenLab provides access to Groq models. Total: **2 models** available.

## Chat Models

| Model ID | Pricing |
|----------|--------|
| `groq-compound` | Input $0.59 / Output $0.79 per 1M tokens |
| `groq-compound-mini` | Input $0.05 / Output $0.08 per 1M tokens |


## Usage

This example uses one model's declared request format. Other models on this page may accept different formats.

```python
from openai import OpenAI

client = OpenAI(
    api_key="sk-your-tokenlab-key",
    base_url="https://api.tokenlab.sh/v1"
)

response = client.chat.completions.create(
    model="groq-compound",
    messages=[{"role": "user", "content": "Hello!"}]
)
```

Check the [model details](/api-reference/models/get-model) for `groq-compound` with `GET /v1/models/groq-compound` and your API key.

<Note>
The table shows current public pricing as input/output rates, a unit price, or a price range. See your dashboard for complete option-specific pricing.
</Note>
