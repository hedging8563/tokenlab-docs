---
title: "Image Tools"
description: "Image Tools models available through TokenLab"
---

## Overview

TokenLab provides access to Image Tools models. Total: **2 models** available.

## Image Generation

| Model ID | Pricing |
|----------|--------|
| `image-background-remover` | $0.008/request |
| `image-upscaler` | $0.004/request |


## Usage

No supported request example was selected from the published format declarations. Check the [model details](/api-reference/models/get-model) for `image-background-remover` with `GET /v1/models/image-background-remover` and your API key. Its declared request endpoint is `/v1/images/edits`.

See the [API Reference](/api-reference/introduction) for the declared endpoint's request shape. Category and provider names do not establish a request protocol.

<Note>
The table shows current public pricing as input/output rates, a unit price, or a price range. See your dashboard for complete option-specific pricing.
</Note>
