---
title: "Zhipu AI (GLM)"
description: "GLM-4, GLM-4.5, GLM-4.6 series"
---

## Overview

TokenLab provides access to Zhipu AI (GLM) models. Total: **7 models** available.

## Chat Models

| Model ID | Pricing |
|----------|--------|
| `glm-5` | Input $1 / Output $3.2 per 1M tokens |
| `glm-5-turbo` | Input $1.2 / Output $4 per 1M tokens |
| `glm-5.1` | Input $1.4 / Output $4.4 per 1M tokens |
| `glm-5.2` | Input $1.17647059 / Output $4.11764706 per 1M tokens |
| `glm-5.3` | Input $1.17647059 / Output $4.11764706 per 1M tokens |
| `glm-5.3-flash` | Input $0.15 / Output $0.5 per 1M tokens |
| `glm-5v-turbo` | Input $1.2 / Output $4 per 1M tokens |


## Usage

```python
from openai import OpenAI

client = OpenAI(
    api_key="sk-your-tokenlab-key",
    base_url="https://api.tokenlab.sh/v1"
)

response = client.chat.completions.create(
    model="glm-5",
    messages=[{"role": "user", "content": "Hello!"}]
)
```

<Note>
The table shows current public pricing as input/output rates, a unit price, or a price range. See your dashboard for complete option-specific pricing.
</Note>
