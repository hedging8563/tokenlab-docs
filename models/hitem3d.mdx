---
title: "Hitem3D"
description: "Hitem3D generation models"
---

## Overview

TokenLab provides access to Hitem3D models. Total: **1 model** available.

## 3D Generation

| Model ID | Pricing |
|----------|--------|
| `hitem3d-2.0` | $0.7–$0.9/request |


## Usage

No supported request example was selected from the published format declarations. Check the [model details](/api-reference/models/get-model) for `hitem3d-2.0` with `GET /v1/models/hitem3d-2.0` and your API key. Its declared request endpoint is `/v1/3d/generations`.

See the [API Reference](/api-reference/introduction) for the declared endpoint's request shape. Category and provider names do not establish a request protocol.

<Note>
The table shows current public pricing as input/output rates, a unit price, or a price range. See your dashboard for complete option-specific pricing.
</Note>
