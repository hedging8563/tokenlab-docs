---
title: "Suno"
description: "Suno models available through TokenLab"
---

## Overview

TokenLab provides access to Suno models. Total: **1 model** available.

## Music Generation

| Model ID | Pricing |
|----------|--------|
| `suno-music` | $0.1–$0.25/request |


## Usage

No supported request example was selected from the published format declarations. Check the [model details](/api-reference/models/get-model) for `suno-music` with `GET /v1/models/suno-music` and your API key. Its declared request endpoint is `/v1/music/generations`.

See the [API Reference](/api-reference/introduction) for the declared endpoint's request shape. Category and provider names do not establish a request protocol.

<Note>
The table shows current public pricing as input/output rates, a unit price, or a price range. See your dashboard for complete option-specific pricing.
</Note>
