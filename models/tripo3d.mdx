---
title: "Tripo3D"
description: "Text/Image to 3D models"
---

## Overview

TokenLab provides access to Tripo3D models. Total: **2 models** available.

## 3D Generation

| Model ID | Pricing |
|----------|--------|
| `tripo-h3.1` | $0.1–$0.4/request |
| `tripo-p1.0` | $0.3–$0.5/request |


## Usage

No supported request example was selected from the published format declarations. Check the [model details](/api-reference/models/get-model) for `tripo-h3.1` with `GET /v1/models/tripo-h3.1` and your API key. Its declared request endpoint is `/v1/3d/generations`.

See the [API Reference](/api-reference/introduction) for the declared endpoint's request shape. Category and provider names do not establish a request protocol.

<Note>
The table shows current public pricing as input/output rates, a unit price, or a price range. See your dashboard for complete option-specific pricing.
</Note>
