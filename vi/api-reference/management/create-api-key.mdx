---
title: "Tạo API key"
openapi: "POST /v1/management/api-keys"
description: "Tạo một API key người dùng mới cho tổ chức hiện tại."
# Synced from canonical OpenAPI x-mint.metadata.playground.
playground: simple
---

## Tổng Quan

Tạo một API key `type=user` mới trong tổ chức gắn với management token hiện tại.

## Thân Yêu Cầu

| Trường | Kiểu | Mặc định / Giới hạn | Ghi chú |
|-------|------|---------------------|------|
| `name` | string | tùy chọn, mặc định `Default Key`, độ dài `1-50` | Tên hiển thị, máy chủ sẽ loại bỏ khoảng trắng ở đầu và cuối |
| `limitAmount` | number \| string \| null | `0–100000` USD | `null` nghĩa là không giới hạn; `0` ngăn phát sinh chi phí. Chuỗi thập phân hỗ trợ tối đa 6 chữ số sau dấu thập phân. Khi tạo khóa, bỏ qua trường này nghĩa là không giới hạn. |
| `limitCurrency` | enum | mặc định `USD` | Chỉ hỗ trợ USD. Gửi `CNY` sẽ trả về `400 currency_retired`. |
| `models` | string[] | mặc định `[]` | Danh sách cho phép model tùy chọn |
| `deliveryPolicy` | string \| null | `auto`, `verified`, `official`, `null` | `null` kế thừa chính sách cung cấp của không gian làm việc. |
| `expiresAt` | string \| null | datetime RFC3339 | `null` nghĩa là không hết hạn |

## Ghi Chú

- Các trường tiền tệ chỉ hỗ trợ USD. Dữ liệu nhập `CNY` đã bị ngừng hỗ trợ và trả về `400 currency_retired`.

- Giá trị đầy đủ của API key chỉ hiển thị một lần trong response.
- `models: []` nghĩa là key không có hạn chế model bổ sung nào ngoài chính sách của tổ chức.
- `limitAmount: 0` tạo ra một key hợp lệ nhưng chưa thể chi tiêu quota cho đến khi được cập nhật.

## Ví Dụ

<RequestExample>
```bash cURL
curl -X POST "https://api.tokenlab.sh/v1/management/api-keys" \
  -H "Authorization: Bearer mt-your-management-token" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Backend Worker",
    "limitAmount": 500,
    "models": ["veo3.1", "kling-3.0-video"],
    "expiresAt": "2026-12-31T23:59:59Z"
  }'
```
</RequestExample>

## Yêu Cầu / Phản Hồi

Trang này cung cấp schema OpenAPI và các ví dụ yêu cầu có thể sao chép. Hãy gửi yêu cầu quản lý từ terminal hoặc ứng dụng khách bằng Management Token; trang này không hỗ trợ gửi trực tiếp.

## Ví dụ phản hồi

<ResponseExample>
```json 201 Created
{
  "id": "key_abc123def456",
  "name": "Backend Worker",
  "key": "sk-live-redacted",
  "key_prefix": "sk-live...",
  "status": "active",
  "limit_amount": 500,
  "limit_amount_decimal": "500",
  "used_amount": 0,
  "used_amount_decimal": "0",
  "models": [
    "veo3.1",
    "kling-3.0-video"
  ],
  "expires_at": "2026-12-31T23:59:59.000Z",
  "last_used_at": null,
  "created_at": "2026-07-08T03:00:00.000Z",
  "delivery_policy": null
}
```
</ResponseExample>

## Các trường quan trọng

<ResponseField name="key" type="string">Giá trị bí mật đầy đủ của khóa API. Giá trị này chỉ được trả về một lần khi tạo.</ResponseField>
<ResponseField name="id" type="string">Mã định danh khóa API dùng trong các lệnh gọi API Quản lý tiếp theo.</ResponseField>
<ResponseField name="name" type="string">Tên hiển thị của khóa API.</ResponseField>
<ResponseField name="key_prefix" type="string">Tiền tố khóa không bí mật để hiển thị và hỗ trợ.</ResponseField>
<ResponseField name="status" type="string">Một trong các giá trị `active`, `inactive`, `suspended` hoặc `revoked`.</ResponseField>
<ResponseField name="limit_amount" type="number | null">Hạn mức chi tiêu bằng USD dưới dạng số JSON để hiển thị. `null` nghĩa là không giới hạn.</ResponseField>
<ResponseField name="limit_amount_decimal" type="string | null">Hạn mức chi tiêu chính xác bằng USD dưới dạng chuỗi thập phân. `null` nghĩa là không giới hạn.</ResponseField>
<ResponseField name="used_amount" type="number">Chi phí sử dụng tích lũy bằng USD dưới dạng số JSON để hiển thị.</ResponseField>
<ResponseField name="used_amount_decimal" type="string">Chi phí sử dụng tích lũy chính xác bằng USD dưới dạng chuỗi thập phân.</ResponseField>
<ResponseField name="models" type="string[]">Danh sách mô hình được phép cho từng khóa. Mảng rỗng nghĩa là không có hạn chế mô hình bổ sung ở cấp khóa.</ResponseField>
<ResponseField name="delivery_policy" type="string | null">`auto`, `verified`, `official`, `null`. `null` kế thừa chính sách cung cấp của không gian làm việc.</ResponseField>
<ResponseField name="expires_at" type="string | null">Dấu thời gian ISO khi khóa hết hạn, hoặc `null` nếu không hết hạn.</ResponseField>
<ResponseField name="last_used_at" type="string | null">Dấu thời gian ISO của lần sử dụng gần nhất, hoặc `null` nếu chưa từng sử dụng.</ResponseField>
<ResponseField name="created_at" type="string">Dấu thời gian ISO khi tạo.</ResponseField>
