---
title: "Danh Sách Lô"
openapi: "/openapi/vi.json GET /v1/batches"
description: "Liệt kê các công việc lô cho tổ chức của khóa API hiện tại."
---

## Tổng Quan

Điểm cuối này là một phần của Batch API tương thích với OpenAI của TokenLab. Các công việc lô sử dụng các tệp JSONL đã tải lên, chạy không đồng bộ và có thể trả về các tệp đầu ra/lỗi sau đó.

## Ghi Chú

- Các tệp đầu vào lô phải sử dụng `purpose=batch`.
- `completion_window` hiện tại là `24h`.
- Thứ tự đầu ra không được đảm bảo; luôn khớp theo `custom_id`.
- Streaming không được hỗ trợ bên trong các mục lô.

## Ví Dụ

<RequestExample>
```bash cURL
curl -X GET "https://api.tokenlab.sh/v1/batches?limit=20" \
  -H "Authorization: Bearer sk-your-api-key"
```
</RequestExample>

## Yêu Cầu / Phản Hồi

Sử dụng bảng điều khiển OpenAPI tương tác ở trên để có sơ đồ chính xác.

## Mẹo Vận Hành

- Sử dụng `custom_id` cho việc đối chiếu không thay đổi ở phía hạ nguồn.
- Mong đợi `output_file_id` và `error_file_id` chỉ sau khi công nhân hoàn tất lô.

## Ví dụ phản hồi

<ResponseExample>
```json 200 OK
{
  "object": "list",
  "data": [{ "id": "batch_abc123", "object": "batch", "status": "completed" }],
  "first_id": "batch_abc123",
  "last_id": "batch_abc123",
  "has_more": false
}
```
</ResponseExample>

## Trường quan trọng

<ResponseField name="object" type="string">Loại object của phản hồi.</ResponseField>
<ResponseField name="data" type="array">Danh sách hoặc payload được trả về trong phản hồi.</ResponseField>
<ResponseField name="status" type="string">Trạng thái vòng đời hiện tại của tài nguyên.</ResponseField>
<ResponseField name="has_more" type="boolean">Có thêm kết quả khi `has_more` là true.</ResponseField>
