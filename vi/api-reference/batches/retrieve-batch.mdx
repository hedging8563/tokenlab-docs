---
title: "Lấy Lô"
openapi: "GET /v1/batches/{batch_id}"
description: "Lấy trạng thái hiện tại, số lượng và các tệp kết quả cho một lô."
---

## Tổng Quan

Điểm cuối này là một phần của API Lô tương thích với OpenAI của TokenLab. Các công việc lô sử dụng các tệp JSONL đã tải lên, chạy không đồng bộ và có thể trả lại các tệp đầu ra/lỗi sau.

## Ghi Chú

- Các tệp đầu vào lô phải sử dụng `purpose=batch`.
- `completion_window` hiện tại là `24h`.
- Thứ tự đầu ra không được đảm bảo; luôn khớp theo `custom_id`.
- Không hỗ trợ streaming bên trong các mục lô.

## Ví Dụ

<RequestExample>
```bash cURL
curl -X GET "https://api.tokenlab.sh/v1/batches/batch_abc123" \
  -H "Authorization: Bearer sk-your-api-key"
```
</RequestExample>

## Yêu Cầu / Phản Hồi

Sử dụng bảng điều khiển OpenAPI tương tác ở trên để có sơ đồ chính xác.

## Mẹo Vận Hành

- Sử dụng `custom_id` cho việc đối chiếu không thay đổi ở phía hạ nguồn.
- Mong đợi `output_file_id` và `error_file_id` chỉ sau khi công nhân hoàn tất lô.

## Ví dụ phản hồi

<ResponseExample>
```json 200 OK
{
  "id": "batch_abc123",
  "object": "batch",
  "status": "completed",
  "input_file_id": "file_abc123",
  "output_file_id": "file_out123",
  "error_file_id": "file_err123",
  "request_counts": { "total": 100, "completed": 98, "failed": 2 }
}
```
</ResponseExample>

## Trường quan trọng

<ResponseField name="request_counts" type="object">Số yêu cầu tổng cộng, hoàn tất, thất bại và bị hủy.</ResponseField>
<ResponseField name="output_file_id" type="string|null">Định danh tệp dùng bởi API liên quan.</ResponseField>
<ResponseField name="error_file_id" type="string|null">Định danh tệp dùng bởi API liên quan.</ResponseField>
