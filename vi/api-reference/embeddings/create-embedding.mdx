---
title: "Tạo Embedding"
openapi: "POST /v1/embeddings"
description: "Tạo một vector embedding đại diện cho văn bản đầu vào"
---

## Nội dung Request

Với đầu vào lớn, đặt thời gian chờ của máy khách HTTP ít nhất `120s`. Phản hồi trả về sau khi xử lý hoàn tất.

<ParamField body="model" type="string" required>
  ID của model embedding sẽ sử dụng (ví dụ: `text-embedding-3-small`).
</ParamField>

<ParamField body="input" type="string | array" required>
  Văn bản dưới dạng chuỗi hoặc mảng chuỗi. Mô hình hỗ trợ đầu vào token còn nhận `number[]` cho một chuỗi token hoặc `number[][]` cho nhiều chuỗi.
</ParamField>

<ParamField body="encoding_format" type="string" default="float">
  Định dạng cho embeddings: `float` hoặc `base64`.
</ParamField>

<ParamField body="dimensions" type="integer">
  Số chiều cho đầu ra (phụ thuộc vào model).
</ParamField>

<ParamField body="user" type="string">
  Một mã định danh duy nhất đại diện cho end-user của bạn để giám sát lạm dụng.
</ParamField>

## Các Model Khả Dụng

Dùng `GET /v1/models?recommended_for=embedding` để xem mô hình và số chiều hiện tại.

## Phản hồi

<ResponseField name="object" type="string">
  Luôn là `list`.
</ResponseField>

<ResponseField name="data" type="array">
  Mảng các đối tượng embedding.

  Mỗi đối tượng bao gồm:
  - `object` (string): `embedding`
  - `index` (integer): Chỉ mục trong mảng đầu vào
  - `embedding` (array | string): Vector số với `float`, hoặc chuỗi Base64 với `base64`.
</ResponseField>

<ResponseField name="model" type="string">
  Model được sử dụng.
</ResponseField>

<ResponseField name="usage" type="object">
  Mức sử dụng token với `prompt_tokens` và `total_tokens`.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST "https://api.tokenlab.sh/v1/embeddings" \
  -H "Authorization: Bearer sk-your-api-key" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "text-embedding-3-small",
    "input": "The quick brown fox jumps over the lazy dog"
  }'
```

```python Python
from openai import OpenAI

client = OpenAI(
    api_key="sk-your-api-key",
    base_url="https://api.tokenlab.sh/v1"
)

response = client.embeddings.create(
    model="text-embedding-3-small",
    input="The quick brown fox jumps over the lazy dog"
)

embedding = response.data[0].embedding
print(f"Embedding dimension: {len(embedding)}")
print(f"First 5 values: {embedding[:5]}")
```

```javascript JavaScript
import OpenAI from 'openai';

const client = new OpenAI({
  apiKey: 'sk-your-api-key',
  baseURL: 'https://api.tokenlab.sh/v1'
});

const response = await client.embeddings.create({
  model: 'text-embedding-3-small',
  input: 'The quick brown fox jumps over the lazy dog'
});

console.log(response.data[0].embedding.slice(0, 5));
```

```php PHP
<?php
$ch = curl_init('https://api.tokenlab.sh/v1/embeddings');

curl_setopt_array($ch, [
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_POST => true,
    CURLOPT_HTTPHEADER => [
        'Content-Type: application/json',
        'Authorization: Bearer sk-your-api-key'
    ],
    CURLOPT_POSTFIELDS => json_encode([
        'model' => 'text-embedding-3-small',
        'input' => 'The quick brown fox jumps over the lazy dog'
    ])
]);

$response = curl_exec($ch);
curl_close($ch);

$data = json_decode($response, true);
print_r(array_slice($data['data'][0]['embedding'], 0, 5));
```
</RequestExample>

<Note>
Vector trong ví dụ đã được rút ngắn để dễ đọc.
</Note>

<ResponseExample>
```json Response
{
  "object": "list",
  "data": [
    {
      "object": "embedding",
      "index": 0,
      "embedding": [0.0023, -0.0194, 0.0081]
    }
  ],
  "model": "text-embedding-3-small",
  "usage": {
    "prompt_tokens": 9,
    "total_tokens": 9
  }
}
```
</ResponseExample>

## Embeddings theo lô

```python
# Embed multiple texts at once
response = client.embeddings.create(
    model="text-embedding-3-small",
    input=[
        "First document text",
        "Second document text",
        "Third document text"
    ]
)

for i, data in enumerate(response.data):
    print(f"Document {i}: {len(data.embedding)} dimensions")
```
