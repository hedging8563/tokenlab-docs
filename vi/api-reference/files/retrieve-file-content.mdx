---
title: "Lấy Nội Dung Tệp"
openapi: "GET /v1/files/{file_id}/content"
description: "Tải xuống nội dung gốc của một tệp đã tải lên."
---

## Tổng Quan

Endpoint này chủ yếu phục vụ việc truy xuất nội dung tệp của Batch API tương thích với OpenAI của TokenLab.

Khi request bật chế độ tương thích Anthropic Files, cùng route này cũng có thể chuyển tiếp nội dung gốc đã lưu của tệp cho workspace đã gắn.


Khi không có header Anthropic, route này tải nội dung file batch hoặc proxy nội dung file ảnh/tham chiếu đã bind nếu provider hỗ trợ đọc nội dung file.
## Ghi Chú

- Các tệp đầu vào batch phải dùng `purpose=batch`.
- `completion_window` hiện là `24h`.
- Thứ tự đầu ra không được đảm bảo; luôn đối chiếu theo `custom_id`.
- Streaming không được hỗ trợ bên trong các mục batch.

## Ví Dụ

<RequestExample>
```bash cURL
curl -X GET "https://api.tokenlab.sh/v1/files/file_abc123/content" \
  -H "Authorization: Bearer sk-your-api-key" \
  --output batch-output.jsonl
```
</RequestExample>

## Yêu Cầu / Phản Hồi

Sử dụng bảng OpenAPI tương tác ở trên để xem schema chính xác.

## Mẹo Vận Hành

- Dùng `custom_id` cho việc đối soát downstream mang tính idempotent.
- Chỉ kỳ vọng `output_file_id` và `error_file_id` sau khi worker hoàn tất batch.
- Giá batch có thể khác với giá đồng bộ vì các quy tắc giảm giá `isBatchRequest=true` được áp dụng.

## Ví dụ phản hồi

<ResponseExample>
```jsonl 200 OK
{"custom_id":"request-1","response":{"status_code":200,"body":{"id":"chatcmpl_abc123"}}}
{"custom_id":"request-2","error":{"code":"invalid_request_error","message":"Invalid model"}}
```
</ResponseExample>

## Trường quan trọng

<ResponseField name="body" type="binary|string">Nội dung phản hồi thô. Lưu trực tiếp, không parse như JSON.</ResponseField>
<ResponseField name="custom_id" type="string">Định danh dùng cho lần gọi tiếp theo và hỗ trợ.</ResponseField>
