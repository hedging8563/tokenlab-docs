---
title: "Lấy Tệp"
openapi: "GET /v1/files/{file_id}"
description: "Lấy metadata cho một tệp đã tải lên."
---

## Tổng Quan

Endpoint này chủ yếu phục vụ các đối tượng tệp của Batch API tương thích với OpenAI của TokenLab.

Khi request bật chế độ tương thích Anthropic Files, cùng route này cũng có thể trả về metadata tệp kiểu Anthropic cho workspace đã gắn.


Khi không có header Anthropic, route này có thể trả về object file batch hoặc object file ảnh/tham chiếu TokenLab.
## Ghi Chú

- Các tệp đầu vào batch phải dùng `purpose=batch`.
- `completion_window` hiện là `24h`.
- Thứ tự đầu ra không được đảm bảo; luôn đối chiếu theo `custom_id`.
- Streaming không được hỗ trợ bên trong các mục batch.

## Ví Dụ

<RequestExample>
```bash cURL
curl -X GET "https://api.tokenlab.sh/v1/files/file_abc123" \
  -H "Authorization: Bearer sk-your-api-key"
```
</RequestExample>

## Yêu Cầu / Phản Hồi

Sử dụng bảng OpenAPI tương tác ở trên để xem schema chính xác.

## Mẹo Vận Hành

- Dùng `custom_id` cho việc đối soát downstream mang tính idempotent.
- Chỉ kỳ vọng `output_file_id` và `error_file_id` sau khi worker hoàn tất batch.
- Giá batch có thể khác với giá đồng bộ vì các quy tắc giảm giá `isBatchRequest=true` được áp dụng.

## Ví dụ phản hồi

<ResponseExample>
```json 200 OK
{
  "id": "file_abc123",
  "object": "file",
  "bytes": 32768,
  "created_at": 1706000000,
  "filename": "batch-input.jsonl",
  "purpose": "batch"
}
```
</ResponseExample>

## Trường quan trọng

<ResponseField name="id" type="string">Định danh tệp dùng bởi API liên quan.</ResponseField>
<ResponseField name="filename" type="string">Tên tệp gốc hoặc được tạo.</ResponseField>
<ResponseField name="purpose" type="string">Mục đích tệp được workflow sở hữu sử dụng, chẳng hạn `batch`, `batch_output`, `user_data` hoặc `vision`.</ResponseField>
