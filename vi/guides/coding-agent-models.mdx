---
title: "Lựa chọn mô hình cho Coding Agent"
description: "Chọn mô hình phù hợp cho từng tác vụ lập trình — bảng so sánh, đề xuất và cấu hình"
---

## So sánh mô hình cho lập trình

| Mô hình | Tốc độ | Chất lượng code | Chi phí | Cửa sổ ngữ cảnh | Tốt nhất cho |
|---------|--------|----------------|---------|-----------------|-------------|
| `claude-opus-4-6` | Trung bình | Xuất sắc | $$$$ | 200K | Kiến trúc, refactoring phức tạp |
| `gpt-5.4` | Trung bình | Xuất sắc | $$$$ | 200K | Suy luận phức tạp, lập kế hoạch |
| `claude-sonnet-4-6` | Nhanh | Rất tốt | $$$ | 200K | Lập trình chung, review |
| `gemini-3.1-pro-preview` | Nhanh | Rất tốt | $$$ | 1M | Phân tích codebase lớn |
| `gpt-5-mini` | Rất nhanh | Tốt | $$ | 128K | Chỉnh sửa nhanh, hoàn thành |
| `gemini-3.5-flash` | Rất nhanh | Tốt | $$ | 1M | Lặp nhanh, tìm kiếm |
| `deepseek-v4-pro` | Trung bình | Rất tốt | $ | 1M | Tác vụ nặng suy luận |
| `deepseek-v4-flash` | Nhanh | Tốt | $ | 1M | Tạo hàng loạt, boilerplate |


Voi nhom model moi huong toi lap trinh, hay can nhac `minimax-m3` cho cong viec agent voi ngu canh lon, `kimi-k2.7-code-highspeed` cho vong lap code nhanh, va `deepseek-v4-pro` / `deepseek-v4-flash`, `glm-5.2`, `step-3.7-flash`, `mimo-v2.5-pro` cho dinh tuyen chat tuong thich OpenAI.

## Đề xuất theo tác vụ

<AccordionGroup>
  <Accordion title="Tạo code / Scaffolding">
    **Đề xuất**: `claude-sonnet-4-6`, `gemini-3.1-pro-preview`

    Code sạch, cấu trúc tốt. Scaffolding đa file phức tạp: dùng `claude-opus-4-6`.

    Thay thế tiết kiệm: `deepseek-v4-flash` xử lý boilerplate với chi phí thấp.
  </Accordion>

  <Accordion title="Review code / Refactoring">
    **Đề xuất**: `claude-sonnet-4-6`, `deepseek-v4-pro`

    `deepseek-v4-pro` đặc biệt hiệu quả về chi phí — cách tiếp cận chain-of-thought phát hiện vấn đề tinh vi.
  </Accordion>

  <Accordion title="Sửa bug / Debug">
    **Đề xuất**: `claude-sonnet-4-6`, `gpt-5-mini`

    Sửa bug thường có phạm vi rõ ràng. Mô hình tiêu chuẩn là đủ.
  </Accordion>

  <Accordion title="Thiết kế kiến trúc">
    **Đề xuất**: `claude-opus-4-6`, `gpt-5.4`

    Quyết định kiến trúc cần khả năng suy luận mạnh nhất. Ít xảy ra nhưng tác động lớn.
  </Accordion>

  <Accordion title="Chỉnh sửa nhanh / Tab completion">
    **Đề xuất**: `gpt-5-mini`, `gemini-3.5-flash`

    Tốc độ là yếu tố quan trọng nhất cho sử dụng tương tác.
  </Accordion>
</AccordionGroup>

## Cách TokenLab chọn nhà cung cấp

### Chiến lược lựa chọn

| Chiến lược | Hành vi | Tốt nhất cho |
|-----------|---------|-------------|
| **PRIORITY** | Dùng nhà cung cấp khả dụng có ưu tiên cao nhất | Độ tin cậy |
| **COST** | Dùng nhà cung cấp khả dụng có chi phí thấp nhất | Tối ưu chi phí |

### Tự động thử lại

```
Yêu cầu: claude-sonnet-4-6
  → Provider A (chính): lỗi 503
  → Provider B (thử lại): ✓ Thành công
```

### Định dạng API gốc

Khi model đã chọn hỗ trợ định dạng API gốc, TokenLab có thể dùng định dạng đó để tương thích tốt hơn:

| Họ mô hình | Định dạng API | URL cơ sở |
|-------------|------------|----------|
| Claude | Anthropic Messages | `https://api.tokenlab.sh` |
| GPT | OpenAI Responses | `https://api.tokenlab.sh/v1` |
| Gemini | Gemini Native API | `https://api.tokenlab.sh` hoặc `https://api.tokenlab.sh/v1beta` tùy theo đường dẫn của client |
| DeepSeek | OpenAI Chat | `https://api.tokenlab.sh/v1` |

Claude Code là một đường Anthropic-native mạnh. Các API Gemini-native cũng tồn tại, nhưng bản thân Gemini CLI vẫn chỉ là một đường tương thích best-effort và không nên được xem là quy trình ổn định lâu dài.

## Cấu hình theo công cụ

<AccordionGroup>
  <Accordion title="Claude Code">
    ```bash
    export ANTHROPIC_API_KEY="sk-your-tokenlab-key"
    export ANTHROPIC_BASE_URL="https://api.tokenlab.sh"
    ```
    Chọn mô hình: `claude --model claude-sonnet-4-6`

    [Hướng dẫn đầy đủ →](/vi/integrations/claude-code)
  </Accordion>

  <Accordion title="Cursor">
    Settings → Models:
    - API Key: `sk-your-tokenlab-key`
    - Base URL: `https://api.tokenlab.sh/v1`

    [Hướng dẫn đầy đủ →](/vi/integrations/cursor)
  </Accordion>

  <Accordion title="Codex CLI">
    ```bash
    export OPENAI_API_KEY="sk-your-tokenlab-key"
    export OPENAI_BASE_URL="https://api.tokenlab.sh/v1"
    ```
    [Hướng dẫn đầy đủ →](/vi/integrations/codex-cli)
  </Accordion>

  <Accordion title="Gemini CLI">
    ```bash
    export GEMINI_API_KEY="sk-your-tokenlab-key"
    export GOOGLE_GEMINI_BASE_URL="https://api.tokenlab.sh"
    ```

    Hãy coi đây là đường tương thích best-effort chứ không phải tích hợp Gemini CLI chính thức.
    [Hướng dẫn đầy đủ →](/vi/integrations/gemini-cli)
  </Accordion>

  <Accordion title="OpenCode">
    ```json
    {
      "provider": "openai",
      "apiKey": "sk-your-tokenlab-key",
      "baseURL": "https://api.tokenlab.sh/v1"
    }
    ```
    [Hướng dẫn đầy đủ →](/vi/integrations/opencode)
  </Accordion>
</AccordionGroup>
