---
title: "Dify"
description: "Intégrez TokenLab avec Dify en utilisant le fournisseur OpenAI et une URL de base personnalisée"
---

## Aperçu

Dify fonctionne bien avec TokenLab grâce à son flux de fournisseur de modèles compatible avec OpenAI.

Il s'agit d'un chemin d'intégration orienté vers les complétions de chat. Cela ne doit pas être interprété comme une garantie que Dify expose les mêmes réponses ou le même comportement WebSocket que les intégrations Codex dédiées.

Pour les versions actuelles de Dify, le chemin le plus sûr est généralement le suivant :
- choisir le fournisseur **OpenAI** intégré
- définir votre clé API TokenLab
- définir une URL de base personnalisée `https://api.tokenlab.sh/v1`

<Note>
Certaines versions plus anciennes de Dify exposent cela en tant que `OpenAI-API-compatible` au lieu du fournisseur `OpenAI` intégré avec un champ d'URL de base personnalisée. Si votre interface Dify semble différente, utilisez le flux de fournisseur personnalisé compatible OpenAI le plus proche disponible dans cette version.
</Note>

<Note>
**Type** : Framework ou Plateforme

**Chemin principal** : Chemin de chat compatible OpenAI

**Niveau de support** : Supporté avec des limites de périmètre
</Note>

## Prérequis

- Compte TokenLab avec accès API
- Dify Cloud ou Dify auto-hébergé

## Étapes de configuration

### Étape 1 : Obtenir votre clé API

1. Connectez-vous au [Tableau de bord TokenLab](https://tokenlab.sh/dashboard)
2. Ouvrez [Clés API](https://tokenlab.sh/dashboard/api)
3. Créez ou copiez une clé API commençant par `sk-`

### Étape 2 : Configurer le fournisseur

<Steps>
  <Step title="Ouvrir les fournisseurs de modèles">
    Dans Dify, allez dans **Paramètres** → **Fournisseurs de modèles**
  </Step>
  <Step title="Sélectionner OpenAI">
    Ouvrez les paramètres du fournisseur **OpenAI**. Si votre version de Dify ne propose pas d'URL de base personnalisée ici, utilisez plutôt l'option de fournisseur personnalisé compatible OpenAI exposée par cette version.
  </Step>
  <Step title="Saisir les paramètres TokenLab">
    Utilisez ces valeurs :

    | Champ | Valeur |
    |-------|-------|
    | Clé API | `sk-your-tokenlab-key` |
    | URL de base API / URL de base personnalisée | `https://api.tokenlab.sh/v1` |
  </Step>
  <Step title="Ajouter des modèles">
    Ajoutez les modèles que vous souhaitez utiliser, par exemple :
    - `gpt-5.4`
    - `gpt-5.4-mini`
    - `claude-sonnet-5`
    - `claude-opus-4-8`
    - `gemini-3.5-flash`
    - `gemini-3.1-flash-lite`
    - `deepseek-v4-flash`
  </Step>
</Steps>

### Étape 3 : Tester la connexion

1. Choisissez un modèle tel que `gpt-5-mini` ou `gpt-4o`
2. Envoyez un prompt de test
3. Confirmez que Dify reçoit une réponse valide

## Embeddings pour les bases de connaissances

Pour le RAG et l'indexation de bases de connaissances, ajoutez un modèle d'embedding tel que :
- `text-embedding-3-small`
- `text-embedding-3-large`

Ensuite, définissez-le comme modèle d'embedding par défaut dans les paramètres de la base de connaissances ou de l'application concernée.

## Rôles de modèles recommandés

| Cas d'utilisation | Modèles suggérés |
|----------|------------------|
| Chat par défaut | `gpt-5.4-mini`, `gpt-5.4`, `claude-sonnet-5` |
| Raisonnement approfondi | `gpt-5.5`, `gpt-5.4`, `claude-opus-4-8`, `deepseek-v4-pro` |
| Rapide/économique | `gpt-5.4-mini`, `gemini-3.5-flash`, `deepseek-v4-flash` |
| Embeddings | `text-embedding-3-small`, `text-embedding-3-large` |

## Bonnes pratiques

<AccordionGroup>
  <Accordion title="Commencez avec le fournisseur OpenAI intégré">
    Dans les versions plus récentes de Dify, le fournisseur OpenAI intégré avec une URL de base personnalisée est généralement la configuration la plus propre pour TokenLab.
  </Accordion>

  <Accordion title="Utilisez des modèles moins coûteux pour l'itération">
    Utilisez `gpt-5.4-mini`, `gemini-3.5-flash` ou `deepseek-v4-flash` pendant vos itérations, puis basculez les flux de travail plus lourds vers des modèles plus puissants uniquement lorsque nécessaire.
  </Accordion>

  <Accordion title="N'oubliez pas que Dify est généralement orienté vers les complétions de chat">
    La plupart des flux Dify utilisent un comportement de chat compatible OpenAI. Si vous avez besoin de réponses spécifiques à Codex ou d'un comportement WebSocket, utilisez plutôt les intégrations Codex dédiées.
  </Accordion>
</AccordionGroup>

## Dépannage

<AccordionGroup>
  <Accordion title="Erreur de connexion">
    - Vérifiez que l'URL de base est exactement `https://api.tokenlab.sh/v1`
    - Supprimez les barres obliques (slashes) finales si Dify les duplique
    - Confirmez que le serveur Dify peut atteindre TokenLab via l'internet public
  </Accordion>

  <Accordion title="401 Non autorisé">
    - Vérifiez à nouveau la clé API
    - Confirmez que la clé est active dans le tableau de bord TokenLab
    - Assurez-vous que la valeur collée dans Dify ne contient pas d'espaces ou de sauts de ligne supplémentaires
  </Accordion>

  <Accordion title="Modèle introuvable">
    - Vérifiez exactement le nom du modèle
    - Ajoutez à nouveau l'entrée du modèle si l'interface du fournisseur a mis en cache une ancienne valeur
    - Vérifiez la disponibilité actuelle du modèle dans la documentation ou le tableau de bord TokenLab
  </Accordion>
</AccordionGroup>