---
title: "LibreChat"
description: "Configurez LibreChat pour utiliser TokenLab en tant que point de terminaison personnalisé compatible avec OpenAI"
---

## Aperçu

LibreChat fonctionne parfaitement avec TokenLab via un point de terminaison personnalisé compatible avec OpenAI.

<Note>
**Type** : Application de chat

**Chemin principal** : Complétions de chat compatibles avec OpenAI

**Niveau de prise en charge** : Chemin pris en charge
</Note>

## Environnement

```bash
TOKENLAB_API_KEY=sk-your-tokenlab-key
```

## Exemple de point de terminaison

Ajoutez TokenLab à votre configuration de point de terminaison LibreChat :

```yaml
customEndpoints:
  - name: 'TokenLab'
    apiKey: '${TOKENLAB_API_KEY}'
    baseURL: 'https://api.tokenlab.sh/v1'
    models:
      default:
        - 'gpt-5.5'
        - 'gpt-5.4'
        - 'gpt-5.4-mini'
        - 'claude-opus-4-8'
        - 'claude-sonnet-5'
        - 'gemini-3.5-flash'
        - 'deepseek-v4-pro'
        - 'deepseek-v4-flash'
      fetch: true
    titleConvo: true
    titleModel: 'gpt-5.4-mini'
    modelDisplayLabel: 'TokenLab'
```

## Remarques

LibreChat utilise le chemin de chat compatible avec OpenAI pour cette configuration. TokenLab expose également des points de terminaison natifs Responses, Anthropic Messages et compatibles avec Gemini pour les clients qui prennent en charge ces formats de requête.