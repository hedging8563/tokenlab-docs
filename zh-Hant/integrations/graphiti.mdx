---
title: "Graphiti"
description: "使用 TokenLab 作為與 OpenAI 相容的 Graphiti LLM 提供者"
---

## 概覽

Graphiti 透過 `OpenAIGenericClient` 支援與 OpenAI 相容的 `/v1` 端點。將該客戶端指向 TokenLab 並選擇一個 TokenLab 模型即可使用。

<Note>
**類型**: 知識圖譜記憶框架

**主要路徑**: 與 OpenAI 相容的 Chat Completions

**支援信心**: 支援與 OpenAI 相容的路徑
</Note>

## 環境

```bash
export TOKENLAB_API_KEY="sk-your-tokenlab-key"
```

## LLM 客戶端

```python
import os

from graphiti_core.llm_client.config import LLMConfig
from graphiti_core.llm_client.openai_generic_client import OpenAIGenericClient

llm_config = LLMConfig(
    api_key=os.environ["TOKENLAB_API_KEY"],
    model="claude-sonnet-5",
    small_model="gpt-5.4-mini",
    base_url="https://api.tokenlab.sh/v1",
)

llm_client = OpenAIGenericClient(config=llm_config)
```

將 `llm_client` 與您的圖資料庫及嵌入器 (embedder) 設定一同傳入 `Graphiti(...)` 實例中。

## 端點注意事項

Graphiti 與 OpenAI 相容的路徑是以 chat-completions 為導向的。請使用能穩定回傳結構化 JSON 以進行提取與去重任務的模型。