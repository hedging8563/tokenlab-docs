---
title: "取得視覺驗證結果"
openapi: "/openapi/zh-Hant.json POST /api/v3"
description: "取得視覺驗證產生的真人素材群組。"
---

H5 視覺驗證流程結束後輪詢此 Action。成功結果會回傳已驗證人員對應的 TokenLab 素材群組 ID。

查詢時須使用建立會話的同一組織、使用者，以及相同且區分大小寫的 `ProjectName`。

## 端點與驗證

```text
POST https://api.tokenlab.sh/api/v3?Action=GetVisualValidateResult&Version=2024-01-01
Authorization: Bearer <TOKENLAB_API_KEY>
Content-Type: application/json
```

傳送 [建立視覺驗證會話](/zh-Hant/api-reference/video/create-visual-validation-session) 回傳的 `BytedToken`。若建立時設定了 `ProjectName`，此處必須傳送相同且區分大小寫的值；否則可省略或使用 `default`。

瀏覽器回呼後，結果可能不會立即就緒。請重試相同請求，直到回傳 `Result.GroupId`，或有效期 30 分鐘的 `BytedToken` 過期。使用相同 token 重複成功請求會回傳相同的對應 `GroupId`。

建立 `liveness_face` 素材時，請將回傳的 `GroupId` 作為 `group_id`。

## 請求範例

```bash
curl 'https://api.tokenlab.sh/api/v3?Action=GetVisualValidateResult&Version=2024-01-01' \
  -H "Authorization: Bearer $TOKENLAB_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"BytedToken":"ZXhhbXBsZS10b2tlbg","ProjectName":"default"}'
```

## 回應範例

```json
{
  "ResponseMetadata": {
    "RequestId": "req_def456",
    "Action": "GetVisualValidateResult",
    "Version": "2024-01-01",
    "Service": "ark",
    "Region": "cn-beijing"
  },
  "Result": {
    "GroupId": "group-20260720123456-real1"
  }
}
```

錯誤使用 `ResponseMetadata.Error.Code` 與 `ResponseMetadata.Error.Message`。成功回應包含 `ResponseMetadata` 與 `Result`，不使用 TokenLab 其他 `/v1` API 的回應結構。
