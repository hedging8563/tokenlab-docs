---
title: "取得素材資源群組"
openapi: "GET /v1/videos/assets/groups/{groupId}"
description: "透過 TokenLab 群組 ID 檢索單一 Seedance 素材群組。"
---

檢索單一素材群組。`library_type` 會告知該群組是虛擬人像（virtual-avatar）群組還是真人（real-person）群組。

如果您需要在上傳前確認該群組仍然存在且狀態為 `status: "ACTIVE"`，請使用此端點。

`project_name` 用於同一組織內的資源隔離，預設為 `default`。相關素材群組和素材須使用相同且區分大小寫的值；POST/PUT 在 JSON 請求主體中傳入，GET/DELETE 在查詢參數中傳入。

## 請求範例

```bash
curl "https://api.tokenlab.sh/v1/videos/assets/groups/group-20260720123456-vrt01?project_name=default" \
  -H "Authorization: Bearer $TOKENLAB_API_KEY"
```

## 回應範例

```json
{
  "id": "group-20260720123456-vrt01",
  "object": "seedance.material_asset_group",
  "project_name": "default",
  "library_type": "aigc_avatar",
  "group_name": "Product references",
  "description": "Reusable product shots",
  "status": "ACTIVE",
  "metadata": null,
  "created_at": 1782998400000
}
```
