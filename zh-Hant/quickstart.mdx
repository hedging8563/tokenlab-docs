---
title: "快速上手"
description: "在 2 分鐘內開始使用 TokenLab API"
---

## 第 1 步：從免費試用開始

<Steps>
  <Step title="建立帳戶">
    使用您的電子郵件、Gmail 或 GitHub 帳戶在 [tokenlab.sh](https://tokenlab.sh) 註冊。
  </Step>
  <Step title="確認工作區餘額">
    在[帳單](https://tokenlab.sh/dashboard/billing)查看可用餘額和已入帳的試用額度。此請求按所選模型目前價格計費，必要時先儲值。
  </Step>
  <Step title="建立並保存 API 金鑰">
    開啟目前工作區的 [API 金鑰](https://tokenlab.sh/dashboard/api?tab=keys)，建立金鑰後立即保存完整值；完整金鑰只顯示一次。如果已保存完整金鑰，請使用保存的值。清單中顯示或複製的前綴不能用於請求驗證。
  </Step>
</Steps>

<Warning>
請妥善保管您的 API 金鑰。切勿在客戶端程式碼或公開的儲存庫中暴露它。
</Warning>

## 為目前終端機設定金鑰

以下命令不會把金鑰寫入命令本身，僅對目前終端機及其子程序生效。部署應用程式時請使用秘密管理工具。

<CodeGroup>
```bash macOS / Linux / WSL
printf 'TokenLab API key: '
read -r -s TOKENLAB_API_KEY
printf '\n'
export TOKENLAB_API_KEY
```

```powershell Windows PowerShell
$secret = Read-Host 'TokenLab API key' -AsSecureString
$env:TOKENLAB_API_KEY = [System.Net.NetworkCredential]::new('', $secret).Password
Remove-Variable secret
```
</CodeGroup>

## 第 2 步：安裝客戶端

<CodeGroup>

```bash Python
pip install openai
```

```bash JavaScript
npm install openai
```

```bash Go
go mod init tokenlab-quickstart
go get github.com/openai/openai-go/v3
```

```bash PHP
composer require openai-php/client guzzlehttp/guzzle
```

</CodeGroup>

## 第 3 步：送出您的第一個請求

對於大多數新的整合，請從 **Chat Completions（聊天補全）** 與 `POST /v1/chat/completions` 開始。

<CodeGroup>

```bash cURL — macOS / Linux / WSL
curl https://api.tokenlab.sh/v1/chat/completions \
  -H "Authorization: Bearer $TOKENLAB_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"model":"gpt-5.6-terra","messages":[{"role":"user","content":"Reply only with OK."}]}'
```

```powershell Windows PowerShell
$body = @{
  model = 'gpt-5.6-terra'
  messages = @(@{ role = 'user'; content = 'Reply only with OK.' })
} | ConvertTo-Json -Depth 6
$result = Invoke-RestMethod -Method Post `
  -Uri 'https://api.tokenlab.sh/v1/chat/completions' `
  -Headers @{ Authorization = "Bearer $env:TOKENLAB_API_KEY" } `
  -ContentType 'application/json' -Body $body -TimeoutSec 30
$result.choices[0].message.content
```

```python Python — quickstart.py
import os
from openai import OpenAI

with OpenAI(
    api_key=os.environ["TOKENLAB_API_KEY"],
    base_url="https://api.tokenlab.sh/v1",
    timeout=30.0,
    max_retries=0,
) as client:
    response = client.chat.completions.create(
        model="gpt-5.6-terra",
        messages=[{"role": "user", "content": "Reply only with OK."}],
    )
    print(response.choices[0].message.content)
```

```javascript JavaScript — quickstart.mjs
import OpenAI from 'openai';

const client = new OpenAI({
  apiKey: process.env.TOKENLAB_API_KEY,
  baseURL: 'https://api.tokenlab.sh/v1',
  timeout: 30_000,
  maxRetries: 0,
});
const response = await client.chat.completions.create({
  model: 'gpt-5.6-terra',
  messages: [{ role: 'user', content: 'Reply only with OK.' }],
});
console.log(response.choices[0].message.content);
```

```go Go — main.go
package main

import (
    "context"
    "fmt"
    "os"
    "time"

    "github.com/openai/openai-go/v3"
    "github.com/openai/openai-go/v3/option"
)

func main() {
    client := openai.NewClient(
        option.WithAPIKey(os.Getenv("TOKENLAB_API_KEY")),
        option.WithBaseURL("https://api.tokenlab.sh/v1"),
        option.WithMaxRetries(0),
    )
    ctx, cancel := context.WithTimeout(context.Background(), 30*time.Second)
    defer cancel()
    response, err := client.Chat.Completions.New(ctx, openai.ChatCompletionNewParams{
        Model: "gpt-5.6-terra",
        Messages: []openai.ChatCompletionMessageParamUnion{
            openai.UserMessage("Reply only with OK."),
        },
    })
    if err != nil { panic(err) }
    fmt.Println(response.Choices[0].Message.Content)
}
```

```php PHP — quickstart.php
<?php
require __DIR__ . '/vendor/autoload.php';

$client = OpenAI::factory()
    ->withApiKey(getenv('TOKENLAB_API_KEY'))
    ->withBaseUri('https://api.tokenlab.sh/v1')
    ->withHttpClient(new \GuzzleHttp\Client(['timeout' => 30]))
    ->make();
$response = $client->chat()->create([
    'model' => 'gpt-5.6-terra',
    'messages' => [['role' => 'user', 'content' => 'Reply only with OK.']],
]);
echo $response->choices[0]->message->content . PHP_EOL;
```

</CodeGroup>

選擇一種 SDK，將範例存為 `quickstart.py`、`quickstart.mjs`、`main.go` 或 `quickstart.php`，再執行 `python quickstart.py`、`node quickstart.mjs`、`go run .` 或 `php quickstart.php`。Python、JavaScript 和 Go 使用 OpenAI 官方 SDK；PHP 使用社群維護的 [OpenAI PHP 用戶端](https://github.com/openai-php/client)，需要 PHP 8.2+。

<Note>
只有在明確需要 **Responses（回應）** 特定行為時才使用 `POST /v1/responses`。某些僅限 **Responses（回應）** 的欄位依賴於所選模型與路由路徑。
</Note>

## 核對結果

成功請求應回傳非空回答。在同一工作區的[請求記錄](https://tokenlab.sh/dashboard/runs)核對模型、狀態及用量。錯誤處理見[疑難排解](/zh-Hant/guides/observability-troubleshooting)；切換模型及串流回應見 [SDK 指南](/zh-Hant/integrations/openai-sdk)。

## 接下來要做什麼？

<CardGroup cols={2}>
  <Card title="驗證" icon="key" href="/authentication">
    了解 API 金鑰管理與安全性。
  </Card>
  <Card title="OpenAI SDK" icon="code" href="/integrations/openai-sdk">
    用已有 OpenAI SDK 接入 OpenAI 相容的 `/v1` 路由。
  </Card>
  <Card title="API 參考" icon="book" href="/api-reference/introduction">
    瀏覽完整的端點參考文件。
  </Card>
  <Card title="模型" icon="robot" href="https://tokenlab.sh/zh-TW/models">
    瀏覽目前的模型可用性與價格。
  </Card>
</CardGroup>
