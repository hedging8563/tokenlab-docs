---
title: "介紹"
description: "透過統一的 API 存取數百個 AI 模型"
---

## 什麼是 TokenLab？

TokenLab 是一個 AI API 聚合平台，使您能夠統一存取來自 OpenAI、Anthropic、Google、DeepSeek、Moonshot、MiniMax 等供應商的 **數百個 AI 模型**。

<CardGroup cols={2}>
  <Card title="原生 + 相容" icon="plug">
    需要供應商原生行為時使用 Anthropic/Gemini 原生路由；已有 OpenAI 風格工具時使用 OpenAI 相容的 `/v1` 路由。
  </Card>
  <Card title="原生格式" icon="shuffle">
    在同一平台上調用 OpenAI、Anthropic、Gemini 和 Responses 風格的端點。
  </Card>
  <Card title="按用量付費" icon="coins">
    無訂閱制。僅為您實際使用的部分付費。
  </Card>
  <Card title="高可用性" icon="shield-check">
    智慧路由、故障轉移與多通道支援提升可靠性。
  </Card>
</CardGroup>

## 支援的能力

公開模型目錄會隨時間變化。請用 `/v1/models`、`recommended_for` 或模型頁確認目前可用性；下方範例使用目前公開模型 ID。

| 能力 | 說明 | 範例模型 |
|---|---|---|
| 聊天與推理 | 文字生成、規劃、工具呼叫 | `gpt-5.4`, `claude-sonnet-4-6`, `deepseek-r1` |
| 視覺與多模態 | 圖像理解與多模態對話 | `gpt-4o`, `claude-sonnet-4-6`, `gemini-3.5-flash` |
| 圖像生成與編輯 | 文生圖與圖像編輯 | `gpt-image-2`, `flux-pro`, `qwen-image-plus` |
| 影片生成 | 文生影片、圖生影片、參考影片與動作控制 | `veo3.1`, `wan-2.7`, `kling-3.0-video`, `happyhorse-1.0` |
| 音訊與 Realtime WebSocket | TTS、轉寫、音訊翻譯和即時 WebSocket 會話 | `gpt-4o-mini-tts`, `whisper-1`, `gpt-realtime` |
| 嵌入與 rerank | 文字向量化與重排序 | `text-embedding-3-small`, `qwen3-vl-rerank` |
| 翻譯 | 文字翻譯和 WebSocket 語音翻譯流程 | `doubao-seed-translation`, `gpt-realtime-translate` |
| 音樂與 3D | 音樂和 3D 資產生成 | `suno-music`, `tripo-h3.1` |
| Worlds | World 生成、狀態輪詢和媒體資產流程 | 使用 `/v1/worlds/generations` 與 Models API 確認目前可用性 |

## 先選擇請求格式

從你需要的行為出發選擇路由。需要供應商原生能力時使用 Anthropic/Gemini 原生路由；遷移已有 OpenAI 風格聊天程式碼時，使用 OpenAI 相容的 `/v1/chat/completions`。

<CodeGroup>

```bash cURL
curl https://api.tokenlab.sh/v1/chat/completions \
  -H "Authorization: Bearer sk-your-api-key" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-5.4",
    "messages": [
      {"role": "user", "content": "Explain what TokenLab does in one sentence."}
    ]
  }'
```

```python Python
from openai import OpenAI

client = OpenAI(
    api_key="sk-your-api-key",
    base_url="https://api.tokenlab.sh/v1"
)

response = client.chat.completions.create(
    model="gpt-5.4",
    messages=[{"role": "user", "content": "Explain what TokenLab does in one sentence."}]
)

print(response.choices[0].message.content)
```

```javascript JavaScript
import OpenAI from 'openai';

const client = new OpenAI({
  apiKey: 'sk-your-api-key',
  baseURL: 'https://api.tokenlab.sh/v1'
});

const response = await client.chat.completions.create({
  model: 'gpt-5.4',
  messages: [{ role: 'user', content: 'Explain what TokenLab does in one sentence.' }],
});

console.log(response.choices[0].message.content);
```

<Note>
只有在您明確需要 Responses 特有行為時才使用 `POST /v1/responses`。這並非所有模型的預設相容路徑。
</Note>

</CodeGroup>

## 關鍵功能

<CardGroup cols={2}>
  <Card title="多種 API 格式" icon="layers" href="/guides/api-formats">
    使用一個帳戶即可使用 OpenAI、Anthropic、Gemini 或 Responses 原生格式。
  </Card>
  <Card title="以 Agent 為先的錯誤" icon="wand-magic-sparkles" href="/guides/agent-first-api">
    結構化提示幫助程式代理更快自我修正。
  </Card>
  <Card title="整合涵蓋範圍" icon="toolbox" href="/guides/ide-sdk-compatibility">
    以較少的供應商特定修改連接 IDE、SDK、框架和聊天應用。
  </Card>
</CardGroup>

## 後續步驟

<CardGroup cols={2}>
  <Card title="快速上手" icon="rocket" href="/quickstart">
    在幾分鐘內讓您的第一個請求運作起來。
  </Card>
  <Card title="API 參考" icon="book" href="/api-reference/introduction">
    查看逐端點的參考文件。
  </Card>
  <Card title="整合" icon="puzzle-piece" href="/integrations/openai-sdk">
    從 SDK、IDE 與應用程式整合開始。
  </Card>
  <Card title="模型" icon="robot" href="https://tokenlab.sh/zh-TW/models">
    瀏覽目前的模型可用性與定價。
  </Card>
</CardGroup>
