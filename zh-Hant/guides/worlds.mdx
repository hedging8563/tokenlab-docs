---
title: "Worlds"
description: "生成可探索的 Worlds、上傳媒體提示、輪詢任務並管理已完成的 World。"
---

World 生成會非同步建立可探索的 Marble World。當輸出是使用者可以開啟或再次訪問的環境時使用 Worlds；如果需要可下載的單獨模型資產，請使用 3D 生成。

## 輸入與媒體資產

World 任務可根據所選模型和請求形態從文字、圖片、多圖或影片開始。可重用的媒體提示應先準備媒體資產，再在 World 請求中引用。

媒體與世界記錄依 API Key 所屬組織檢查存取權。分享世界前，請核對所選模型的 `permission` 選項；傳回的素材或檢視連結可能有獨立存取規則。

## 建立與輪詢

建立響應包含公共任務 ID，通常也包含 poll_url。展示進度前先保存這個 ID，然後跟隨 poll_url 或 GET /v1/tasks/{id}，直到任務 completed 或 failed。

```bash
curl -X POST "https://api.tokenlab.sh/v1/worlds/generations" \
  -H "Authorization: Bearer sk-your-api-key" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "marble-1.1",
    "prompt": "A quiet coastal town at sunset with narrow alleys and warm lights"
  }'
```

## 管理已完成的 Worlds

Worlds 列表和詳情端點用於查看目前帳戶裡已完成的 World。待處理或失敗的生成任務請使用任務狀態端點。只有使用者明確刪除產品裡的生成 World 時才呼叫刪除。

## 常見問題

- 不要假設每個 World 模型都支援所有輸入類型。
- 簽名圖片或影片 URL 要保持足夠長的有效期，覆蓋服務端拉取重試。
- 逾時後重試前，先保存並查詢已返回的任務 ID。
- 需要可下載模型檔案時使用 3D 生成，而不是 Worlds。

## API 參考

| 主題 | 參考 |
|-------|-----------|
| 建立 World | [建立 World](/zh-Hant/api-reference/worlds/create-world) |
| 取得 World 狀態 | [取得 World 狀態](/zh-Hant/api-reference/worlds/get-world-status) |
| 準備媒體資產上傳 | [準備媒體資產上傳](/zh-Hant/api-reference/worlds/prepare-media-asset-upload) |
| 取得媒體資產 | [取得媒體資產](/zh-Hant/api-reference/worlds/get-media-asset) |
| 列出 Worlds | [列出 Worlds](/zh-Hant/api-reference/worlds/list-worlds) |
| 取得 World | [取得 World](/zh-Hant/api-reference/worlds/get-world) |
| 刪除 World | [刪除 World](/zh-Hant/api-reference/worlds/delete-world) |
| 非同步任務 | [非同步任務](/zh-Hant/guides/async-jobs-polling) |
| 3D 生成指南 | [3D 生成指南](/zh-Hant/guides/3d-generation) |
